이 폴더는 설치 파일을 만들 때 **자료 꾸러미가 담기는 자리**입니다.

  · _core 설치 파일 — 이 안내문만 들어갑니다 (프로그램만).
  · _full 설치 파일 — `npm run dist:full` 이 자료 꾸러미를 여기에 복사합니다.

프로그램은 설치판 안의 이 폴더에서 `manifest.json` 을 찾습니다.
없으면 '담겨 온 자료 없음' 으로 지나가므로, 두 갈래를 코드에서 나눌 필요가 없습니다.

여기에 손으로 넣지 마세요 — `npm run dist:full` 이 만들 때마다 비우고 다시 채웁니다.
