# 변경 이력

> 무엇이 언제·왜 바뀌었는지. 사용법은 [USER-GUIDE](USER-GUIDE.md),
> 문제 해결은 [TROUBLESHOOTING](TROUBLESHOOTING.md),
> 설계 판단의 배경은 [PLAN.md](../PLAN.md) 와 `docs/plan-service-tab*.md` 를 보세요.
>
> 최종 갱신 2026-09-03

---

## 2026-09-03 — 예배 순서: 탭 이동에 안 사라진다 / 찬양 검색 / 불러온 순서 저장

**요청**: ① 저장하지 않고 '찬양' 탭에 갔다 돌아오면 짜던 순서가 기본 유형으로
되돌아간다 ② '예배 순서'에서 「은혜」가 검색되지 않는다 (찬양 탭에서는 된다)
③ 불러온 순서를 고쳐 저장하는 길이 없다 — 이름을 정확히 맞혀 '덮어쓰기' 하는 것뿐이다.

### ① 편집 초안을 담아 둔다

탭은 조건부 렌더다. '찬양' 을 누르는 순간 `PlanPanel` 이 언마운트되고 `useState` 가
전부 사라진다. 돌아오면 '아무것도 열지 않았으면 첫 유형을 연다' 규칙이 다시 돌아
**기본 유형이 들어찬다** — 짜 놓은 순서가 통째로 없어진다.

`sessionStorage` 에 초안(편집 중인 유형·항목·저장 여부·커서·펼친 항목)을 담고
첫 렌더에 되살린다 (`src/control/panels/plan-draft.ts`).

- 모듈 변수가 아니라 `sessionStorage` 인 이유: 브라우저 새로고침까지 견딘다.
  OBS 안내 배너를 보고 F5 를 누르는 일이 실제로 있다. 탭을 닫으면 비워진다 —
  '저장하지 않은 편집' 은 다음 예배까지 끌고 가면 안 되는 것이라 이 수명이 맞다.
- `useState(() => …)` 로 **첫 렌더에** 넣는다. effect 로 넣으면 그 사이에 기본 유형이
  들어찼다가 초안이 덮어써 화면이 한 번 튄다.
- 편집 중인 유형·회차를 **삭제하면 초안도 버린다** — 남겨 두면 지운 것이 되살아난다.
- 저장이 막힌 브라우저(시크릿 창·용량 초과)에서는 조용히 넘어간다. 초안을 못 담는
  것이 예배 진행을 막을 이유는 없다.

### ② 찬양 검색 — 잘려서 안 보였다

원인이 둘이었다.

1. **정렬**. 제목 검색이 가나다순이라 '은혜' 로 찾으면 제목이 정확히 「은혜」인 곡이
   34곡 중 **17번째**였다. **일치 등급 순**으로 바꿨다 — 완전일치 → 앞부분 → 포함,
   같은 등급 안에서는 짧은 제목이 먼저. 찬양 탭도 함께 좋아진다.
2. **개수**. 예배 순서 탭은 8개만 받아 왔다. 30개로 올렸다 (`SONG_HIT_LIMIT`).

`total` 도 고쳤다. 늘 `hits.length` 를 넘겨서 `truncated` 가 영원히 false 였고,
"34곡 중 8곡만 보임" 을 **아무도 알려 주지 않았다**. 제목·가사 일치 곡을 실제로
세어 넘긴다 (4,528곡·69,514줄에서 18ms 실측 — 200ms 디바운스 안).
후보 아래에 `656곡 중 30곡만 보입니다` 안내가 붙는다.

### ③ 불러온 순서를 그 자리에 저장한다

'순서 불러오기' 로 회차를 열어 고치면 **저장할 길이 없었다**. '순서 저장하기' 로
`2026-08-17 주일 1부 예배` 를 **똑같이 맞혀 쳐야** 덮어써졌고, 한 글자만 달라도
회차가 하나 더 생겼다. 화면에는 무엇을 열었는지도 안 나왔다 — 맨 위 칸이 유형만
담아서 저장된 순서를 열면 '— 예배 유형 —' 로 비었다.

- **맨 위 칸이 '지금 고치는 중인 것'** 이 됐다. `<optgroup>` 으로 예배 유형과 저장된
  순서를 함께 담는다 — 열어 둔 것의 이름이 늘 보이고, 거기서 바로 고를 수 있다.
- **버튼이 열어 둔 것을 따라간다.**

  | 열어 둔 것 | 2번 버튼 | 3번 버튼 |
  |---|---|---|
  | 예배 유형 | 템플릿 업데이트 | 순서 저장하기 |
  | 저장된 순서 | **저장하기** | **다른 이름으로 저장** |

- **`저장 안 됨` 줄이 어디에 남는지 말해 준다** — `저장 안 됨 — 저장하기 를 누르면
  '2026-08-17 주일 1부 예배' 순서에 남습니다`. 버튼 이름이 상황마다 달라지므로
  표시만 '저장 안 됨' 이면 어느 버튼인지 매번 헷갈린다.
- **⧉ ✎ ✕ 가 순서에도 걸린다.** 전에는 유형에만 걸려, 순서를 열면 셋이 모두 회색이라
  **이름을 고칠 길이 없었다** — '제목을 정확히 찾아 저장하기 어렵다' 던 것이 이것이다.
- '다른 이름으로 저장' 은 **원본 회차의 날짜를 물려받는다.** 오늘로 찍으면 지난주
  순서의 사본이 `2026-09-03 · 2026-08-17 주일 2부` 처럼 날짜 둘이 붙어 읽히지 않는다.

**검증**: `tsc` · `vitest` 1,114개(신규 14개) · `vite build`.
격리 서버(7810)에 실제 곡 4,528개를 넣고 브라우저로 —
「은혜」가 후보 1번, `656곡 중 30곡만 보입니다` 안내, 항목을 넣고 '찬양' 탭에 갔다
돌아와도 그대로, 새로고침에도 그대로. 회차를 불러와 곡을 넣고 '저장하기' →
**회차가 늘지 않고 그 자리에 저장**(id 5, 항목 1 → 2), '다른 이름으로 저장' → 새 회차
하나만 생기고 날짜는 08-17 그대로, ✎ 로 이름을 바꿔도 항목 그대로. 콘솔 오류 없음.

---

## 2026-08-28 — 새찬송가 한/영 정렬을 끝냈다: 정상 36곡 → 498곡

**요청**: 하이픈을 정리하고, 자료를 반입하고, 영어를 한국어 줄에 맞춰 달라.

| | 8/27 | 지금 |
|---|---|---|
| A 영어 없음 | 8곡 | **0곡** |
| B 줄이 부족 | — | 83곡 |
| C 줄 수는 맞지만 끊김 | 561곡 | **64곡** |
| **D 문제 없음** | **36곡** | **498곡** |
| 실제로 부른 곡 중 문제 | — | **3곡** |

### 사람이 고친 것

사용자가 원본 자료에서 45곳을 손봤다 — 낱말이 줄을 넘어 쪼개진 41곳, 두 낱말 사이에
하이픈이 들어간 4곳. 그리고 가사가 달라 반입이 막혔던 4곡과 새 384장.
찾아 주는 도구는 `npm run report:hyphen`.

### 도구가 고친 것

1. **파서 버그** — `| en-vy` 의 `en` 을 언어 표로 읽고 떼어 버렸다. 새 104·215·316·
   402·520·568장의 영어에서 `en` 이 사라져 있었다.
2. **`Life-Line` → `LifeLine`** — 붙인 결과가 사전에 있으면 음절 하이픈으로 봤다.
   하이픈 **뒤가 대문자면 남긴다**는 규칙을 더했다.
3. **아멘을 한국어만 뗐다** — 227곡에서 한국어는 아멘 없이, 영어는 `… Amen.` 으로
   나가고 아멘 슬라이드가 또 붙었다. 짝을 맞춰 함께 뗀다. 243줄 → 6줄.
4. **크롤링 잔재** — `etc 파일 … © Daum Corp.` 가 287곡에 있었다. 0줄.
5. **영어 줄나눔** (`npm run hymn:realign`) — 고정 폭으로 잘린 영어를 한국어 줄에
   맞춰 다시 끊었다. 508곡 1,468섹션. 사람이 맞춰 둔 663개 섹션으로 재서 85.1% 재현.

### 지켜진 것

낱말은 하나도 바뀌지 않았다 — 645곡의 한국어·영어 낱말 열을 스냅샷과 대조해 확인했다.
줄 수도 25,213 로 같다. **끊는 자리만** 달라졌다.

### 남은 것

C 64곡. 한국어가 영어 어절보다 잘게 나뉜 곡(새 83장 「나의 맘에 근심 구름」의
`자나 깨나` `깨끗하게`)이 많아 영어만 다시 끊어서는 풀리지 않는다. 일부는 판정
기준의 헛것이다(새 84장은 실제로 맞아 있다).

> ⚠️ `npm run hymn:bilingual` 을 다시 돌리면 원본 폴더의 줄나눔으로 되돌아가
> realign 결과가 지워진다. 반입을 다시 하면 realign 도 다시 돌려야 한다.

## 2026-08-27 — 새찬송가 한/영 대조 점검: 정상은 36곡뿐이었다

**요청**: 새찬송가만 작업한다. 문제가 있는 곡의 목록과 내용을 정리해 볼 것.

### 숫자는 97.8% 인데 실제로는 깨져 있었다

| | |
|---|---|
| 영어 가사가 있는 곡 | 637 / 645 |
| 줄 합계 | 한국어 12,807 · 영어 12,525 (97.8%) |
| **줄 대조가 정상인 곡** | **36곡** |

영어가 한국어 줄에 맞춰 들어간 것이 아니라 **한 덩이 글을 30자쯤에서 기계적으로 자른**
상태다. 새 305장이 4=4 인데도

```
ko: 나 같은 죄인 살리신     en: Amazing grace! how sweet the
ko: 주 은혜 놀라워         en: sound! That saved a wretch like
```

`the / sound!` 처럼 낱말과 문장이 줄 사이에서 끊긴다. 서버가 만드는 슬라이드와 실제
출력 화면에서 그대로 나가는 것을 확인했다.

### 판정 기준

**둘째 줄 이후인데 소문자로 시작** = 문장 중간에서 끊김. 실측:

- 그런 줄 **4,822줄 (38.5%)** · 그런 곡 **600곡 (94%)**
- **첫 줄이 소문자인 경우는 0건** — 무작위가 아니라 순서대로 자른 증거
- 줄 길이의 28% 가 28~34자에 몰려 있다 — 고정 폭으로 자른 흔적

### 기존 리포트가 놓친 이유

`report:missing-en` 은 **짝이 없는 줄**을 센다(282줄·119곡). 줄 수가 맞으면 정상으로
본다. 그래서 새 305장 같은 곡이 걸리지 않았다. 세는 기준을 하나 더 뒀다.

### 갈래

| 갈래 | 뜻 | 곡 수 |
|---|---|---|
| A | 영어가 전혀 없다 | 8 (그중 5곡은 **아멘 응답송**) |
| B | 영어 줄이 한국어보다 적다 | 111 |
| C | 줄 수는 맞지만 문장이 끊긴다 | **490** ← 가장 큰 덩이 |
| D | 문제 없음 | 36 |

빠진 167개 섹션은 **전부 `verse`** 이고 후렴은 하나도 없다. 111개가 딱 1줄 차이다.

### 우선순위는 '실제로 부른 곡'

송출 기록이 있는 곡 중 문제가 있는 것이 **25곡**이다. 645곡을 다 손볼 수는 없으니
거기서 시작한다 — 새 305장(2,092회) · 278장(303회) · 365장(183회) · 1장(151회)…

```bash
npm run report:bilingual
```

**새 파일**: `scripts/report-bilingual-hymns.ts` (읽기 전용). 리포트는
`data/reports/bilingual-hymn_new-*.md` (336K · 6,094줄).

> 이번에는 **읽기만 했다.** 재분할은 방식을 정한 뒤에 한다.
> 다른 곡집(통일찬송가·찬미예수 2000 등 3,751곡)은 영어가 아예 없다 — 이번 범위 밖.

---

## 2026-08-23 (밤) — 새찬송가 641곡에 영어 가사

`가사모음` 프로젝트에서 만든 한/영 병기 자료를 넣었습니다. **새찬송가 645곡 중
641곡에 영어 가사**가 생겼고, 줄나눔도 그쪽 것으로 바뀌었습니다(한국어 한 줄 20자 —
이 앱의 목표값과 같습니다).

넣기 전에 원본에서 걸러 낸 것:

| 무엇 | 규모 |
|---|---|
| 웹 찌꺼기 (`© Daum Corp.` 등) | 338곡 359줄 |
| 악보용 음절 하이픈 (`rug-ged` → `rugged`) | 영어 줄의 86% |
| 아멘 (가사 → `has_amen` 플래그) | 303곡 |

**한국어 가사 글자가 다른 4곡(19·29·624·628)은 넣지 않았습니다** — 줄나눔을 바꾸는
작업이 가사 내용까지 바꾸면 안 되기 때문입니다. 원본에 남은 영어 오타도 고치지
않았습니다. 자세한 것은 [KNOWN-DATA-ISSUES §5](KNOWN-DATA-ISSUES.md).

```bash
npm run hymn:bilingual -- --dir <폴더>          # 미리보기
npm run hymn:bilingual -- --dir <폴더> --apply  # 실제로 넣는다
```

---

## 2026-08-23 (밤) — 송출 중인 줄이 라이트 테마에서 안 보였다

찬양 탭 **슬라이드** 목록에서 지금 나가는 줄의 바닥이 어두운 색으로 **박혀 있어**,
라이트 테마에서 검은 글자가 묻혔습니다. 테마 변수(`--live-wash`·`--live-ink`)로 바꾸고
**왼쪽 띠**를 더했습니다 — 색 하나에만 걸지 않아야 저조도·색약에서도 보입니다.

실측: 글자 대비 **15.4 : 1**(라이트) · **11.8 : 1**(다크), 라벨 6.3 / 5.9 : 1.

마우스를 올리면 보였던 것은 CSS 특정성 때문이었습니다 —
`button:hover:not(:disabled)` 가 `.slide-item:hover` 를 이겨 글자를 파랗게 만들었습니다.
가사를 읽는 목록이므로 hover 가 글자색을 바꾸지 않게 되받았습니다.

예배 순서 목록(`.cue-row`)은 이미 알파 틴트를 써서 문제가 없었습니다(12:1 이상).
대신 3차 재설계에서 버려진 `.plan-item*` 규칙 9개(같은 색이 박혀 있던)를 지웠습니다.

---

## 2026-08-23 — 타언어 가사 입력

찬양 탭에서 **＋ 타언어 가사 추가** 를 누르고 언어를 고르면 두 칸 창이 열립니다 —
**왼쪽에 한국어, 오른쪽에 번역.** 보면서 같은 줄 수로 적으면 **적는 즉시** `|` 형식으로
짝지어집니다(버튼을 누를 필요가 없습니다). 절마다 줄 수가 `1절 4/4 · 2절 0/4` 로
보여, 어느 절이 비었는지 적는 동안 알 수 있습니다.

**中文·日本語 는 화면에서 뺐습니다** — 넣은 가사가 0줄이었고, 고를 것이 많으면 정작
쓰는 한/영이 묻힙니다. 데이터는 그대로 읽으므로 그 언어 가사가 있는 곡에서는 계속
보입니다. 다시 켜려면 `lib/lang-select.ts` 의 `ACTIVE_LANGS` 에 한 줄이면 됩니다.

---

## 2026-08-22 (밤) — 태블릿 연결 QR

컨트롤 패널 **설정 탭 → '태블릿에서 조작하기'** 에 주소별 QR 이 뜹니다. 태블릿 카메라로
찍으면 로그인 화면이 열리고, 암호는 거기서 넣습니다.

**QR 에는 주소만 담습니다** — 암호를 담으면 그 그림이 곧 열쇠가 됩니다.

VPN(Tailscale 등) 터널 주소는 목록에서 뺐습니다. 실제로 `utun8`(100.x)이 태블릿 주소로
섞여 나와, 찍어도 열리지 않는 QR 이 생겼습니다.

---

## 2026-08-22 (오후) — 태블릿 접속 암호

태블릿으로 조작하려면 **암호를 넣어야 합니다**(보안 감사 권고 4). 이 PC 의 컨트롤 패널·
OBS·강사 모니터·프로젝터는 **묻지 않습니다** — OBS 브라우저 소스는 암호를 입력할 수
없기 때문입니다.

```bash
npm run password       # 암호를 정한다
presentation lan       # 태블릿에 연다 (암호가 없으면 그 자리에서 물어본다)
```

암호는 scrypt 해시로만 저장되고 평문은 남지 않습니다. 잊으면 다시 정하면 됩니다
(그러면 접속해 있던 기기가 모두 로그아웃됩니다). 암호가 없으면 서버가 LAN 으로
뜨기를 **거부합니다** — 경고만 하면 그대로 열리기 때문입니다.

자세한 내용은 [보안 감사 8장](SECURITY-AUDIT.md).

---

## 2026-08-22 — 문서 구조 정돈 · 검토 조치 · 통일찬송가 원제

| 커밋 | 내용 |
|---|---|
| `ad26abc` | **규칙을 `CLAUDE.md` 로** (자동으로 읽힌다 · `AGENTS.md` 는 같은 파일). 협업 보고서 2,119 → 414줄, 이력은 [work-log.md](work-log.md) 로 |
| `d4e8661` | (보안) **Origin 검사를 Host 위조로 뚫을 수 있었다** — DNS 리바인딩. 이 PC·사설망·`.local` 만 통과 |
| `9b4a6b4` | `coverage/` 55파일(35,077줄) 추적 해제 |
| `bc1be63` | `migrate-template-ids.ts` 도 쓰기 전에 스냅샷 |
| `1d8acff` | `PlanPanel.tsx` 3,147 → 2,244줄 · 파일 4개로 나눔 |
| — | **통일찬송가 483곡이 영어 원제를 물려받음** (대응 새찬송가에서) |

[Agent C 작업 검토 리포트](REVIEW-agent-c-2026-08-22.md)에서 나온 조치들입니다.
남은 일: `PlanPanel` 을 800줄 아래로 내리려면 상태를 훅으로 빼는 재설계가 필요하고,
영어 원제는 아직 **화면에 그려지지 않습니다**(API 로는 나갑니다).

---

## 2026-08-21 (오후) — 새찬송가 영어 원제 641곡

**요청**: 노션에 모아 둔 한/영 찬송가 제목 표를 점검하고, 쓸 만하면 넣을 것.

### 왜 필요했나

`songs` 표에 `title_alt`(원제)·`author`·`composer` 열이 있는데 **4,396곡 전부 비어
있었다.** 그래서 '새찬송가 305장이 어느 영어 찬송인가' 를 알 방법이 없었다. 영어 가사를
찾으려면 그것이 먼저다.

### 출처를 먼저 점검했다

| 확인 | 결과 |
|---|---|
| 규모 | 새찬송가 1~645장 전곡 · **영어 제목 645/645** |
| 한국어 제목 (우리 DB 대조) | 625/645 일치 |
| 통일찬송가 번호 | 481건 · **558 초과(불가능한 번호) 0건** |
| 영어 제목 | 634종 · 중복 7종은 대부분 정당(`Amen` 6회) |
| 실제 송출 29곡 | **23곡 커버** |

`HymnData.plist` 와도 맞대 봤다 — 통일 번호 477건 중 471 일치. 불일치한 새 606장은
**plist 가 틀렸다**(통일 606장은 존재하지 않는다. 통일찬송가는 558장까지).

### 넣지 않은 4건

출처에서 **행이 중복되며 뒤가 밀린** 구간이 있다.

```
번호   우리 제목          출처의 한국어        출처의 영어
631   우리 기도를         진리와 생명 되신 주    Hear Our Prayer, O Lord
632   주여 주여 우리를      우리 기도를          O Lord, O Lord!
638   주 너를 지키시고      모든 것이 주께로부터    All Things Come of Thee, O Lord
639   주 함께 하소서       주 너를 지키시고       The Lord Bless You and Keep You
```

계통적인 한 칸 밀림은 아니다(641/645 정상). **넣지 않고 표로 뽑아** 사람이 확인하게 했다.
스크립트가 넣은 뒤 그 넷이 정말 비어 있는지 스스로 검사한다.

### 한국어 제목은 가져오지 않았다

출처에 우리보다 오타가 많다 — `정혼한 처녀에세`·`취후기도`·`사람의 주님께`·`내밀리고`.
**우리 DB 가 원본**이고, 가져온 것은 **영어 원제와 통일 번호 둘뿐**이다.
`HymnEnRow.koreanForReference` 라는 이름에 그 뜻을 박아 뒀다.

### 영어 제목의 잡티만 기계적으로 뗐다

찬송가 영어 제목에는 `Pow'r`·`'Tis`·`Jesus'` 처럼 **뜻이 있는 축약**이 흔해, 지우면
제목이 틀린다. 셋만 건드린다.

| 뗀 것 | 예 |
|---|---|
| 소문자를 쪼갠 음절 하이픈 | `Cleans-ing` → `Cleansing` (새 259) |
| 낱말 끝에 홀로 붙은 둥근 아포스트로피 | `Ocean’ Divine` → `Ocean Divine` (새 302) |
| 낱말 안의 둥근 아포스트로피 | `God’s` → `God's` |

### 결과

```
넣었습니다: 원제 641곡 · 연결 66건
확인: 원제 641곡을 다시 읽었습니다
확인: 어긋난 4곳(631·632·638·639)은 비어 있습니다
```

`song_links` 856 → **988행**. 다시 넣어도 0건(여러 번 해도 같다).

| 새 | 우리 제목 | 원제 |
|---|---|---|
| 305 | 나 같은 죄인 살리신 | Amazing Grace! How Sweet the Sound! |
| 69 | 온 천하 만물 우러러 | All Creatures of Our God And King |
| 357 | 주 믿는 사람 일어나 | Encamped Along the Hills of Light |

**새 파일**: `lib/hymn-en-titles.ts` · `tests/unit/hymn-en-titles.test.ts` ·
`scripts/import-hymn-en-titles.ts` · `data/hymn-en-titles.tsv`(출처, gitignore)

### 어긋난 구간을 우리 제목 기준으로 판정했다 (같은 날 후속)

사용자 결정: '우리 제목을 사용해줘'. 그런데 **'제목으로 짝짓기' 를 일괄 적용하면 안 된다** —
두 구간이 서로 다르게 밀렸기 때문이다.

```
번호   우리 제목          출처 한국어          출처 영어
630   진리와 생명 되신 주    진리와 생명 되신 주      Spirit of Truth, of Life, of Power
631   우리 기도를         진리와 생명 되신 주 ←중복   Hear Our Prayer, O Lord   ← 영어는 번호에 맞다
632   주여 주여 우리를      우리 기도를           O Lord, O Lord!           ← 영어는 번호에 맞다
633   나의 하나님 받으소서   나의 하나님 받으소서      Take These Gifts          ← 다시 맞음

638   주 너를 지키시고     모든 것이 주께로부터      All Things Come of Thee   ← 두 열이 함께 밀렸다
639   주 함께 하소서      주 너를 지키시고        The Lord Bless You and Keep You
```

631·632 는 **한국어 열만** 중복되며 밀렸고 영어는 번호에 맞다. 638·639 는 **두 열이 함께**
밀렸다. 한 규칙으로 뭉치면 631·632 가 틀린다.

그래서 자동 규칙이 아니라 **사람이 판정한 표**(`RESOLVED_SUSPECTS`)를 코드에 두고,
줄마다 **근거**를 적었다.

| 새 | 우리 제목 | 넣은 원제 | 근거 |
|---|---|---|---|
| 631 | 우리 기도를 | Hear Our Prayer, O Lord | 「우리 기도를」의 정석 영어 응답 |
| 632 | 주여 주여 우리를 | O Lord, O Lord! | '주여 주여' ↔ 'O Lord, O Lord' 대응 |
| 638 | 주 너를 지키시고 | The Lord Bless You and Keep You | 민수기 6:24 축도와 일치 |
| 639 | 주 함께 하소서 | **(비움)** | 출처에 이 곡이 없다 — 짐작해 채우지 않는다 |

```
넣었습니다: 원제 3곡 · 연결 0건
확인: 어긋난 구간 — 판정한 3곡은 채워지고, 나머지는 비어 있습니다
```

**원제가 채워진 곡: 644 / 645.** 남은 하나(새 639)는 출처에 없어서 비워 뒀다.
스크립트의 자기 검증이 '판정한 것은 채워졌고 나머지는 비어 있다' 를 확인한다 —
경계를 코드가 지킨다. 테스트 931개 통과.

> ⚠️ `title_alt` 는 저장·백업만 되고 **검색·화면에는 아직 쓰이지 않는다.** 화면에 보이게
> 하는 것은 별도 작업이다.

---

## 2026-08-21 — 기능 매뉴얼(그림) · 문서 실측 대조

**요청**: diagram-design 스킬로 현재 프로젝트의 기능 매뉴얼을 그래프로.

### 그림 한 장

[docs/feature-map.html](feature-map.html) — 1280×720(16:9) 한 장. 왼쪽에서 오른쪽으로
**자료**(읽기 전용 원본 → 앱 DB) → **준비·조작**(컨트롤 패널 → 예배 순서 → 템플릿) →
**표시**(OBS·강사 모니터·프로젝터). 가운데 **라이브 상태**가 허브다.

노드 상한(9개) 때문에 그림에 못 담은 것 — 탭 6개, 항목 9종, 화면 4개가 각각 하는 일 —
은 그림 아래 카드 3장에 넣었다.

### 색은 이 앱의 것을 쓴다

스킨을 `src/control/styles.css` 에서 그대로 가져왔다 (`--bg` → paper, `--fg` → ink,
`--accent` → accent, `--ok` → link). 다크 값도 앱의 `prefers-color-scheme` 블록에서
가져왔으므로 두 스킨 모두 앱의 색이다. `~/.diagram-design/profiles/sermon-presentation.md`
로 저장하고 `.diagram-design` 마커를 뒀으니, 다음 다이어그램도 같은 색으로 나온다.

**앱의 '송출 중' 빨강 `#d42a1f` 은 일부러 뺐다.** 제품에서 가장 큰 소리를 내는 색이고
'지금 회중에게 나가고 있다' 는 뜻으로 아껴 둔 것이라, 다이어그램 강조에 쓰면 뜻이 묽어진다.

### 문서를 실측으로 대조했다

그림을 그리려고 기능을 세다가 문서가 뒤처진 것을 찾았다.

| 낡은 곳 | 실제 |
|---|---|
| README `lib/` 목록 | **15개가 빠져 있었다** (projector-view·verse-quotes·kyodoc-parser 등) |
| README `server/db`·`routes` | db 2개(readings·snapshot)·routes 2개(readings·backgrounds) 누락 |
| README `scripts/` | **7개 누락** (import-kyodoc·remove-text-quotes·make-projector-icons 등) |
| README `public/` | stage·projector 누락 |
| README 프리셋 수 | '9종' → **8종**. 설명도 '사본이 만들어진다' → '덮어쓰고 되돌릴 수 있다' 로 |
| PLAN.md 프리셋 수 | '7종' → **8종** |
| README 기능 목록 | 교독문 두 벌·인용구·프로젝터가 없었다 |

숫자를 손으로 세지 않고 파일 목록과 대조하는 스크립트로 확인했다.

---

## 2026-08-20 (밤, 후속 2) — 저장해도 인용구가 남는다 · 프로젝터 순서 표시 글자 통일

**사용자 지적 두 가지.**

### ① 저장하면 인용구가 성경 항목으로 되돌아갔다 (버그)

> 인용구로 추가한 후에 '템플릿 업데이트' 를 클릭하면 인용구들이 모두 '성경' 처럼
> 타이틀과 본문으로 바뀐다. 매주 미리 준비한 후에 '템플릿 업데이트' 로 저장을
> 대신해서 쓸 것 같은데, 인용구가 바뀌어 버리면 인용구로 쓴 의미가 없어진다.

원인은 저장 경로의 **화이트리스트**다. `normalizeItems` 는 아는 필드만 하나씩 옮겨
담고 나머지는 버린다 — 모르는 쓰레기가 순서표에 박히지 않게 하는 옳은 설계다. 그런데
인용구를 만들면서 거기에 `quote`·`preview` 를 **더하지 않았다.** 화면에서는 잘 보였으므로
저장하기 전까지 알 수 없었다.

고친 것: 그 두 필드를 화이트리스트에 넣었다.

| 규칙 | 왜 |
|---|---|
| `quote` 는 **`true` 만** 받는다 | `'yes'` 같은 값이 박히면 판정이 흐려진다 |
| `preview` 는 **인용구일 때만** 담는다 | 본문 낭독에는 쓰이지 않는다 — 뜻 없는 값을 남기지 않는다 |
| `preview` 는 길이를 잘라 담는다 | 라벨이다 (`PREVIEW_MAX`) |

**같은 부류의 버그를 막는 테스트를 만들었다** — `tests/integration/plan-item-roundtrip.test.ts`.
종류마다 **모든 선택 필드를 채운 항목**을 저장 경로에 통과시켜 하나도 빠지지 않았는지
본다. 새 필드를 넣고 그곳을 잊으면 실패한다. 나머지 다섯 종류(성경·찬양·교독문·순서
표시·전례문)를 대조했고 빠진 것은 없었다.

### ② 프로젝터의 순서 표시 글자 크기를 고르게

> 프로젝터 화면의 '예배 순서' 표시는 글자 크기를 리듬있게 하지 말고 첫 글자의 크기로
> 동일하게 해줘.

대비가 낮은 프로젝터에서는 작아진 글자가 먼저 뭉개진다. 두 곳을 막았다.

| 무엇 | 어디서 |
|---|---|
| 자동 리듬 | `projectorTemplate()` 이 `titleRhythm: 0`, `titleRhythmY: 0` 을 **명시한다** |
| **손으로 맞춘 글자별 조정** | `projector.css` 가 `.rhythm-char` 의 인라인 크기를 되돌린다 |

자동 리듬은 '전체' 프리셋에 설정이 없어 이미 0 이었다(`clampRhythm(undefined)`).
그래도 명시한 이유: 나중에 그 템플릿에 리듬을 넣어도 프로젝터는 고르게 남는다.

진짜 원인은 **수동 조정**이었다 — 사용자 순서표의 `찬양과경배` 에 글자별 크기가
`0.8`·`0.9` 로 박혀 있었다. 그것은 항목에 담겨 있고 렌더러가 **인라인**으로 꽂으므로
템플릿으로 막을 수 없다. CSS 로 덮었다. 인라인을 이기려면 `!important` 뿐이고,
**렌더러가 만든 표시용 인라인을 표시 전용 화면에서 되돌리는** 이 자리가 그것이 옳은
경우다 (색·크기처럼 CSS 변수로 되는 것은 `body` 선언으로 처리했다 — 이것만 예외다).

`1em` 은 부모 글자 크기 = **첫 글자의 크기**다 (리듬 표의 첫 칸이 `1.0`).

### 실측

| 확인 | 결과 |
|---|---|
| 인용구 넣고 '템플릿 업데이트' | 두 줄 모두 그대로 (`고전 1:3 …`, `고전 1:4 …`) |
| DB | `quote: true` · `preview` · `paging: 'verse'` 살아 있음 · 일반 성경 항목과 구분됨 |
| 프로젝터 | 인라인이 `0.8em`·`0.9em` 인데 **실제 크기는 다섯 글자 모두 96px** · 이동 `none` |
| OBS(미리보기) | `120 · 115.2 · 96 · 108 · 108px` — **리듬 그대로** |
| 테스트 | 909개 통과 (왕복 10개 · 프로젝터 4개 추가) |

---

## 2026-08-20 (밤, 후속) — 인용구가 네 화면에서 같은 글로 나온다

**사용자 지적**: 인용구를 실행하면 네 화면의 말씀 형식이 모두 다르다.

```
관리자      고전 1:3 하나님 우리 아버지와…
OBS         고전 1:3 ⏎ 3 하나님 우리 아버지와…
강사 모니터   3 하나님 우리 아버지와… ⏎ 고전 1:3
프로젝터     하나님 우리 아버지와…
```

### 왜 넷이 달랐나

네 화면이 **서로 다른 렌더러**를 쓰고 참조·절 번호 규칙이 제각각이었다.

| 화면 | 규칙 |
|---|---|
| 관리자 목록 | `describeItem` 이 참조 뒤에 미리보기를 붙인다 |
| OBS | 참조를 **별도 줄**(`#reference`)로 위/아래에 그리고 절 번호를 붙인다 |
| 강사 모니터 | 절 번호를 **늘** 붙이고(설정을 보지 않았다) 참조를 아래에 붙인다 |
| 프로젝터 | '전체' 템플릿이 참조·절 번호를 **둘 다 끈다** |

`showReference: 'inline'` 이라는 값은 타입에 있었지만 **구현이 없었다** — `'none'` 이
아니면 무조건 별도 줄이었다.

### 참조를 **본문 글자에 넣었다**

참조를 별개 요소로 두는 한 렌더러 셋을 각각 고쳐야 하고, 프로젝터는 템플릿이 참조를
끄므로 예외가 또 필요하다. **규칙이 넷이면 넷은 다시 어긋난다.**

글자로 넣으면 네 화면이 그것을 '본문' 으로 받으므로 어긋날 자리가 **구조적으로**
없어진다. 접두사는 **항목의 `ref`** 를 쓴다 — 목록 줄과 글자까지 같아진다
(서버가 주는 `slide.reference` 는 정식 이름 `고린도전서 1:3` 이라 목록과 달라진다).

DB 는 손대지 않는다. 화면에 보낼 **사본**을 만든다 (`quoteSlides` 는 불변이다).

### 함께 끈 것

| 무엇 | 왜 |
|---|---|
| 절 번호 | `고전 1:3` 에 이미 절이 있다 — 안 끄면 `고전 1:3 3 하나님…` |
| 별도 참조 줄 | 붙였는데 아래에도 나오면 같은 것이 두 번 나간다 |
| 보조 역본 접두사 | 붙이지 **않는다** — 참조가 두 번 보인다 |

항목의 **참조 표기를 `끔`** 으로 두면 접두사를 붙이지 않는다 — 그 토글이 인용구에서도
뜻을 갖는다.

### 강사 모니터가 설정을 따르게 했다

`stage.js` 가 절 번호를 **늘** 붙이고 있었다(`verse.verse + ' ' + verse.text`).
`display.verseNumbers === false` 를 보게 했다. 이 파일은 의존성 0 이라 규칙을 두 곳에
적게 되므로 `tests/unit/quote-consistency.test.ts` 가 **파일을 읽어** 묶어 둔다.

### 📖 성경(본문 낭독)은 조금도 바뀌지 않았다

`resolveItem` 이 `item.quote` 일 때만 이 변환을 부른다. 실측으로 확인했다 —
`창 1:1-6` 은 절 번호 `1`, 참조 줄 `창 1:1-6`, 보조 역본까지 예전 그대로다.

### 실측 (네 화면 동시)

| 화면 | 나온 글 |
|---|---|
| 관리자 목록 | `고전 1:3 하나님 우리 아버지와 주 예수 그리스도로부터 은혜와 평강이 있기를 원하노라` |
| OBS(미리보기) | 같음 · 참조 줄 없음 · 절 번호 없음 |
| 강사 모니터 | 같음 (한 줄) |
| 프로젝터 | 같음 · 참조 줄 없음 · 절 번호 없음 |
| 성경 낭독 대조 | `1태초에 하나님이…` + `1In the beginning…` · 참조 줄 `창 1:1-6` — **그대로** |
| 테스트 | 895개 통과 (`verse-quotes` 9개 · `quote-consistency` 9개 추가) |

> 화면마다 옛 판이 떠 있으면 여전히 다르게 보인다 — 그 창을 새로고침해야 한다
> (TROUBLESHOOTING §2.4.13).

---

## 2026-08-20 (밤) — 인용구를 '성경 절 낱개'로 바꿨다

**요청**: 광고와 인용구가 기능이 겹친다. 인용구는 성경 본문을 **카테고리가 아닌 개별
슬라이드로** 가져오는 기능으로 바꾼다. 성경에서 `창 1:1-6` 은 '창 1:1-6' 타이틀 밑에
6절이 들리지만, 인용구에서는 **타이틀 없이** 창 1:1 … 창 1:6 이 나온다.

### 겹쳐 있던 것이 맞았다

광고와 인용구는 화면에서 **똑같이 그려졌다**(둘 다 `kind: 'text'`). 실제 차이는
아이콘과 `↩ 직전으로` 뿐이었다. 그리고 사용자가 갖고 있던 인용구 항목의 내용이
바로 **`창 3:15`** 였다 — 이미 참조를 인용구로 쓰려 했고, 지금은 글자 그대로 화면에
나갔다. 요청과 정확히 맞는 증거였다.

### 무엇이 바뀌었나

| | 📖 성경 | 💬 인용구 |
|---|---|---|
| `창 1:1-6` | 항목 **하나** | 항목 **여섯** (`창 1:1` … `창 1:6`) |
| 목록 줄 | 머리 줄 + 펼치면 6장 | 여섯 줄, 각 줄에 **본문 함께** |
| 펼치기 | `▸` 로 장을 고른다 | 없다 — **한 번 클릭으로 송출** |
| 제목 슬라이드 | 참조가 먼저 나간다 | **나가지 않는다** |
| `↩ 직전으로` | 없음 | 있음 (유지) |

### 성경 항목과 **같은 구조**로 담았다

인용구도 `type: 'bible'` 이고 `quote: true` 표시만 다르다. 자유 글자로 담으면 역본을
바꿀 수 없고 본문이 순서표에 복사된다. 같은 구조로 두면 역본·참조 표기·절 번호·글꼴·
템플릿이 **그대로** 쓰이고, **본문은 언제나 DB 가 원본**이다.

### 목록 줄의 본문은 항목이 담는다

`창 1:1 태초에 하나님이 천지를 창조하시니라` 처럼 보여 달라는 요청이었다. 그런데
컨트롤 패널은 **펼친 항목 하나만** 본문을 조회한다(`resolveItem`) — 낱개로 흩어진
여섯 줄의 본문을 조회 없이 그릴 방법이 없다. 그래서 `preview` 를 항목에 담는다
(`songTitle`·`readingTitle` 과 같은 이유).

**라벨일 뿐이다.** 화면에 나가는 본문은 언제나 DB 에서 다시 읽으므로 잘려 있어도 되고
없어도 된다(없으면 참조만 보인다). 역본을 바꾸면 새 역본으로 다시 읽는다.

### 막은 버그 두 개

- **비동기 뒤의 덮어쓰기**: 역본을 바꾼 직후 미리보기를 다시 읽는 경로가 옛 `items`
  배열로 덮어써 **역본 변경을 되돌렸다.** `patchItems` 가 함수형 갱신을 받게 하고
  그 경로를 함수형으로 바꿨다. 항목을 여러 개 넣는 `insertItems` 도 같게 했다 —
  자리는 어긋나도 되지만 항목이 사라지면 안 된다.
- **한 번에 여러 개**: `insertItem` 을 여섯 번 부르면 `items` 가 이 렌더의 값이라
  마지막 하나만 남는다. `insertItems` 로 한 번에 넣는다.

### 옛 항목

옛 자유 글자 인용구는 **그대로 나간다**(광고처럼 글자가 뜬다). 새로 만들 수만 없다.
사용자의 한 건(`창 3:15`)은 요청에 따라 지웠다 — `scripts/remove-text-quotes.ts`
(미리보기 기본 · 무엇을 지울지 내용까지 보여 준다 · `--apply` 가 스냅샷을 먼저 뜬다).

### 실측

| 확인 | 결과 |
|---|---|
| `창 1:1-6` 넣기 | 6줄 · `인용구 6개를 넣었습니다 — 창 1:1-6` |
| 목록 줄 | `창 1:1 태초에 하나님이 천지를 창조하시니라` (요청한 모양) |
| 펼치기 | 인용구 줄에 `▸` 없음 · 성경 줄에는 있음 |
| 한 번 클릭 | 제목 없이 `4빛이 하나님이 보시기에 좋았더라…` 가 바로 나갔다 |
| `↩ 직전으로` | `(순서 표시)` 로 뜨고, 누르면 설교 화면으로 돌아왔다 |
| 역본 바꾸기 | 목록 줄이 `창 1:2 Now the earth was formless…` 로 다시 읽혔다 |
| 실제 데이터 | 옛 인용구 1개 삭제 · 주일예배 22 → 21개 · 순서 표시 8개 그대로 |
| 테스트 | 877개 통과 (`verse-quotes` 17개 추가) |

---

## 2026-08-20 (저녁) — 프로젝터: 주소줄을 실제로 없애기

**사용자 재요청**: 스크린샷으로 제목줄('프로젝터 — Sermon Presentation')과
출처줄('127.0.0.1:7777/projector/')을 빨간 박스로 표시하며 "없앨 수 없나".

### 기능은 있었지만 **찾을 수 없었다**

전체 화면(`F`)이 이미 있었는데 도달하지 못했다. 원인은 둘로 본다 — `F` 를 눌러야 하는
것을 몰랐거나, **키보드 초점이 브라우저 쪽에 있어서** `F` 가 페이지에 닿지 않았다.
창을 열고 바로 누르면 실제로 그렇게 된다. 둘 다 **클릭**으로 풀린다.

| 무엇 | 왜 |
|---|---|
| **화면을 클릭하면 전체 화면** | 가장 찾기 쉬운 동작. 클릭은 초점도 함께 가져온다 |
| 클릭으로 **나가지는 않는다** | 예배 중 실수로 벽에 브라우저 창이 드러나면 안 된다 |
| 막대를 누른 클릭은 제외 | `+` 를 누르려다 전체 화면이 되면 안 된다 |
| **전체 화면 선택을 기억한다** | 다음에 열면 첫 클릭·키 입력에서 저절로 들어간다 |
| 마우스 이동은 세지 않는다 | 브라우저가 사용자 동작으로 인정하지 않아 거절되고, 기회를 잃는다 |
| 사람이 직접 나가면 기억을 끈다 | 창 모드로 쓰겠다는 뜻이다 |
| 안내는 전체 화면이 아닐 때만, 막대와 함께 | 상시 표시하면 프로젝터 번인 위험 |

### '앱으로 설치' 를 붙였다 (PWA)

매주 쓰는 화면이라면 설치판이 `F` 보다 편하다 — **애초에 주소줄이 없는 창**으로 열리고,
`display: fullscreen` 이라 처음부터 전체 화면으로 뜬다. 클릭조차 필요 없다.

아이콘 PNG(192·512)는 **코드로 그린다** — `scripts/make-projector-icons.ts` 가
`node:zlib` 만으로 만든다. 이미지 도구를 깔지 않아도 되고, 무엇을 그렸는지 읽어서 알 수
있다(바이너리는 diff 가 되지 않는다). 검은 바탕에 흰 가로 막대 셋 — 벽에 비친 글줄.

설정 탭에 **프로젝터로 띄우기** 카드를 넣었다. 방법을 **찾을 수 있는 자리**가 필요했다.

### 실측

| 확인 | 결과 |
|---|---|
| 매니페스트 | `200 · application/manifest+json` · `display: fullscreen` · 아이콘 2개 |
| 아이콘 | 530 B / 1,939 B · PNG 서명 확인 |
| **실제 클릭 · 실제 `F`** | 둘 다 `requestFullscreen` 호출 · 기억 `1` 저장 |
| 막대 클릭 | 기억을 바꾸지 않는다 |
| 거절돼도 화면 정상 | 본문 그대로 · `diag.errors = 0` |
| 테스트 | 860개 통과 (`projector-page` 7개 추가) |

> ⚠️ **전체 화면이 실제로 켜지는 것은 확인하지 못했다.** 검증에 쓰는 임베드 브라우저 창이
> Fullscreen API 자체를 막는다(`Permissions check failed`). **호출까지** 확인했고,
> 브라우저가 허용하는지는 표준 동작에 맡겼다.

---

## 2026-08-20 (오후) — 프로젝터 화면 (`/projector`)

**요청**: 가끔 프로젝터로 띄워야 한다. TV 가 아니라 프로젝터면 선명하지 않아 글자를
크고 두껍게 해야 한다. 강사 모니터처럼 별도 창으로, '전체 — 성경·찬송 겸용' 처럼 표시.
주소줄·시계·제목·다음 화면·연결됨·글자 크기 라벨은 전부 없애고 풀스크린.
마우스를 올릴 때만 막대가 보이고 그 안에서 `+ −` 로 글자 크기. 색 반전 옵션.

### OBS 와 **동시에 다른 배치**를 쓴다

이것이 핵심이다. 카메라 영상 위에는 '하단 두 줄'로 나가면서, 같은 내용이 프로젝터에는
큰 글씨로 나간다. 템플릿을 갈아 끼울 필요가 없다.

| 화면 | 템플릿 |
|---|---|
| OBS 송출 | 활성 템플릿 (예: 하단 두 줄) |
| 프로젝터 | **'전체 — 성경·찬송 겸용'** 고정 |

`server/ws.ts` 의 `templateFor()` 가 `projector` layer 에만 다른 템플릿을 보낸다.
`getTemplateOrDefault(-8)` 를 거치므로 **'전체' 프리셋을 고치면 프로젝터도 따라간다** —
눈에 보이지 않는 별도 설정을 만들지 않았다. 다른 템플릿을 편집하는 중에 뿌리는
`style:set` 은 프로젝터에 보내지 않는다 (벽에 비친 글이 춤추면 안 된다).

### 렌더러를 복제하지 않았다

`output.js` 는 1044줄이고 성경·찬양·교독문·전례문 렌더러가 모두 들어 있다. 프로젝터
페이지는 **같은 DOM id** 를 두고 같은 CSS·같은 스크립트를 불러온다. `projector.css`/
`projector.js` 는 덧칠만 한다. `tests/unit/projector-page.test.ts` 가 파일을 읽어
이 규칙을 지킨다.

`output.js` 는 `location.pathname` 이 `/projector` 로 시작하면 layer 를 스스로 정한다 —
쿼리에 맡기면 즐겨찾기에서 빠졌을 때 OBS 화면으로 집계된다.

### 서버 패치와 싸우지 않는다

서버의 `style:patch` 는 CSS 변수를 `<html>` 인라인에 꽂는다. 그런데 **CSS 변수는
상속으로 결정된다** — 값을 읽는 쪽에서 가장 가까운 조상이 이긴다. `body` 가 `html` 보다
가까우므로 `body.projector { --primary-color: … }` 가 서버 패치를 가린다.
`!important` 도, 인라인을 지우는 짓도 필요 없다.

### autoFit 을 껐다

키우면 넘치고, 넘치니 되돌려 깎아서 **글자 크기가 안 바뀌는 것처럼 보인다** —
2026-08-19 에 내가 오진한 현상이 정확히 이것이다. 조작자는 벽을 직접 보고 있으니
넘치면 `−` 를 누르면 된다. 대신 넘치는 동안 막대에 알린다.

### 둘째 줄의 바닥을 받쳤다

'전체' 템플릿의 둘째 줄은 42px 이다(첫 줄의 0.44배). OBS 에서 *둘째 역본*을 곁들일 때를
생각한 값인데, 프로젝터에서 둘째 줄은 곁가지가 아니다 — **한/영 병기 찬양의 영어 가사**,
**교독문의 회중 줄**이다.

| 줄 | 프로젝터 크기 |
|---|---|
| 둘째 줄 일반 | 첫 줄의 **0.72배 이상** (`max()` 로 받침 — 덮지 않고 아래로만 막는다) |
| 교독문 회중 줄 | 첫 줄과 **같은 크기** (교독문 전용 템플릿이 그렇다) |

### 주소줄은 지울 수 없다 ⚠️

웹페이지가 브라우저 주소줄을 없애는 방법은 없다(피싱 방지). **전체 화면**이 유일한
길이고, 그러면 주소줄·탭·창 테두리가 전부 사라진다 — 요청 두 개가 한 동작으로 모인다.
브라우저 규칙상 사람이 누르는 순간에만 되므로, 창을 처음 열면 6초간 안내를 띄운다.

### 실측 (1920×1080)

| 확인 | 결과 |
|---|---|
| 템플릿 격리 | 활성이 '하단'(68px)인데도 프로젝터는 **96px '전체'**. 68 → 30px 로 끌어도 96px 유지 |
| 접속 집계 | 프로젝터 버튼에 초록 점 · **OBS 는 미연결로 남는다** |
| 줌 · 반전 | 96 → 124.8px(130%) · 흰 바탕 `#fff`/글자 `#111`/둘째 `#8a4b00` · 새로고침 뒤 유지 |
| 교독문 | 인도자 124.8px = **회중 124.8px** |
| 한/영 찬양 | 한국어 96px/800 · 영어 **69.12px**/600 (42px 아님) |
| 넘침 경고 | 200% 에서 뜨고 100% 로 되돌리면 사라진다 |
| 테스트 | 853개 통과 (`projector-view` 7 · `projector-page` 10 추가) |

**남은 판단**: 순서표에서 항목마다 지정한 템플릿(교독문·순서 표시 등)을 프로젝터가
무시한다. 지금은 요청에 맞지만(늘 크고 두껍게), 항목이 명시적으로 고른 것은 따르게
바꾸는 선택지가 있다 — [plan-projector.md §9](plan-projector.md).

---

## 2026-08-20 — 교독문이 두 벌이 되었다 (통일찬송가용 / 새찬송가용)

**요청**: 지금 들어 있는 교독문은 통일찬송가의 것이다. 새찬송가 교독문 DB(`~/Desktop/Data/Kyodoc.sqlite`)
를 넣고, 교독문을 고를 때 '통일찬송가용' / '새찬송가용' 으로 구분해서 가져오게 해 달라.

### 왜 두 벌인가

찬송가마다 교독문이 다르고 **번호가 같아도 다른 글**이다 — 15번이 통일에서는 시편 51편,
새찬송가에서는 시편 27편이다. 덮어쓰면 예전 순서표의 교독문이 조용히 다른 글로 바뀐다.

### 무엇이 바뀌었나

| 곳 | 내용 |
|---|---|
| `responsive_readings` 표 | 기본 키가 `number` → **`(book, number)`**. 기존 76편은 `book='hymn_old'` 로 옮겼다 (자동, 여러 번 해도 같다) |
| `lib/kyodoc-parser.ts` (새 파일) | `NEW_KYODOC` 표 어댑터. `NAME`='1. 시편 1편', `DESCRIPTION`= 줄마다 `/` 로 끝난다. 통일찬송가 원본(txt)과 형식이 전혀 달라 어댑터를 따로 뒀다 |
| `scripts/import-kyodoc.ts` (새 파일) | 미리보기가 기본, `--apply` 로 넣는다. 원본은 **읽기 전용**으로 연다. 넣기 전에 스냅샷을 뜨고, **통일찬송가 76편이 그대로인지 스스로 확인**한다 |
| 예배 순서 탭 | 교독문 추가 칸 위에 **두 버튼**(편수 표시). 목록에서 새찬송가용은 `새 교독문 15. 시편 27편` |
| 항목 상세 | `새찬송가용 · 15번` 을 **보여만 준다.** 바꾸면 내용이 통째로 달라지므로 일부러 못 바꾸게 했다 |
| 화면 | 제목 슬라이드는 여전히 `교독문 15, 시편 27편` — **어느 찬송가인지 나가지 않는다.** 회중에게는 뜻이 없다 |

### 예전 순서표는 그대로 열린다

항목의 `readingBook` 이 **없으면 통일찬송가용**으로 본다. 새찬송가용을 넣은 항목에만
값이 붙으므로, 이미 만들어 둔 6개 순서표는 손대지 않아도 예전과 같은 글이 나온다.

### 넣은 결과

```
원본 행 137개 → 읽은 교독문 137편
번호 1~137 · 빠진 번호 없음
본문 줄 1542개 → 화면 795장
넣었습니다: 137편 (새찬송가용 0 → 137)
확인: 137편 · 줄 1542개를 다시 읽었습니다
확인: 통일찬송가용 76편은 그대로입니다
```

원본에서 발견한 문제 1건(**5번 '시편 8편'이 내용을 반복한다**)은 **고치지 않고 알렸다** —
[KNOWN-DATA-ISSUES §4.6](KNOWN-DATA-ISSUES.md) 참고.

### 확인

- 테스트 836개 통과 (`kyodoc-parser` 13개 새로 추가)
- 실제 화면: 두 버튼이 4번부터 갈리는 목록을 정확히 갈아 끼우고(통일 `시편 8편` / 새 `시편 5편`),
  15번을 넣으니 `새 교독문 15. 시편 27편` 이 통일 15번과 나란히 앉고, 본문이 시편 27편으로 나갔다
- 실제 DB: 순서표 6·템플릿 1·설정 1 그대로. 다시 반입해도 결과가 같다

> **서버를 껐다 켜세요.** 표의 구조가 바뀌었습니다.

---

## 2026-08-19 (오후) — 템플릿 정리 11개 요청 완료

| 커밋 | 내용 |
|---|---|
| `d677542` | **프리셋 덮어쓰기 · 원본 불러오기** — `getTemplate` 을 'DB 먼저 → 코드 대체' 로 뒤집었다. 코드가 곧 원본이라 '원본' 을 따로 저장할 필요가 없다 |
| `47223a7` | **프리셋 11 → 8개** 정리. 사용자 조정값(`id 3`)을 '하단' 프리셋으로. 순서표 참조 9곳 이관 |
| `79f9750` | **참조 표기·소제목·절 번호를 항목에서** 켜고 끈다 (모양은 템플릿, 켜고 끄기는 항목) |
| `ff66cbd` | **폰트를 예배 순서로** — 성경·찬양도 고딕/명조를 항목에서 고른다 |
| `eb92dd7` | **성경·찬양 탭에 프리셋+폰트** (겸용 셋만 고를 수 있다) |
| `bcb5270` | **항목을 누르면 제목을 띄운다** — `창 1:1-6` · `교독문 15, 시편 51편` |
| `3b39eaa` | **폴더로 번역 반입** — `언어/곡집/번호.txt` |

**'단일/이중' 과 '본문/찬양' 은 아무것도 가르지 않았다.** 역할은 내용이 정하고 템플릿은
모양만 정한다(`index === 0 ? primary : secondary`). 그래서 넷을 '하단' 하나로 합쳤다.

점검 보고서: [plan-template-cleanup.md](plan-template-cleanup.md)

---

## 2026-08-19 — 폰트 통일 · 다국어 가사

| 커밋 | 내용 |
|---|---|
| `7d83ac0` | (버그) **명조 체인 2순위가 고딕**이었다 — 나눔명조가 없는 PC 에서 명조를 골라도 고딕이 나온다. 체인을 `lib/korean-fonts.ts` 한 곳으로 모으고 **Noto Sans KR · Noto Serif KR** 을 대표로. 설정 탭에 설치 안내 |
| `e9aed07` | (버그) **3개 언어를 넣을 수 없었다** — 모든 `\|` 줄이 한 언어로 태그돼 저장이 HTTP 500. `\|en` `\|zh` 언어 표를 붙일 수 있게. 상한 2 → **3개** (성경의 주 역본 + 보조 2개와 같게) |
| `2b168b7` | 찬양 탭에 **언어별 비교·편집 격자**. 줄마다 언어를 모아 놓고 그 자리에서 고친다 |

**왜 격자인가**: 찬양에는 성경의 `장:절` 같은 외부 기준이 없어 언어별 줄 수가 어긋날 수
있다(새찬송가 1장: 한국어 2줄, 영어 원문 4줄). 기계가 맞출 수 없어 사람이 봐야 한다.
언어를 **열**로 놓으면 1440×900 랩탑의 편집 칸 479px 에 865px 이 필요해 좌우 스크롤이
생긴다(실측). 그래서 줄로 묶고 언어를 행으로 놓는다.

**함께 고친 것**: `mergeSecondaryLyrics` 가 中文 을 붙일 때 English 를 지웠다(데이터 손실).
`SongPanel` 이 자기 안에 갖고 있던 언어 토글 규칙·라벨 표를 공용 모듈로 옮겼다.

---

## 2026-08-18 — 표시 옵션 · 한/영 병기 준비

| 커밋 | 내용 |
|---|---|
| `4f2d318` | 강사 모니터 글자 크기(60~260%) · 전례문 폰트(고딕/명조)·크기 + **절대 행간** |
| `2f9840a` | (버그) 교독문·전례문의 폰트·크기를 고쳐도 **화면이 따라오지 않았다** — 나가 있는 항목을 다시 보내지 않아 ▶ 를 새로 눌러야 했다. 겸해서 사라져 있던 '예배용으로 올리기' 버튼 복구 |
| `ca650b4` | 찬양 탭 **영어 가사 붙여 넣기** — 영어만 넣으면 한국어 줄에 짝지어 준다. 남는 줄·모자란 줄을 절 이름과 함께 알린다 |
| `434d9c3` | (버그) 예배 순서 탭에 **찬양 표시 언어를 고를 칸이 아예 없었다** — 항목을 만들 때 정해진 언어로 굳어, 영어를 넣어도 순서표에서는 늘 한국어만 나갔다 |

**절대 행간**(`line-height: calc(1em + Npx)`): `1em` 은 글자 크기를 따라가고 `+Npx` 는
고정이라, 글자를 키워도 줄 간격이 함께 부풀지 않고 겹치지도 않는다. 상대 배수는 키울 때
간격이 벌어지고, 순수 px 는 키울 때 겹친다.

**측정 주의**: 출력 무대는 창 크기를 따라간다. 720p 창에서는 자동 축소가 최소 배율(0.6)까지
내려가 크기 증가를 상쇄하므로, 실측은 **1920×1080** 에서 해야 한다.

설계 문서: [한/영 병기](plan-bilingual-songs.md) · [템플릿 정리 점검](plan-template-cleanup.md)

---

## 2026-08-16

| 커밋 | 내용 |
|---|---|
| `08d48b5` | `chorus:merge --song <id>` — 곡을 콕 집었을 때만 **승인한 곡도** 후렴 병합. 일괄 실행은 여전히 건너뛴다 |

**데이터 작업**: 통일찬송가 40장 '주 하나님 지으신 모든 세계' 의 후렴을 각 절 뒤로 병합.
1,202곡 중 유일하게 남아 있던 후렴 섹션이었고, 승인한 곡이라 자동 병합이 건너뛴 것이었다.
백업 → 미리보기 → 사용자 승인 → 적용. 결과: 후렴 섹션 0, 가사 본문의 '후렴' 0, 승인 표시 유지.

---

## 2026-08-15 — 예배 순서 탭 재설계와 순서 표시

하루 동안 세 차례에 걸쳐 '예배 순서' 탭을 다시 만들고, 순서 표시 화면을 갖췄다.
설계 문서: [1차](plan-service-tab.md) · [2차](plan-service-tab-2.md) · [3차](plan-service-tab-3.md)

### 1차 — 탭 구조 (Agent C)

| 커밋 | 내용 |
|---|---|
| `1b9f387` | 예배 순서를 **첫 번째·기본 탭**으로 |
| `595ba78` | 구분(divider)·인용구 항목 종류 |
| `474e00f` | 2열 조밀 화면. **선택과 송출을 분리** |

작업 중 기존 버그 3건 발견·수정 — `useMeasure` 매 렌더 재생성(미리보기가 비어 있음),
`←→` 이중 처리, 실패 시 이전 슬라이드 잔류.

### 2차 — 유형·배경·자동 진행 (Agent J)

| 커밋 | 내용 |
|---|---|
| `94d8d2b` | 광고 **Enter 로 추가** · **순서 표시** 항목 종류 |
| `a5cfcb6` | **예배 유형 템플릿** + 순서 저장하기/불러오기 |
| `57d2d44` | (버그) 추가 바 종류 전환 시 입력창 포커스 유실 |
| `43ca860` | **배경 그림·반복 동영상** (템플릿별, 폴더+업로드) |
| `3602e75` | **예배 전 안내** — 구분 단위 자동 진행 (8초·순환) |
| `03d63e9` | OBS 없이 쓰기 안내 (전체화면 창은 지금도 가능) |

### 3차 — 한 열 구조 (Agent J)

| 커밋 | 내용 |
|---|---|
| `da1f25e` | **한 열**로. 펼치기(Tab)·항목별 템플릿. 규칙을 "슬라이드를 눌러야 나간다" 로 다듬음 |

이때 `CueItem.templateId` 가 **저장만 되고 쓰이지 않던** 것을 발견해 두 경로(단독 송출·덱 경계)
모두에 연결했다. WS 허브가 템플릿 변경을 방송하지 않던 것도 함께 고쳤다.

### 순서 표시 화면 만들기

| 커밋 | 내용 |
|---|---|
| `eee2877` | **순서 이름 \| 담당자 + 밑줄** 배치 (`kind: 'order'` 슬라이드) |
| `811a4d0` | 글자 리듬 + 명조 프리셋 |
| `0f7e465` | Noto Serif KR 체인 + **실제 사용 폰트 표시** |
| `a50be06` | 높낮이를 크기와 **따로** 조절 |
| `18bde1e` | **글자마다 슬라이더**로 직접 조정 |
| `d9f54d9` | 담당자 크기(항목별) |
| `d4575f7` | 순서·담당자 **테두리 두께**(항목별) |
| `95ede09` | 자동 기본값을 사용자가 정한 **네 글자 표**로 |
| `9877e02` | 자동은 **네 글자까지만**, 그 뒤는 직접 조정 |
| `6ad5711` | (버그) 2자 어절에서 리듬이 통째로 사라짐 |

### 안정성·편의

| 커밋 | 내용 |
|---|---|
| `6ec5236` | **옛 판 출력 페이지 자동 감지** — 컨트롤 패널이 새로고침을 안내 |
| `6210a41` | (버그) '공백' 이 아무 일도 하지 않음 |
| `64c1eec` | (버그) 한글 Enter 로 항목이 두 개 생김 · 구분 이동 버튼 |
| `32df2bf` | 성경 항목의 **주·보조 역본 선택** |
| `9dde289` | (버그) **블랙이 슬라이드를 바꾸면 풀림** · **예배 기본 설정** 신설 |
| `7c3002b` `729f5f3` `d620e63` | 찬송가 **절 번호** — 목록 → 첫 장에만 → 출력 화면에도 |

---

## 그 이전 (Phase 0~5, 곡집·가사 정리)

[work-log.md](work-log.md) 에 Phase 별로 정리돼 있습니다.
요약하면 — 성경 12역본 342,317절 통합, 찬양 1,202곡, 템플릿 시스템, 예배 순서,
곡집 다대다 구조, 후렴 병합(586곡), 절 간 운율 정렬, 줄나눔 검토 화면, git 도입.

---

## 이 프로젝트가 일하는 방식 (짧게)

- **추측 말고 실측.** 판단은 측정에서 나온다 — ESV 판본 비교, 운율 순위 규칙,
  폰트 대체 비율, 스크롤바 폭, 글자 크기 배수까지 브라우저에서 재고 정했다.
- **화면을 스스로 비우지 않는다.** 서버가 죽어도 출력은 마지막 내용을 유지한다.
- **자동 결과는 제안이다.** 사람이 승인해야 확정되고, 승인한 것은 자동이 건드리지 않는다.
- **되돌릴 수 없는 일 앞에는 백업과 미리보기.**
- 검증 3종은 항상 통과: `npx tsc --noEmit && npx vitest run && npx vite build`
  (2026-08-16 기준 **556개 통과**)
