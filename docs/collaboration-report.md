# 협력 보고서 (Collaboration Report)

> **여러 AI Agent / 개발자가 이 프로젝트를 동시에 작업할 때 상태를 공유하는 문서입니다.**
> 관련 문서: [README.md](../README.md) (사용법·구조) · [PLAN.md](../PLAN.md) (전체 설계와 판단 근거) ·
> [KNOWN-DATA-ISSUES.md](KNOWN-DATA-ISSUES.md) (원본 데이터의 알려진 문제)

**최근 갱신**: 2026-08-15 · 갱신자: Claude (Agent J, Opus 5) · 기준 커밋: `0f7e465`

> 이 문서는 다른 프로젝트(translateviewer)의 협업 규칙을 이 프로젝트에 맞게 옮겨온 것입니다.
> **0~1장의 협업 방식은 그대로**, 2장 이하의 구체적인 내용은 이 프로젝트 것으로 새로 썼습니다.

---

## 0. 이 문서 사용 규칙 (중요)

작업을 시작·완료할 때 **이 문서를 먼저 읽고, 끝나면 갱신**해 주세요. 목적은 **중복 작업과
충돌 방지**입니다.

1. **착수 전**: 4장(작업 보드)에서 그 항목이 `🔴 미착수` 인지 확인하고, `🟡 진행중`으로 바꾸고
   본인 이름(Agent 명)과 날짜를 적습니다.
2. **완료 후**: `🟢 완료`로 바꾸고 **검증 방법과 결과**를 3장에 한 줄 남깁니다.
   (주장만 쓰지 말고 "무엇으로 확인했는지" 적어주세요)
3. **파일 충돌 주의**: 5장 "파일별 점유 현황"에서 다른 Agent가 만지는 파일과 겹치면
   먼저 6장 "인계 메모"에 남기고 조율합니다.
4. **커밋 정책**: Agent 가 작업 완료 후 **직접 커밋·푸시합니다.**
   - **브랜치는 `main` 직통**입니다(2026-08-14 사용자 결정). 예배 직전 급한 수정에
     병합 단계가 끼면 느려지기 때문입니다. 대신 **작게 자주** 커밋해 충돌 범위를 줄입니다.
   - 논리적 단위로 나눠 커밋하고(성격이 섞이면 분리), 메시지에 **원인·조치·검증**을 담습니다.
   - ⚠️ **푸시 전 점검 필수**: `git diff --name-only origin/main..HEAD` 로
     `data/`·`.claude/settings.local.json` 이 포함되지 않았는지 확인.
   - 협업 중이므로 푸시 실패 시 `git pull --rebase` 후 재시도합니다.
   - **이력 재작성(force push, filter-repo)은 사용자 승인 필요** — 다른 Agent 작업을 깨뜨릴 수 있음.
5. **착수할 때 "복구 정보"를 먼저 쓴다** (⚠️ 가장 중요 — 아래 0.1 참고).
   사용량 만료·크래시는 예고 없이 오므로, **끝난 뒤 쓰는 기록은 남지 않습니다.**
6. **사용자 데이터를 되돌리기 전에 반드시 묻는다** (⚠️ 이 프로젝트 고유 — 아래 0.2 참고).

---

## 0.1 중단(사용량 만료·크래시·강제 종료) 대응 규칙 ★필수

Agent 작업은 **토큰/사용량 소진, 세션 만료, 크래시**로 언제든 중간에 끊길 수 있습니다.
그 경우 "완료 후 갱신" 규칙만 있으면 보드에 `🟡 진행중` 만 남아, 다른 Agent 가
**"누가 하는 중이니 손대지 말자"** 고 오해해 작업이 정지합니다.

### 규칙 A — 착수 즉시 3줄을 먼저 기록 (선행 기록)
작업을 시작하면 **코드를 만지기 전에** 8장(진행 중 작업 로그)에 다음을 씁니다.
```
- [시작 2026-08-15 14:30 / Agent B] 찬미예수 2000 곡집 가져오기
  계획: scripts/import-songbook-folder.ts 신규 / 폴더 규약(번호 제목.txt) 파서 재사용
  다음 단계: 파서 단위테스트 → 미리보기 실행 → 사용자 확인 → --apply
```
중단돼도 **이 기록이 남아** 다음 Agent 가 이어받을 수 있습니다.

### 규칙 B — 긴 작업은 단계마다 한 줄 갱신
30분 이상 걸릴 작업은 단계가 끝날 때마다 `다음 단계:` 줄만 고칩니다.
(마지막으로 갱신된 지점이 곧 **중단 지점**입니다)

### 규칙 C — 이어받는 Agent 의 판정 절차
`🟡 진행중` 인데 담당자가 응답 없거나 오래됐다고 판단되면 **아래 근거로 직접 확인**하세요.
추측하지 말고 실제 상태를 보고 판단합니다.

```bash
# 1) 미커밋 변경 = 진행 중이던 흔적 (무엇을 만지고 있었는지)
git status --short
git diff                     # 어디까지 고쳤는지

# 2) 파일 수정 시각 = 마지막 활동 시점 (중단 시각 추정)
ls -lt lib/*.ts server/**/*.ts src/control/**/*.tsx scripts/*.ts docs/*.md | head

# 3) 남은 잔여물 확인
git stash list               # 중단 전 stash 했을 수 있음
git branch --no-merged main  # 미병합 작업 브랜치

# 4) 코드가 실제로 동작하는 상태인지 (반쯤 고친 상태일 수 있음)
npx tsc --noEmit
npx vitest run
```

**판정 기준 (제안)**
| 상황 | 판정 | 조치 |
|---|---|---|
| 미커밋 변경 있음 + 파일 수정이 **수 시간 이상 전** | 중단됨 | 8장 로그를 읽고 이어받기. 상태를 `🟡 진행중(인계: 본인)` 으로 갱신 |
| 미커밋 변경 없음 + 보드만 `🟡` | **착수 직후 중단** | 그 항목은 사실상 미착수 → `🔴` 로 되돌리고 새로 시작 |
| 테스트/타입검사 실패 | **반쯤 고친 상태** | 먼저 그 부분을 완성하거나 되돌린 뒤 진행 (사용자에게 보고) |
| 판단이 애매함 | — | **사용자에게 확인** (임의로 되돌리지 말 것) |

### 규칙 D — 되돌리기는 사용자 승인 후
중단된 작업의 미커밋 변경을 **버리는 것(`git checkout --`, `git stash drop`)은 사용자 승인
없이 하지 않습니다.** 다른 Agent 의 미완성 작업일 수 있고, 커밋 전이라 복구가 불가능합니다.

### 규칙 E — 사용자가 할 수 있는 최소 조치
Agent 가 갑자기 멈췄을 때 사용자는 다음 한 줄만 남겨도 충분합니다.
```
- [중단 2026-08-15 15:10] Agent B 사용량 만료로 중단. 위 항목 이어받을 Agent 필요.
```

---

## 0.2 사용자 데이터 취급 규칙 ★이 프로젝트 고유·필수

**이 프로젝트에서 가장 위험한 실수는 코드 버그가 아니라 사용자 데이터를 지우는 것입니다.**
실제로 한 번 일어났습니다(3.2 사고 기록).

이 프로젝트의 데이터(`data/songs.sqlite`)에는 **사용자가 UI 로 직접 손본 가사**가 들어 있고,
**git 에 들어가지 않으므로 되돌릴 방법이 백업뿐**입니다.

### 지켜야 할 것

1. **사용자는 Agent 가 작업하는 동안에도 병행해서 데이터를 편집합니다.**
   백업과 DB 가 다르다고 해서 "내 테스트 흔적"이라고 단정하지 마세요.
2. **되돌리기 전에 반드시 묻습니다.** 데이터를 원상복구하는 작업은 사용자 승인 후에만.
3. **파괴적 스크립트 실행 전 백업**:
   ```bash
   cp data/songs.sqlite "data/backups/songs-before-$(date +%Y%m%d-%H%M%S).sqlite"
   ```
4. **`lines_source = 'manual'`(승인) 은 사람의 작업 표시입니다.**
   자동 스크립트뿐 아니라 **Agent 도** 이 곡을 건드리지 않습니다.
   찬양 탭 목록에 초록색 `승인` 표로 보입니다.
5. **자기가 만든 변경만 목록으로 관리**하고, 정리할 때 그 목록에 있는 것만 되돌립니다.
6. **원본 성경 DB(`~/Desktop/Data/BibleDB`)와 찬양 자료 폴더(`~/Desktop/Data/Praise`)는
   읽기 전용으로만 엽니다.** 절대 쓰지 않습니다.

### 스크립트는 미리보기가 기본
데이터를 바꾸는 스크립트는 모두 **미리보기가 기본이고 `--apply` 를 붙여야 실제로 씁니다.**
새 스크립트를 만들 때도 같은 규칙을 따르세요.

```bash
npm run lyrics:realign          # 미리보기 (아무것도 쓰지 않음)
npm run lyrics:realign -- --apply
```

---

## 0.3 실행 환경 전제 — 같은 PC 공유 ★필수

**모든 Agent 는 사용자의 같은 맥에서 같은 `data/` 를 씁니다** (2026-08-14 사용자 결정).
데이터를 git 이나 별도 저장소로 공유하지 않습니다 — 찬양 가사는 사용자 개인 자료이고,
성경 DB 는 원본에서 다시 빌드할 수 있기 때문입니다.

그래서 다음 세 가지가 **동시에 일어날 수 있습니다.** 각각 대비가 필요합니다.

### (1) 사용자와 Agent 가 같은 DB 를 동시에 편집
`data/songs.sqlite` 는 WAL 모드라 동시 읽기·쓰기 자체는 안전하지만, **논리적 충돌**은
막아 주지 않습니다. 사용자가 검토 화면에서 가사를 고치는 중에 Agent 가 일괄 스크립트를
돌리면 서로의 결과를 덮어씁니다.

- 데이터를 바꾸는 스크립트를 **`--apply` 로 돌리기 전에 사용자에게 알립니다.**
- 실행 직전 백업은 예외 없이 남깁니다(0.2 참고).
- `lines_source='manual'` 인 곡은 어떤 경우에도 건드리지 않습니다.

### (2) 포트 7777 을 여러 Agent 가 잡으려 함
서버가 이미 떠 있으면 다음 포트를 자동으로 잡지만, **옛 프로세스가 구버전 코드로 응답**해
"수정이 반영되지 않은 것처럼" 보입니다. 실제로 여러 번 겪었습니다.

```bash
lsof -ti:7777 | xargs kill -9    # 내 서버를 띄우기 전에 확인
```

⚠️ 사용자가 예배 준비 중 띄워 둔 서버일 수 있습니다. **죽이기 전에 물어보세요.**

### (3) 미커밋 변경이 섞임
`main` 직통이므로 다른 Agent 의 미완성 변경이 작업 트리에 남아 있을 수 있습니다.
착수 전 `git status --short` 로 확인하고, 내 것이 아닌 변경이 있으면 8장 로그를 읽고
0.1 규칙 C 로 판정합니다. **임의로 되돌리지 않습니다.**

---

## 1. 참여자 및 역할 기록

### 협업의 취지 — 사용량 한도 이어달리기

이 프로젝트의 협업은 **다른 회사의 AI 가 아니라, 같은 Claude Code 의 다른 계정**입니다.
시간 한도·주간 한도에 걸리면 며칠 동안 작업이 멈추므로, **계정을 바꿔 이어서** 작업합니다.

그래서 이 문서의 목적은 "여러 AI 의 조율"이 아니라 **이어달리기 배턴**입니다.
0.1 의 선행 기록(규칙 A)이 특히 중요합니다 — 한도는 예고 없이 걸립니다.

**Agent 이름은 계정 이니셜을 씁니다.** (`Agent C` = 계정 이니셜 C)

| 참여자 | 담당·기여 | 최근 작업일 |
|---|---|---|
| **사용자 (소유자)** | 요구사항 정의·우선순위 결정, 실제 예배 운영 검증, 가사 검토·승인, 방향 승인 | 계속 |
| **Agent C / Claude** (Opus 5) | Phase 0~5 전체 구현, 곡집 구조, 가사 줄나눔 재정렬, 검토 화면, git 도입, 협업 문서 이식, 예배 순서 탭 재설계 | 2026-08-15 |
| **Agent J / Claude** (Opus 5) | 예배 순서 2~3차 개선(유형 템플릿·순서 표시·한 열 재설계·항목별 템플릿), 배경 그림·동영상, 예배 전 안내, 글자 리듬·명조, 옛 판 출력 감지, 버그 4건(광고 Enter·추가 바 포커스·공백 무효·템플릿 미적용) | 2026-08-15 |

> 이 프로젝트는 **예배 중 사고가 최악의 결과**이므로, 편의 기능이라도 송출을 막지 않는지
> 먼저 확인하고 넣습니다.

---

## 2. 프로젝트 현재 상태 요약

| 항목 | 상태 |
|---|---|
| 무엇인가 | OBS Studio **브라우저 소스**로 성경 본문·찬양 가사를 송출하는 로컬 앱 |
| 기술 | Node.js 26(TypeScript 네이티브 실행, 빌드 없음) · Fastify 5 · `node:sqlite` · React 19 + Vite |
| 기능 완성도 | **실사용 가능**. 성경 12역본, 찬양 1,202곡, 템플릿 9종, 예배 순서(유형·저장·자동 진행·배경), 줄나눔 검토 |
| 테스트 | **518개 통과** (단위 + 통합, `npx vitest run` 약 3초) |
| 문서 | **3종으로 유지**(2026-08-14 결정) — README(사용법·구조) · PLAN(설계와 판단 근거) · KNOWN-DATA-ISSUES(원본 데이터 문제). 별도 analysis·handover·setup-guide 는 만들지 않습니다(PLAN 이 그 역할을 겸함) |
| 저장소 | `github.com/ccumgol/sermon-presentation` (**PRIVATE**) · `main` 직통 |
| 실행 환경 | **모든 Agent 가 사용자의 같은 맥·같은 `data/` 를 공유** (0.3 참고) |
| 배포 | 아직 로컬 실행만. **Phase 6(Electron 패키징) 미착수** — 다른 봉사자 PC 배포용 |

### 이 프로젝트를 처음 만지는 Agent 가 먼저 알아야 할 것

```bash
npm install
npm run bible:build     # 원본 성경 DB → data/bible.sqlite (읽기 전용으로만 원본을 연다)
npm run bible:verify    # 47개 항목 검증
npm start               # http://localhost:7777
```

- **`data/` 는 git 에 없습니다.** 성경 DB 는 위 명령으로 다시 빌드할 수 있지만,
  **찬양 가사(`data/songs.sqlite`)는 사용자가 직접 손본 개인 자료라 재현할 수 없습니다.**
  같은 맥에서 작업하므로 이미 있는 파일을 그대로 씁니다 — **지우거나 덮어쓰지 마세요**(0.2·0.3).
- **서버는 TypeScript 를 네이티브로 실행**합니다. 코드를 고치면 **서버를 재시작**해야
  반영됩니다(브라우저 새로고침만으로는 안 됨 — 실제로 여러 번 헷갈렸음).
- 컨트롤 패널(React)은 `npx vite build` 로 `public/app/` 에 빌드합니다.

---

## 3. 최근 완료된 작업

> 형식: 무엇을·왜·**어떻게 검증했는지**. 주장만 쓰지 않습니다.

### 3.1 Phase 0~5 (기반 → 운영 완성도)

| Phase | 내용 | 검증 |
|---|---|---|
| 0 | 서버·출력 페이지 골격, OBS 투명 배경 | 실제 OBS 합성 확인 |
| 1 | 성경 12역본 342,317절 통합 | 검증기 47항목 + 원본과 무작위 200절 대조 |
| 2 | WebSocket 상태 동기화, 컨트롤 패널 | 브라우저 실측 |
| 3 | 템플릿 8종, 실측 기반 자동 분할 | 1920×1080 숨긴 iframe 으로 실제 측정 |
| 4 | 찬양 1,202곡, 2언어 페어링 | 가져오기 리포트 + 통합 테스트 |
| 5 | 예배 순서, 항목 단위 이동 | 통합 테스트 |

자세한 설계 판단과 그 근거는 **[PLAN.md](../PLAN.md)** 에 Phase 별로 정리돼 있습니다.
새 Agent 는 코드를 고치기 전에 해당 Phase 절을 읽어 주세요 — **왜 그렇게 했는지**가
대부분 적혀 있어 같은 논의를 반복하지 않아도 됩니다.

### 3.2 ⚠️ 사고 기록: 사용자가 직접 수정한 가사를 되돌림 (2026-08-14, 복구됨)

**같은 실수를 반복하지 않기 위해 남깁니다. 0.2 규칙이 여기서 나왔습니다.**

- **무슨 일**: 브라우저 검증 중 백업과 DB 를 대조해 나온 차이를 **"내 테스트 흔적"으로
  단정하고 되돌렸습니다.** 실제로는 사용자가 병행 작업으로 직접 고치고 승인한
  가사 4곡과 승인 표시 6곡이었습니다.
- **왜 막히지 않았나**: "백업과 다르면 내가 만든 것"이라는 전제를 세웠고, 사용자가
  같은 시간에 작업할 가능성을 계산에 넣지 않았습니다.
- **복구**: SQLite WAL 파일(`data/songs.sqlite-wal`)에 남아 있던 옛 페이지에서 원문을
  찾아 복원했습니다. 네 곡 모두 **잘려 있던 단어를 붙인 수정**이었습니다
  (`영원/무궁하기까지`, `돌려/보내세`, `영광/받으옵소서`, `온누리/다스리시는`).
- **교훈 / 다른 Agent 가 지킬 것**: 0.2 참고. 요약하면
  1. 사용자는 Agent 와 **동시에** 데이터를 편집한다
  2. 되돌리기 전에 **묻는다**
  3. 파괴적 작업 전 **백업**한다
  4. `lines_source='manual'` 은 자동 작업뿐 아니라 **Agent 도** 건드리지 않는다

### 3.3 곡집 구조 · 가사 줄나눔 (2026-08-13~14)

- **곡집을 행으로**: `song_entries` 다대다 → 시리즈를 스키마 변경 없이 추가.
- **후렴 병합**: 원본이 인라인이던 것을 되돌림(586곡). 절 끝 어절이 후렴을 수식하는
  97곡에서 문장이 끊겨 있었음.
- **절 간 운율 정렬**: 같은 곡의 절들은 같은 멜로디라는 제약으로 공통 행 구조를 찾음.
  새256 이 `9.9.9.9`, 새305 가 `8.6.8.6`(Common Meter)으로 정확히 일치.
- **순위 규칙은 측정으로 결정**: 1,146곡에 네 규칙을 돌려 비교(`npm run measure:meter`).
  짝수 강제의 대가는 꼬리조각 0.38%p, 얻는 것은 홀수 행 286곡 → 0곡.

### 3.4 헬라어 자간 벌어짐 — 폰트 체인 (2026-08-14)

- **데이터는 정상**이었음. 원인은 폰트 체인이 총칭 `serif` 로만 끝나, 총칭 폰트가
  다음절 그리스어(U+1F00 대역)를 못 덮어 **글자마다 다른 폰트로 대체**된 것.
- 판별법: **다음절 폭 ÷ 단음절 폭 비율**. 1.02 면 자체 글리프, 1.10 이면 대체.
- 총칭 앞에 Times New Roman 을 넣어 819.6px → 526.6px, 비율 1.02 로 정상화.
- **프리셋만 고쳐서는 부족했음** — 저장된 사본은 JSON 으로 굳어 있어, 초기화 때
  **옛 문자열과 정확히 같을 때만** 새 체인으로 올리는 마이그레이션을 넣음.

### 3.6 예배 순서 탭 재설계 (2026-08-15, S-1~S-7)

설계·결정은 [plan-service-tab.md](plan-service-tab.md). 실행 결과만 적는다.

- 예배 순서를 **첫 번째·기본 탭**으로. 2열(순서 목록 | 항목 내용) + 기존 사이드바.
- **선택과 송출을 분리.** 항목을 눌러도 화면에 나가지 않는다(파란 테두리 = 선택,
  빨간 점 = 송출 중). 브라우저에서 "선택해도 출력이 안 바뀜"을 실측으로 확인.
- 인라인 추가 바로 성경·찬양·광고·인용구·공백·구분을 이 화면에서 추가.
  성경은 파싱 결과를 먼저 보여 주고 틀리면 넣지 않는다.
- `↩ 직전으로` — 인용구를 띄우기 직전 슬라이드로 복귀.

**작업 중 찾아 고친 버그 3개** (모두 이 작업 전부터 있던 것):

| 버그 | 증상 | 원인 |
|---|---|---|
| `useMeasure` 가 매 렌더 새 객체 반환 | 우측 미리보기가 **통째로 비어 있음** | 이걸 의존성으로 쓰는 effect 가 끝없이 재실행되며 요청을 시작하자마자 취소 |
| `←→` 이중 처리 | 미리보기 커서를 움직이면 **실제 화면까지 넘어감** | 전역·패널 핸들러가 둘 다 처리. 전역이 `defaultPrevented` 를 존중하도록 수정 |
| 실패 시 이전 슬라이드 잔류 | 깨진 항목을 골라도 **앞 항목 내용이 남아** 엉뚱한 걸 송출할 위험 | catch 에서 미리보기를 비우지 않음 |

⚠️ **오래된 순서표 발견**: `주일 1부 예배`(id 2)의 곡 항목이 `songId: 305`, `289` —
찬송가 **장 번호**이지 곡 id 가 아니다. 곡집 구조 도입 전 데이터로 보인다.
화면은 '곡을 찾을 수 없습니다'로 올바르게 알린다. **사용자 데이터라 건드리지 않았다**(0.2).

### 3.7 예배 순서 탭 2차 개선 (2026-08-15, S-8~S-10)

남은 결정과 설계는 [plan-service-tab-2.md](plan-service-tab-2.md). 실행 결과만 적는다.

- **S-8 광고 Enter** — 광고·인용구 입력창이 textarea 라 Enter 가 줄바꿈으로만 동작했다.
  Enter = 추가, Shift+Enter = 줄바꿈으로 바꿔 성경·찬양과 손놀림을 맞췄다.
- **S-9 순서 표시(`variant: 'order'`)** — 대표기도·주기도문·사도신경·설교 제목·축도처럼
  '지금 무슨 순서인지'를 띄우는 항목. 저장 구조는 `text` 그대로라 하위 호환이 깨지지 않는다.
  자주 쓰는 이름 10종을 버튼으로 바로 넣는다.
- **S-10 예배 유형 템플릿** — `service_plans.kind`(`template`|`plan`) 한 컬럼만 더했다.
  기본값이 `plan` 이라 기존 순서표는 그대로 '저장된 순서'가 된다.
  유형이 **하나도 없을 때만** 기본 4종(주일/수요/새벽/부흥회)을 넣는다 —
  지운 유형이 되살아나지 않도록 조건은 그것 하나뿐이고 테스트로 고정했다.
  버튼 셋: `템플릿 업데이트`(유형 원본 갱신) · `순서 저장하기`(회차 보관) · `순서 불러오기`.

**작업 중 찾아 고친 버그 1개** (이 작업 전부터 있던 것):

| 버그 | 증상 | 원인 |
|---|---|---|
| 추가 바 종류 전환 시 포커스 유실 | 광고를 누르고 바로 타이핑하면 글자가 안 들어가고 **전역 단축키로 들어감**(Enter 로 송출까지 나갈 수 있음) | 클릭 즉시 `focus()` 를 부르는데 그 시점 ref 는 교체 전 옛 입력창(한 줄 input ↔ 여러 줄 textarea 는 다른 요소). `requestAnimationFrame` 도 답이 아니다 — 탭이 화면에 없으면 콜백이 오지 않는 것을 실측으로 확인 |

**검증**: `npx tsc --noEmit` · `npx vitest run` **468 passed**(기존 462 + 신규 6) ·
`npx vite build` 성공 · 브라우저 실측(사용자 서버 7777 을 건드리지 않으려고
`SERMON_DATA_DIR` 을 임시 폴더로 준 별도 서버 7778 에서 확인 —
유형 시딩·Enter 추가·Shift+Enter 줄바꿈·순서 표시 칩·저장/불러오기 왕복·
저장된 순서를 열면 '템플릿 업데이트' 비활성).

⚠️ **사용자 서버 재시작 필요**: 서버 코드(`server/db/plans.ts`)가 바뀌었으므로
7777 에 떠 있는 서버를 다시 띄워야 유형 기능이 동작한다.

### 3.8 배경 · 예배 전 안내 (2026-08-15, S-11~S-13)

- **S-11 배경 그림·반복 동영상** — `canvas.background` 에 `image`·`video` 모드.
  파일은 `data/backgrounds/`, **폴더 직접 넣기와 앱 업로드 둘 다** 받는다(64MB 한도).
  경로 조작 방어(`basename` + 확장자 허용 목록)를 테스트로 고정했다.
  **배경 실패가 검은 화면이 되지 않는다** — 페이지 배경은 계속 투명이고 `#backdrop`
  요소만 사라지며 컨트롤 패널에 오류로 올라간다. 출력 페이지 의존성 0 원칙 유지(img/video 태그만).
- **S-12 예배 전 안내** — 구분에 `auto { holdMs, loop }`. 그 구분 아래 항목을 8초마다
  자동으로 넘기고 마지막에서 처음으로 돌아간다. **예배 중 덱은 여전히 순환하지 않는다.**
  자동은 ▶ 로만 시작하고, 사람이 다른 항목을 송출하거나 순서표를 올리면 즉시 멈춘다.
- **S-13 OBS 없이 쓰기** — 코드 변경 없음. 전체화면 출력은 지금도 `F11` 로 되고 배경만
  고르면 되므로, 전용 앱 창은 Phase 6 과 함께 판단하기로 했다(사용자 결정 유보).

**검증**: `npx tsc --noEmit` · `npx vitest run` **487 passed** · `npx vite build` ·
브라우저 실측(7778 격리 서버) — 배경이 **저장 전에** 미리보기에 반영, 불투명도 즉시 반영,
없는 파일이면 배경만 빠지고 투명 유지, 자동 진행 5장 순환 확인,
다른 항목 송출 시 자동 정지 확인.

### 3.9 순서 표시 배치 — '순서 이름 | 담당자 + 밑줄' (2026-08-15)

사용자가 실제 예배 화면 캡처를 보내 같은 형태를 요청했다.

- `SlidePayload` 에 **`kind: 'order'`**(title · presenter) 추가. `text` 로 두지 않은 이유는
  두 값의 **자리가 다르기 때문**이다 — 줄 배열로는 "이건 왼쪽, 저건 오른쪽"을 출력 페이지에
  알릴 방법이 없다.
- 나누는 규칙은 `lib/plan-deck.ts` 의 `splitOrderText` 순수 함수(테스트 5건).
  첫 줄=순서 이름, 나머지를 합친 것=담당자, 빈 줄은 버린다.
- **담당자가 없으면 밑줄도 없다.** 빈 밑줄만 떠 있으면 이름을 못 넣은 것처럼 보인다.
- `.order-row` 만 **안전 영역 전체 폭**을 쓴다. 템플릿 블록 폭이 `auto` 면 글자 길이만큼만
  넓어져 두 값이 딱 붙는다.
- 항목별로 `좌우`(기본) / `쌓기` 선택. **기본값은 저장하지 않는다** — 순서표에 기본값이
  박히면 나중에 기본을 바꿀 수 없다.

**검증**: 브라우저 1920×1080 실측 — 순서 이름 좌측 끝(x=60), 담당자 우측 끝(x=1860),
밑줄이 이름 왼쪽(x=1176)까지 이어짐. 담당자 없는 항목은 오른쪽 칸이 만들어지지 않음.
`npx vitest run` **493 passed**.

### 3.10 ⚠️ 사고 기록: 새 슬라이드 종류가 OBS 화면에 조용히 안 나옴 (2026-08-15)

**증상**: 순서 표시 배치를 `쌓기`(kind `text`)로 하면 나오는데 `좌우`(kind `order`)로 하면
아무 일도 일어나지 않았다.

**원인**: OBS 안의 출력 페이지가 **옛 판**이었다. 서버를 재시작해도 그 페이지는
WebSocket 만 자동 재연결하고 **JS 는 메모리에 있던 것 그대로**다. 권장 설정상
'장면 활성화 시 새로고침'도 꺼져 있어 저절로 갱신되지 않는다.
옛 판은 `RENDERERS['order']` 가 없어 렌더에 실패하는데, 출력 페이지는 **모르는 내용을
받으면 화면을 비우지 않고 이전 내용을 유지**하도록 되어 있다(예배 중 검은 화면 방지).
그래서 **검은 화면이 아니라 "안 바뀜"** 으로 나타나 원인을 찾기 어려웠다.

**조치**: 사람이 기억해야 하는 절차로 두지 않고 **앱이 알리게** 했다.
출력 페이지가 접속할 때 자기가 로드된 시각을 보내고, 서버가 `public/output/` 파일
수정 시각과 비교해 컨트롤 패널에 노란 배너를 띄운다(`server/output-build.ts`).
'모르면 경고하지 않는다'가 기본이다 — 예배 직전 헛경고가 더 나쁘다.

**교훈**: 출력 페이지의 **계약(슬라이드 종류)을 늘리는 변경은 배포 문제와 함께 온다.**
서버·컨트롤 패널은 재시작·새로고침으로 갱신되지만, OBS 안의 페이지는 **사람이 눌러야만**
갱신된다. 새 `kind` 를 더할 때는 옛 판이 그것을 받으면 어떻게 되는지 함께 생각한다.

### 3.11 ⚠️ 사고 기록: '공백' 항목이 아무 일도 하지 않음 (2026-08-15, 수정됨)

**증상**: 예배 순서의 '공백'을 송출해도 **이전 화면이 그대로** 떠 있었다.

**원인**: 공백 슬라이드가 내용을 지우지 않고 `body.blanked`(투명도 0)만 켰는데,
바로 뒤 `applyState` 가 `setBlank(live.blank)` 를 부르며 그 클래스를 **다시 껐다**.
`live.blank` 는 `B`(블랙)를 눌렀을 때만 참이므로 공백 항목은 **항상** 무효였다.

**진짜 원인은 개념이 겹친 것**이다.
- **블랙(B)** = 내용을 **남긴 채** 숨기기 (즉시 복구가 목적)
- **공백** = 내용이 **없는** 슬라이드

둘이 같은 장치(`blanked` 클래스)를 쓰면서, 하나가 다른 하나를 지웠다.

**조치**: 공백은 `clearSlide()` 로 내용을 실제로 지우고, `blanked` 는 `B` 전용으로 뒀다.
'내용 없음'(비우기) 경로도 같은 함수를 쓰게 해 두 곳이 어긋나지 않게 했다.

**교훈**: 화면을 비우는 방법이 둘 이상이면 **어느 쪽이 이기는지** 정해 둬야 한다.
출력 페이지는 상태를 눈으로 확인하기 어려워, 이런 겹침은 조용히 지나간다.

### 3.12 순서 표시 글자 리듬 · 명조 · 실제 사용 폰트 표시 (2026-08-15)

- **글자 리듬** — 순서 표시 제목을 글자마다 크기·높이를 달리해 그린다.
  규칙은 **세 글자 주기(큰·작은·작은)** 이고 띄어쓰기에서 다시 시작한다.
  ⚠️ 처음에는 '어절 안에서 양끝 크게'로 만들었다가, **2자 어절은 두 글자가 모두 양끝이라
  크기가 같아져 리듬이 사라지는** 것을 실사용에서 발견해 고정 주기로 바꿨다
  ('예배 부름'·'축도'·'광고' 등 흔한 순서 이름이 전부 해당했다).
  크기와 **높낮이는 따로** 조절한다(`titleRhythm` / `titleRhythmY`).
  **무작위를 쓰지 않는다** — 무작위면 다시 그릴 때마다 배치가 달라져 같은 순서가
  예배마다 다른 모양으로 나간다. 강도는 템플릿에서 0~0.4(CSS 변수라 저장 전에도 반영).
- **명조 프리셋**(`kind: 'order'`) 추가. 체인은 Noto Serif KR → 바른바탕 Pro → 나눔명조 →
  애플명조 → Batang → serif.
- ⚠️ **폰트 이름이 안 잡혀도 화면은 멀쩡해 보인다.** `BareunBatangOTFPro-3.otf` 라는 파일
  이름을 보고 `"BareunBatangOTFPro 3"` 으로 적었더니 어디에도 안 걸려 조용히 고딕으로
  대체됐다. 실제로 잡히는 가족 이름은 번호 없는 `BareunBatangOTFPro` 였다.
  **축소된 스크린샷으로는 구분이 안 돼**, 1:1 크기로 다시 찍고서야 알았다.
- 그래서 컨트롤 패널이 **실제 쓰이는 폰트**를 재서 보여 준다(작업 보드 4.2 항목 완료).
  폭 비교로 판정하고, 총칭까지 떨어지면 빨간 글씨로 경고한다.
  `lib/font-chain.ts` 는 순수 함수 + 테스트, 내장 프리셋 전체가 총칭 앞에 실제 폰트를
  두는지도 테스트로 고정했다.

**검증**: `npx vitest run` **518 passed** · 브라우저 1:1 실측 —
'신앙고백' 신1.18/앙0.91/고0.91/백1.18, '폐회송' 폐1.18/회0.82/송1.18,
'실제 사용: BareunBatangOTFPro · 없는 폰트: Noto Serif KR' 표시 확인.

### 3.5 git 도입 (2026-08-14)

- `github.com/ccumgol/sermon-presentation` (PRIVATE) 연결, 초기 커밋 `70ebd54`.
- 95개 파일 1.1MB 만 커밋 — `data/`(144MB)·`node_modules/`·`public/app/` 제외.

---

## 4. 작업 보드 (Work Board)

상태: 🔴 미착수 · 🟡 진행중 · 🟢 완료 · ⚪ 보류(의도적)

### 4.1 데이터 — 찬양 자료

| ID | 항목 | 상태 | 담당 | 비고 |
|---|---|---|---|---|
| D-1 | 곡집 구조(다대다) + 통합 검색 | 🟢 완료 | Agent C | 2026-08-13 |
| D-2 | 후렴을 각 절 뒤로 병합 | 🟢 완료 | Agent C | 586곡, 원형 복원이라 무손실 |
| D-3 | 절 간 운율 정렬 + 짝수 행 | 🟢 완료 | Agent C | 측정으로 순위 규칙 결정 |
| D-4 | 전체 1,202곡 초안 + 검토 화면 | 🟢 완료 | Agent C | 사용자 검토 진행 중 — 승인 9곡 / 미확인 1,193곡 (2026-08-14 기준) |
| **D-5** | **찬미예수 2000 가져오기(2,062곡)** | 🔴 미착수 | — | 폴더 규약 확정됨. **줄바꿈이 이미 있으므로 `imported` 로 넣고 자동 정렬에서 제외** |
| D-6 | 악보 이미지 연결(번호로 100% 매칭) | 🔴 미착수 | — | `0001.bmp`·`(1889).bmp` 두 형식 |
| D-7 | 한/영 병기 — 영어 가사 병합 가져오기 | 🔴 미착수 | — | 곡은 번호로 매칭, **행 짝맞춤은 사람 확인** |

### 4.2 기능

| 우선 | 항목 | 상태 | 담당 |
|---|---|---|---|
| **최우선** | **'예배 순서' 탭 재설계** — 설계 제안서: [plan-service-tab.md](plan-service-tab.md) | 🟢 완료 | Agent C (2026-08-15) · S-1~S-7 전부 |
| **최우선** | **예배 순서 2차 개선** — [plan-service-tab-2.md](plan-service-tab-2.md) · S-8 광고 Enter · S-9 순서 표시 · S-10 유형 템플릿 | 🟢 완료 | Agent J (2026-08-15) |
| **최우선** | **S-11 배경 그림·반복 동영상** (템플릿별 · 폴더+업로드) | 🟢 완료 | Agent J (2026-08-15) |
| 높음 | **S-12 예배 전 안내** (구분 단위 자동 진행 8초·순환) | 🟢 완료 | Agent J (2026-08-15) |
| 높음 | **S-13 OBS 없이 쓰기** — 전체화면은 이미 가능(README). 전용 앱 창은 Phase 6 과 함께 | ⚪ 보류 | 사용자 결정 유보 (2026-08-15) |
| 높음 | **Phase 6 — Electron 패키징·배포** | 🔴 미착수 | — |
| 중간 | 언어별 실제 사용 폰트 경고 표시 | 🟢 완료 | Agent J (2026-08-15) · 3.12 |
| 낮음 | 예배 순서 프리셋 저장 | 🔴 미착수 | — |

### 4.3 품질·기술부채

| 항목 | 상태 | 비고 |
|---|---|---|
| 테스트 453개 | 🟢 유지 | 단위 + 통합. `fileParallelism: false`(통합 테스트가 SQLite 파일 공유) |
| lint 도구 | 🔴 없음 | ESLint/Prettier 미도입 — 도입 여부 사용자 확인 필요 |
| CI | 🔴 없음 | GitHub Actions 미설정 |
| `server/db/songs.ts` 1,067줄 | ⚪ 보류 | 가장 큰 파일. 분리하면 충돌 위험 커서 기능 안정화 후 판단 |

---

## 5. 파일별 점유 현황 (충돌 방지)

최근 수정된 파일과 **주의점**입니다. 같은 파일을 만질 때는 6장에 메모를 남기세요.

| 파일 | 줄 수 | 주의 |
|---|---:|---|
| `server/db/songs.ts` | 1,067 | **가장 충돌 위험 높음.** 곡·곡집·검색·사용기록·검토가 모두 지나감. 스키마 변경은 `addUsageColumns` 처럼 **기존 DB 에서도 도는 마이그레이션**으로 |
| `shared/types.ts` | 531 | **서버·클라이언트 단일 계약.** 여기를 고치면 양쪽이 함께 바뀜 |
| `src/control/panels/SongPanel.tsx` | 536 | 찬양 탭 2단 레이아웃·즐겨찾기·가사 편집 |
| `src/control/panels/ReviewPanel.tsx` | 453 | 줄나눔 검토 화면 |
| `src/control/panels/PlanPanel.tsx` | ~640 | **예배 순서 — 가장 많이 쓰는 화면.** 선택/송출 분리가 핵심이라 여기를 고칠 때는 "눌러도 안 나가는지" 를 반드시 재확인 |
| `src/control/hooks/useMeasure.ts` | — | 반환 객체를 `useMemo` 로 고정해 뒀다. 풀면 이걸 의존성으로 쓰는 effect 가 무한 재실행된다 |
| `src/control/styles.css` | — | 여러 패널이 공유. `.main:has(...)` 로 탭별 스크롤을 제어하므로 전역 규칙 수정 주의 |
| `lib/meter-align.ts` | — | 순수 로직. 바꾸면 **1,202곡의 줄나눔이 달라짐** — 반드시 `npm run measure:meter` 로 영향 측정 |
| `public/output/output.js` | — | **의존성 0 유지**(React·CDN·외부 폰트 금지). OBS 내장 브라우저 대비 |
| `PLAN.md` / `README.md` | — | 동작을 바꾸면 **문서도 같이** 갱신(사실 불일치 주의) |

---

## 6. 인계 메모 / 다음 Agent 에게

### 바로 이어서 하기 좋은 작업 (권장 순서)

0. **'예배 순서' 탭 재설계 (최우선)** — [plan-service-tab.md](plan-service-tab.md) 참고.
   실제로 가장 많이 쓰는 화면인데 지금은 광고 추가도, 골라서 송출도 안 됩니다.
   **S-1(탭 순서) · S-2(타입 추가) 를 먼저** 하면 나머지는 화면 작업이라 충돌 없이 이어집니다.
1. **D-5 찬미예수 2000 가져오기** — 폴더 규약이 확정돼 있어 바로 시작 가능.
   `npm run praise:check` 로 자료를 먼저 점검하고, `lib/songbook-import.ts` 파서를 재사용.
   ⚠️ **원본에 줄바꿈이 이미 있으므로 `linesSource: 'imported'` 로 넣고 자동 정렬 대상에서 뺍니다.**
2. **Phase 6 Electron 패키징** — 다른 봉사자 PC 배포용. 경로는 이미 `server/paths.ts`
   한 곳을 거치므로 `SERMON_DATA_DIR` 만 지정하면 됩니다.
3. **D-7 한/영 병기** — 스키마는 이미 지원(`song_lines(section_id, line_index, lang)`).
   곡집 가져오기에 "언어 병합" 모드를 붙이는 일이 남았습니다.

### 작업 시 지켜야 할 이 프로젝트의 관례

- **추측 말고 실측.** 이 프로젝트의 거의 모든 판단은 측정에서 나왔습니다
  (ESV 판본 비교, 운율 순위 규칙, 폰트 대체 비율, 스크롤바 폭).
  숫자 없이 "좋아 보인다"로 정하지 마세요.
- **화면을 스스로 비우지 않습니다.** 서버가 죽어도 출력 페이지는 마지막 내용을 유지합니다.
  예배 중 검은 화면이 최악의 결과입니다.
- **자동 결과는 제안입니다.** 사람이 확인(승인)해야 확정됩니다.
- **본문을 임의로 고치지 않습니다.** 성경은 `text_raw` 에 원문을 남기고, 가사는
  어절 경계에서만 끊어 **글자가 바뀌지 않음**을 테스트로 고정합니다.
- **출력 페이지는 의존성 0.** OBS 내장 브라우저(CEF)에서 초기화가 실패할 지점을 없앱니다.
- **문서 동시 갱신**: 동작을 바꾸면 README·PLAN 의 해당 문구도 고칩니다.
- **검증 3종은 항상 통과**:
  ```bash
  npx tsc --noEmit && npx vitest run && npx vite build
  ```
- **서버 재기동 필수**: TypeScript 를 네이티브로 실행하므로 **코드를 고치면 서버를
  재시작**해야 반영됩니다. 옛 프로세스가 7777 을 잡고 있으면 구버전이 응답해
  "수정이 반영되지 않은 것처럼" 보입니다(실제로 여러 번 겪음).
  ```bash
  lsof -ti:7777 | xargs kill -9
  ```
- **출력 페이지를 고쳤으면 OBS 소스도 새로고침**해야 합니다(2026-08-15 실제로 겪음).
  서버를 재시작해도 OBS 안의 페이지는 **WebSocket 만 다시 붙고 JS 는 옛 판 그대로**입니다.
  게다가 권장 설정상 '장면 활성화 시 새로고침'이 꺼져 있어 저절로 갱신되지 않습니다.
  → 브라우저 소스 더블클릭 → `현재 페이지 새로고침`.
  옛 판이 떠 있으면 **새로 만든 슬라이드 종류가 화면에 나오지 않습니다** — 출력 페이지는
  모르는 내용을 받으면 이전 화면을 유지하므로(의도된 안전 동작) 검은 화면이 아니라
  "안 바뀜"으로 나타나 원인을 찾기 어렵습니다.
  진단: OBS 렌더러 프로세스 시작 시각과 `public/output/output.js` 수정 시각을 비교하면 됩니다.
  ```bash
  for p in $(lsof -ti:7777); do ps -o pid,lstart,comm -p $p | tail -1; done
  ```
- **브라우저 확인은 직접**: 사용자에게 "확인해 보세요"라고 하지 말고 Agent 가 직접
  띄워서 재고 스크린샷으로 보여 줍니다.

### 결정된 사항 (2026-08-14)

| 질문 | 결정 | 반영 위치 |
|---|---|---|
| 데이터 공유 방법 | **같은 PC 에서만 작업** — git·별도 저장소로 공유하지 않음 | 0.3 |
| 브랜치 전략 | **`main` 직통 + 작게 자주 커밋** | 0장 4번 |
| 문서 구성 | **README·PLAN·KNOWN-DATA-ISSUES 3종 유지** | 2장 |
| Agent 명명 | **계정 이니셜** (`Agent C` 등) | 1장 |
| 협업의 취지 | 같은 Claude Code 의 **다른 계정으로 이어달리기** (사용량 한도) | 1장 |

### 아직 열린 질문 (사용자 결정 필요)

> 답이 정해지면 이 목록에서 지우고 해당 장에 반영해 주세요.

- **lint 도구**: ESLint·Prettier 를 도입할지. 지금은 없고 `tsc --noEmit` 만 씀
- **CI**: GitHub Actions 로 푸시마다 테스트를 돌릴지

---

## 7. 변경 이력 (이 문서)

| 날짜 | 갱신자 | 내용 |
|---|---|---|
| 2026-08-15 | Claude (Agent J) | **순서 표시 글자 리듬 + 명조 + 실제 사용 폰트 표시**(3.12) — 폰트 이름이 안 잡혀 조용히 대체되던 함정을 화면에서 잡아 준다. 518 passed |
| 2026-08-15 | Claude (Agent J) | **'공백' 항목이 무효였던 버그 수정**(3.11) — 공백과 블랙(B)이 같은 장치를 써서 서로 지웠다. 공백은 내용을 실제로 지우고 blanked 는 B 전용으로 분리 |
| 2026-08-15 | Claude (Agent J) | **옛 판 출력 페이지 자동 감지**(3.10) — OBS 안의 페이지가 앱보다 옛 판이면 컨트롤 패널이 '소스를 새로고침하세요' 배너를 띄운다. 새 슬라이드 종류가 조용히 안 나오던 문제의 재발 방지. 498 passed |
| 2026-08-15 | Claude (Agent J) | **순서 표시 배치**(3.9) — 캡처 요청대로 '왼쪽 순서 이름 · 오른쪽 담당자 + 밑줄'. `SlidePayload` 에 `kind: 'order'` 추가(자리가 다른 두 값은 줄 배열로 표현할 수 없다). 493 passed |
| 2026-08-15 | Claude (Agent J) | **S-11 배경 · S-12 예배 전 안내 완료**(3.8). 사용자 결정 ①~⑤ 반영(배경은 템플릿별, 폴더+업로드 둘 다, 백업 제외, 자동 진행 구간, 8초·순환). S-13(OBS 비의존)은 전체화면이 이미 가능해 문서만 남기고 전용 앱 창은 Phase 6 으로 미룸. 487 passed |
| 2026-08-15 | Claude (Agent J) | **예배 순서 2차 개선 S-8~S-10 완료**(3.7) — 광고 Enter, 순서 표시 항목, 예배 유형 템플릿 + 저장/불러오기. 작업 중 기존 버그 1건(추가 바 포커스 유실) 발견·수정. 468 passed. 남은 3건(배경·예배 전 안내·OBS 비의존)은 [plan-service-tab-2.md](plan-service-tab-2.md) 에 갈림길과 권장안을 정리하고 **사용자 결정 대기** |
| 2026-08-15 | Claude (Agent J) | **Agent J 합류 등록**(1장). 인수 시점 상태를 실측으로 확인 — 작업 트리 깨끗(`624ef1b`, 미푸시 없음), `tsc --noEmit` 통과, `vitest run` **462 passed**. 기준 커밋 표기를 `565dff5` → `624ef1b` 로 갱신(실제 HEAD 와 어긋나 있었음) |
| 2026-08-15 | Claude (Agent C) | **예배 순서 탭 재설계 완료**(3.6) — S-1~S-7. 선택/송출 분리, 인라인 추가 바, 직전으로. 작업 중 기존 버그 3건(useMeasure 재생성·←→ 이중처리·실패 시 잔류) 발견·수정. 462 passed |
| 2026-08-14 | Claude (Agent C) | **'예배 순서' 탭 재설계 제안서** 작성([plan-service-tab.md](plan-service-tab.md)) — 보드 최우선 항목으로 등록. 협업 취지(같은 Claude Code 의 다른 계정 이어달리기)와 Agent 명명(계정 이니셜) 명시 |
| 2026-08-14 | Claude (Agent C) | **협업 전제 확정**(0.3 신설) — 같은 PC·같은 data/ 공유, main 직통, 문서 3종. 그로 인한 동시 편집·포트 충돌·미커밋 혼재 대비를 명시 |
| 2026-08-14 | Claude (Agent C) | **다른 프로젝트(translateviewer)의 협업 규칙을 이 프로젝트로 이식.** 0~1장 협업 방식은 유지, 2장 이하는 이 프로젝트 내용으로 재작성. 파이썬/FastAPI 전제의 명령·경로·작업 보드를 Node/TypeScript 로 교체. **0.2(사용자 데이터 취급) 신설** — 실제 사고(3.2)에서 나온 규칙 |

---

## 8. 진행 중 작업 로그 (Live Work Log) ★중단 대비

> **규칙 A**: 작업을 시작하면 **코드를 만지기 전에** 여기에 3줄(시작·계획·다음 단계)을 씁니다.
> **규칙 B**: 단계가 끝나면 `다음 단계:` 줄을 갱신합니다. 마지막 갱신 지점이 곧 중단 지점입니다.
> 완료되면 이 항목을 지우고 4장 보드를 `🟢 완료` 로 바꾸고 3장에 결과를 요약합니다.

### 현재 진행 중
```
- [시작 2026-08-15 01:40 / Agent J] 예배 순서 탭 2차 개선 (사용자 요청 7건)
  완료: S-8 광고 Enter(94d8d2b) · S-9 순서 표시(94d8d2b) · S-10 유형 템플릿(a5cfcb6)
        · 추가 바 포커스 버그(57d2d44)
  주의: 사용자 서버가 7777 에 떠 있다(PID 58548). 죽이지 않았다 — 검증은 별도 데이터 폴더의
        7778 서버로 했다. **사용자가 7777 을 재시작해야 유형 기능이 동작한다.**
        착수 전 data/app.sqlite 백업 완료(data/backups/app-before-agentj-*.sqlite).
  완료(2차): S-11 배경(43ca860) · S-12 예배 전 안내(3602e75) · S-13 문서만
  다음 단계: 요청 7건 모두 처리됨. 남은 것은 Phase 6(Electron)과 함께 볼 전용 출력 창뿐.
```

### 작성 예시 (복사해서 쓰세요)
```
- [시작 2026-08-15 14:30 / Agent B] D-5 찬미예수 2000 가져오기
  계획: scripts/import-songbook-folder.ts 신규. 폴더=곡집, 「번호 제목.txt」=곡.
        원본 줄바꿈을 보존하므로 linesSource='imported'
  다음 단계: praise:check 로 자료 점검 → 파서 단위테스트 → 미리보기 → 사용자 확인 → --apply
```

### 중단·인계 기록 (Interrupted / Handed over)
> 중단이 확인되면 여기에 남깁니다(규칙 C·E). 이어받은 Agent 도 여기에 이어서 적습니다.

```
(없음)
```
