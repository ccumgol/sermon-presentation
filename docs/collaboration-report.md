# 협력 보고서 (Collaboration Report)

> **언제 읽는가**: 작업을 시작할 때 **매번**. 지금 누가 무엇을 하는 중인지, 어디에
> 착수 기록을 쓸지가 여기 있습니다.
>
> **필수 규칙은 [CLAUDE.md](../CLAUDE.md) 입니다** (자동으로 읽힙니다 · 다른 도구는 `AGENTS.md`).
> 작업 이력은 [work-log.md](work-log.md) · 사용법은 [USER-GUIDE.md](USER-GUIDE.md) ·
> 문제 해결은 [TROUBLESHOOTING.md](TROUBLESHOOTING.md) · 변경 요약은 [CHANGELOG.md](CHANGELOG.md) ·
> 설계 근거는 [PLAN.md](../PLAN.md) · 데이터 문제는 [KNOWN-DATA-ISSUES.md](KNOWN-DATA-ISSUES.md).

**최근 갱신**: 2026-08-22 · 갱신자: Claude (Agent J, Opus 5) · 기준 커밋: `1d8acff`

> 협업 규칙 자체는 다른 프로젝트(translateviewer)에서 옮겨온 것입니다. 2026-08-22 에
> **규칙은 `CLAUDE.md` 로, 이력은 `work-log.md` 로** 나눴습니다 — 이 문서는 상태만 담습니다.

---
## 0. 이 문서 사용 규칙

**필수 규칙은 [CLAUDE.md](../CLAUDE.md) 에 있습니다** — 데이터 취급, 착수 기록, 검증,
커밋, 절대 하지 않는 것. Claude Code 는 그 파일을 **자동으로 읽습니다.** 다른 도구는
저장소 뿌리의 `AGENTS.md`(같은 내용)를 읽습니다. 이 문서를 열기 전에 이미 읽었을 것입니다.

**이 문서는 "지금 누가 무엇을 하는 중인가" 를 담습니다.** 규칙이 아니라 상태입니다.

| 장 | 무엇 | 언제 |
|---|---|---|
| 4장 작업 보드 | 항목별 상태(🔴🟡🟢⚪)·담당·날짜 | **착수 전** 확인하고 `🟡` 로 바꾼다 |
| 5장 파일 점유 | 다른 Agent 가 만지는 파일 | 겹치면 6장에 남기고 조율 |
| 6장 인계 메모 | 다음 Agent 에게 넘길 것 | 중단·인계할 때 |
| **8장 진행 중 로그** | 착수 3줄 · 중단 지점 | **코드를 만지기 전에 쓴다** (필수) |

작업이 끝나면 4장을 `🟢 완료` 로 바꾸고 **[work-log.md](work-log.md) 에 결과를 남깁니다**
— 무엇을·왜·**어떻게 검증했는지**. 주장만 쓰지 않습니다.

---

## 1. 참여자 및 역할 기록

### 협업의 취지 — 사용량 한도 이어달리기

이 프로젝트의 협업은 **다른 회사의 AI 가 아니라, 같은 Claude Code 의 다른 계정**입니다.
시간 한도·주간 한도에 걸리면 며칠 동안 작업이 멈추므로, **계정을 바꿔 이어서** 작업합니다.

그래서 이 문서의 목적은 "여러 AI 의 조율"이 아니라 **이어달리기 배턴**입니다.
0.1 의 선행 기록(규칙 A)이 특히 중요합니다 — 한도는 예고 없이 걸립니다.

**Agent 이름은 계정 이니셜을 씁니다.** (`Agent C` = 계정 이니셜 C)

| 참여자 | 담당·기여 | 최근 작업일 |
|---|---|---|
| **사용자 (소유자)** | 요구사항 정의·우선순위 결정, 실제 예배 운영 검증, 가사 검토·승인, 방향 승인 | 계속 |
| **Agent C / Claude** (Opus 5) | Phase 0~5 전체 구현, 곡집 구조, 가사 줄나눔 재정렬, 검토 화면, git 도입, 협업 문서 이식, 예배 순서 탭 재설계 | 2026-08-15 |
| **Agent J / Claude** (Opus 5) | 예배 순서 2~3차 개선(유형 템플릿·순서 표시·한 열 재설계·항목별 템플릿·예배 기본 설정), 배경 그림·동영상, 예배 전 안내, 글자 리듬·명조·글자별 조정, 역본 선택, 찬송가 절 번호, 옛 판 출력 감지, 버그 8건, 운영 문서 3종 | 2026-08-16 |

> 이 프로젝트는 **예배 중 사고가 최악의 결과**이므로, 편의 기능이라도 송출을 막지 않는지
> 먼저 확인하고 넣습니다.

---

## 2. 프로젝트 현재 상태 요약

| 항목 | 상태 |
|---|---|
| 무엇인가 | OBS Studio **브라우저 소스**로 성경 본문·찬양 가사를 송출하는 로컬 앱 |
| 기술 | Node.js 26(TypeScript 네이티브 실행, 빌드 없음) · Fastify 5 · `node:sqlite` · React 19 + Vite |
| 기능 완성도 | **실사용 가능**. 성경 12역본 · 찬양 **4,531곡**(2026-09-07 실측) · 템플릿 프리셋 **8종** · 예배 순서(유형·저장·자동 진행·배경) · 줄나눔 검토 · 교독문 두 벌 · 프로젝터·강사 모니터 |
| 테스트 | **1,744개 통과** · 103파일 (단위 + 통합 + **UI**, `npx vitest run` 약 14초 · 2026-09-07 실측) |
| 문서 | README(설치·구조) · PLAN(설계와 판단 근거) · KNOWN-DATA-ISSUES(원본 데이터 문제) + **운영 3종**(2026-08-16 사용자 요청으로 신설) — [USER-GUIDE](USER-GUIDE.md)(탭별 사용법) · [TROUBLESHOOTING](TROUBLESHOOTING.md)(증상별 해결) · [CHANGELOG](CHANGELOG.md)(변경 이력) · [SECURITY-AUDIT](SECURITY-AUDIT.md)(보안 감사, 2026-08-16). analysis·handover·setup-guide 처럼 **겹치는 문서는 여전히 만들지 않습니다** |
| 저장소 | `github.com/ccumgol/sermon-presentation` (**PRIVATE**) · `main` 직통 |
| 실행 환경 | **모든 Agent 가 사용자의 같은 맥·같은 `data/` 를 공유** (0.3 참고) |
| 배포 | 맥 `.dmg` · 윈도우 NSIS 포장 완료(Phase 6). **점검 14건을 모두 처리했다** — [REVIEW-release-2026-09-07](REVIEW-release-2026-09-07.md). `release/` 의 DMG(2026-09-07 · arm64 124.4MB)에 그것이 다 들어 있다 |

### 이 프로젝트를 처음 만지는 Agent 가 먼저 알아야 할 것

```bash
npm install
npm run bible:build     # 원본 성경 DB → data/bible.sqlite (읽기 전용으로만 원본을 연다)
npm run bible:verify    # 47개 항목 검증
npm start               # http://localhost:7777
```

- **`data/` 는 git 에 없습니다.** 성경 DB 는 위 명령으로 다시 빌드할 수 있지만,
  **찬양 가사(`data/songs.sqlite`)는 사용자가 직접 손본 개인 자료라 재현할 수 없습니다.**
  같은 맥에서 작업하므로 이미 있는 파일을 그대로 씁니다 — **지우거나 덮어쓰지 마세요**(0.2·0.3).
- **서버는 TypeScript 를 네이티브로 실행**합니다. 코드를 고치면 **서버를 재시작**해야
  반영됩니다(브라우저 새로고침만으로는 안 됨 — 실제로 여러 번 헷갈렸음).
- 컨트롤 패널(React)은 `npx vite build` 로 `public/app/` 에 빌드합니다.

---

---

## 3. 완료된 작업 → [work-log.md](work-log.md) 로 옮겼습니다

작업 이력 42항목(1,641줄)은 별도 문서로 분리했습니다 (2026-08-22).
이 문서가 **매번 읽는 문서**인데 77%가 이력이어서, 정작 착수할 때 봐야 할
4장(보드)과 8장(진행 중 로그)이 1,641줄 뒤에 묻혀 있었습니다.

**절 번호는 그대로입니다** — `§3.42` 같은 기존 참조가 계속 통합니다.

---

## 4. 작업 보드 (Work Board)

### 4.5 검토 조치 (REVIEW-agent-c-2026-08-22)

| # | 항목 | 상태 | 담당 |
|---|---|---|---|
| R-1 | Origin 검사에 Host 확인 한 겹 (DNS 리바인딩) | 🟢 완료 | Agent J (2026-08-22) |
| R-2 | `coverage/` 추적 해제 | 🟢 완료 | Agent J (2026-08-22) |
| R-3 | `migrate-template-ids` 스냅샷 | 🟢 완료 | Agent J (2026-08-22) |
| R-4 | `PlanPanel.tsx` 분할 | 🟢 **완료** 3,147 → **444줄** (훅 일곱 + 컴포넌트 여덟, 전부 800줄 아래) | Agent J (2026-08-22) · Agent C (2026-09-07·08) |
| R-5 | 문서 머리말·테스트 수 갱신 | 🟢 완료 | Agent J (2026-08-22) |
| Q-1 | 통일찬송가 영어 원제 483곡 | 🟢 완료 | Agent J (2026-08-22) |
| 권고4 | **태블릿 접속 암호** (감사 8장) | 🟢 완료 | Agent J (2026-08-22) |
| — | **태블릿 연결 QR** (§3.45) | 🟢 완료 | Agent J (2026-08-22) |

**R-4 를 마쳤습니다** (2026-09-07·08, Agent C). 3,147 → **444줄**.
목적은 줄 수가 아니라 **검사**였습니다 — 로직과 화면이 한 컴포넌트에 있으면 화면을
다 띄우지 않고는 부를 수 없어 커버리지가 0 이었습니다.

**훅 일곱** (로직 — "부르면 무엇을 하는가")

| 훅 | 무엇 | 줄 | 입력 | 검사 |
|---|---|---:|---:|---|
| `usePlanFeedback` | 배너 셋 (화면 아홉 중 일곱이 쓴다) | 39 | 0 | (간접) |
| `usePlanBackgrounds` | 배경으로 쓸 그림 목록 | 87 | 3 | (간접) |
| `usePlanDraft` | 편집 중인 것 — **척추** | 118 | 0 | 46개에 포함 |
| `usePlanPreview` | 항목을 슬라이드로 푼다 | 279 | 3 | 37개 · 94% |
| `usePlanStorage` | 읽기·저장·삭제 (**사용자 데이터를 쓰는 길**) | 364 | 3 | 46개 · 98% |
| `usePlanSend` | 화면으로 내보내는 것 전부 | 429 | 8 | 41개 · 94% |
| `usePlanAdd` | 항목 추가 · 검색 | 479 | 5 | 44개 · 97% |

**컴포넌트 여덟** (화면 — "무엇이 보이고 무엇을 누를 수 있는가"), `components/plan/`

| 컴포넌트 | 줄 | 프롭 | 검사 |
|---|---:|---:|---|
| `PlanLoadList` | 33 | 1 | 100% |
| `PlanDirtyLine` | 35 | 3 | 31개에 포함 |
| `PlanNameBar` | 80 | 2 | 31개에 포함 |
| `PlanHead` | 106 | 3 | 92% |
| `PlanActions` | 124 | 6 | 82% |
| `PlanCueList` | 227 | 8 | 28개 · 79% |
| `PlanDefaultsCard` | 264 | 10 | 🔴 없음 |
| `PlanAddBar` | 308 | 4 | 🔴 없음 |
| `PlanItemEditor` | 732 | (전에 분리) | 🔴 없음 |

### 어떻게 정했나 — 이 판단이 R-4 를 끝냈습니다

전에 멈춘 이유는 "프롭 60개짜리 껍데기" 였습니다. **자르기 전에 블록별로 필요한
이름과 그 출처를 셌더니** 답이 나왔습니다: `add-bar` 는 이름 스물여덟 중
**스물다섯이 `usePlanAdd` 하나**에서 옵니다.

> **구조 원칙: 낱개 값이 아니라 훅 객체를 넘긴다.**
> `add-bar` 를 낱개로 넘기면 프롭 28개, `add` 객체째로 넘기면 4개입니다.
> 같은 자리를 자르는데 결과가 다릅니다 — 훅 분리가 프롭 그룹을 이미 정해 놓았습니다.

**가드레일: 블록당 프롭 10개.** 넘으면 자르는 자리가 틀렸다는 신호로 보고,
덜 자르거나 훅을 하나 더 만들었습니다. `usePlanFeedback`·`usePlanBackgrounds` 는
그 판정에서 나왔습니다 — 산발을 걷지 않고 잘랐으면 컴포넌트마다 프롭이 1~4개씩
늘었습니다.

**자를 수 있다고 자르지 않았습니다.** `PlanDefaultsCard`(264줄)는 계획대로 안을
다시 봤고, 6행이 한 폼이라 나누면 `<select>` 한 줄짜리 껍데기 여섯이 됩니다.

### 그 과정에서 나온 것

**실제 버그 하나**: 기본 템플릿을 바꾼 직후 ▶ 를 누르면 이전 판이 나갔습니다
(`sendItem` 의 의존성에 `plan` 이 없었습니다). 격리 서버에서 가린 실험으로 확정하고
고쳤습니다 (`83f2eaf`, TROUBLESHOOTING 2.4.20). 죽은 import 14개도 걷었습니다.

### 남은 것

`PlanAddBar` · `PlanDefaultsCard` · `PlanItemEditor` 에 아직 검사가 없습니다.
그리고 이 방식은 다른 탭에도 그대로 통합니다 — `SongPanel`(1,150줄) ·
`TemplatePanel`(700줄)이 다음 후보입니다.

**영어 원제(`titleAlt`)는 화면에 안 나옵니다** — API 로는 나갑니다. 찬양 목록·검색에
보이게 할지 사용자 결정이 필요합니다 (8장 ③).


상태: 🔴 미착수 · 🟡 진행중 · 🟢 완료 · ⚪ 보류(의도적)

### 4.6 만들어져 있는데 화면에 길이 없는 것 (AUDIT-2026-09-03, Agent C)

사용자 요청으로 전수 조사했다 — 서버 라우트 67개 · `api.*` 45개 · WS 메시지 · `TextStyle`
14필드 · `CueItem` 8종을 화면 사용처와 대조. 방법과 실측은 §3.52.

**A. 완성돼 있고 동작도 확인했는데 부를 곳이 없다** (격리 서버 7811 실측)

| ID | 기능 | 어디까지 되어 있나 | 상태 |
|---|---|---|---|
| U-1 | **성경 전문 검색** `GET /api/bible/search` | 한국어 LIKE · 영어 FTS · 신/구약 필터 · 잘림 표시. 통합 테스트 2개. 실측 '사랑' 557절 | 🟢 **완료** Agent C (2026-09-03) — 성경 탭 '🔍 낱말로 찾기' |
| U-2 | **최근 부른 곡** `GET /api/songs/recent` | `api.recentSongs()` 래퍼까지 있다. 사용 기록은 실제로 쌓이는 중 | 🟢 **완료** Agent C (2026-09-03) — 찬양 탭 '즐겨찾기 \| 최근' |
| U-3 | **대응곡 연결·해제** `POST/DELETE /api/songs/:id/link` | 화면은 대응곡을 **보여 주기만** 했다 | 🟢 **완료** Agent C (2026-09-03) — 찬양 탭 곡 카드 · 통합 테스트 5개 |
| U-4 | **곡 수록 정보(곡집·번호) 편집** `PUT /api/songs/:id/entries` | `api.setSongEntries()` 래퍼까지 있었다 | 🟢 **완료** Agent C (2026-09-03) — 찬양 탭 곡 카드 '수록' 줄 · 통합 테스트 7개 |
| U-5 | **가사 파싱 미리보기** `POST /api/songs/parse-lyrics` | 실측 확인 | ⚪ **보류(의도적)** — `parseLyrics` 가 `lib/` 에 있어 패널이 직접 부르고, 가사 편집 창이 이미 `draftDeck` 로 슬라이드를 실시간 미리보기한다. 라우트를 쓰면 오히려 느려진다 |
| U-6 | **역본별 장 수** `GET /api/bible/chapters` | 실측 시편 150 | 🔴 UI 없음 |
| U-7 | **성경 DB 빌드 정보** `GET /api/bible/build-info` | 12역본 · 342,317절 · 빌드 시각 | 🟢 **완료** Agent C (2026-09-03) — 설정 탭 '서버 정보' |
| U-8 | **곡 수** `GET /api/songs/count` | 실측 4,528 | ⚪ 백업 요약으로 대신 얻는다 |
| U-9 | **템플릿 CSS 미리보기** `GET /api/templates/:id/css` | 주석에 '편집 UI 미리보기용' | ⚪ 그 UI 가 없다 |

**B. 화면에서 못 바꾸는 값** — 🟢 **완료** Agent C (2026-09-03)

`lineGapPx`(절대 행간) · `letterSpacing`(자간) · `opacity`(글자 투명도) ·
`bgBox`(글자 뒤 상자) · `textTransform`(대문자화). 렌더는 전부터 되어 있었고
프리셋만 쓰고 있었다 — 템플릿 탭 `StyleFields` 가 `TextStyle` 14개 중 9개만
편집시켜서 JSON 내보내기/가져오기로만 바꿀 수 있었다.

- 투명도는 **모든 역할**에, 나머지 넷은 **주·보조에만** 붙였다 (`full` 프롭).
  절 번호·참조·소제목은 `simpleStyleVars` 를 타서 그 변수가 아예 안 나간다 —
  거기 컨트롤을 두면 만져도 화면이 안 바뀐다.
- 넷은 `자간 · 대문자 · 글자 뒤 상자` 접이 안에 뒀다. 위쪽(크기·색·외곽선)은
  준비마다 만지는 값이라 늘 펼쳐져 있어야 한다.
- **`lineGapPx?: number | null` 로 바꿨다.** `undefined` 로 끄면 `JSON.stringify` 가
  키를 지워 서버(`mergeTemplate`)에 안 가고, 옛 값이 남아 **화면에서 끌 수 없다.**
  단위 테스트로 못을 박았다 (`0` 은 '여백 없음' 이라 끈 것으로 보면 안 된다).

**C. 자리만 잡아 둔 것** — 🟢 **정리 완료** Agent C (2026-09-04)

| 무엇 | 판단 |
|---|---|
| `t: 'show'` (단일 슬라이드 송출) + `state.show()` | 🟢 **지웠다.** 아무도 보내지 않았고, 덱을 버리는 동작이라 남겨 두면 LAN 의 아무 클라이언트가 예배 중 진행 위치를 날릴 수 있었다 |
| `t: 'state:patch'` | 🟢 **지웠다** (타입 + `stage.js` 처리기). 서버가 한 번도 보내지 않았다 |
| `lib/song-slides.ts` `buildSongSlides` | 🟢 **지웠다.** `buildSongDeck` 과 `order` 계산이 그대로 겹쳤다 |
| 같은 자리의 어긋난 주석 | 🟢 **바로잡았다.** `verseNumberPrefix` 의 설명이 `isSectionStart` 위에 붙어 있었다 |
| `t: 'measure:report'` | ⚪ **남긴다.** `server/ws.ts` 에 "Phase 3 에서 사용한다" 고 명시돼 있다 |
| `image` 슬라이드의 `crop` | ⚪ **남긴다.** `output.js` 가 실제로 자를 줄 안다 — **악보 출력(D-6)의 남은 절반**이라 지우면 그 작업을 다시 해야 한다 |

**앞선 조사에서 틀린 것**: `describeSongSlide` 는 죽지 않았다 — `buildSongDeck` 이
같은 파일 안에서 쓴다. 조사 스크립트가 **파일 밖 사용만** 봐서 잘못 짚었다.

`state.show()` 를 지우면서 함께 옮긴 것:
- **블랙 유지 규칙**(2026-08-15 실사용 발견)이 `show` 의 주석에만 있었다 →
  `server/state.ts` 의 '조작' 절 머리말로 옮겼다. `loadDeck`·`goto` 두 곳이 그 주석을
  가리키고 있었다.
- 테스트가 `state.show()` 를 **11곳에서** 썼다. '묶음 없는 한 장' 상태를 만들려고
  쓴 것인데, 그 상태는 **비우기 → 복구** 로 실제로 도달한다. 테스트를 그 경로로
  바꿨다 — 이제 사람이 누르는 버튼으로 같은 상태를 검사한다.

**D. 조용히 버려지는 것**

~~서버가 보내는 `{ t: 'error' }` 를 컨트롤 패널도 출력 페이지도 처리하지 않는다.~~
🟢 **완료** Agent C (2026-09-03) — `useLiveState` 가 받아 오류 배너에 올린다.
출력 페이지(`output.js`)는 그대로 둔다: 거기서 오류를 그리면 **예배 중 화면에 글자가
나간다.** 출력 쪽 오류는 이미 `client:error` → `output:error` 로 조작 화면에 올라온다.

**E. 문서에 없는 스크립트** — 🟢 **완료** Agent C (2026-09-04)

`titles:borrow` 하나가 아니었다. README 스크립트 표에 **11개**가 빠져 있었다
(`test:watch` · `lyrics:lang` · `titles:borrow` · `hymn:bilingual` ·
`report:missing-en` · `restore:lyrics` · `report:bilingual` · `report:hyphen` ·
`hymn:realign` · `misc:import` · `song:retitle`). 앞선 조사가 **문서 전체**에서
찾아서 CHANGELOG·work-log 에 이름만 나오면 '문서에 있다' 로 봤다 — 정작 찾아보는
곳은 README 표다.

README 에 **'자료 반입·정리 스크립트'** 표를 새로 만들어 전부 넣었다. 각 스크립트의
머리말을 읽어 설명을 달았고, 리포트는 읽기만 하고 쓰는 것은 `-- --apply` 가 필요하다는
규칙을 표 앞에 적었다. `node -e` 로 package.json ↔ README 를 대조해 0개 남은 것을 확인했다.

---

### 4.7 배포 직전 점검 (REVIEW-release-2026-09-07, Agent C)

전문: [REVIEW-release-2026-09-07.md](REVIEW-release-2026-09-07.md). 전부 **격리 서버
7810** 에서 실측했고 코드·사용자 데이터는 바꾸지 않았다.

**나눠 주기 전에 할 넷 — 🟢 전부 완료** (2026-09-07, Agent C · 사용자 승인 순서대로)

| # | 항목 | 상태 |
|---|---|---|
| S-1 | **내보낸 번들에 접속 암호 해시·세션 서명 열쇠가 실린다** — 열쇠로 쿠키 위조가 통하는 것을 실증 | 🟢 완료 — `SECRET_SETTING_KEYS` 를 `auth.ts` 가 내보내고 내보내기·가져오기가 함께 제외 (`cc678e0`) |
| P-2 | **가져오기가 승인·즐겨찾기·대응곡을 버린다** — 번들에는 있는데 `applyBundle` 이 `linesSource` 를 안 넘겨 전부 `auto` | 🟢 완료 — `writeSections` 가 **구간별** 값을 받고, `isFavorite`·`links` 도 되살린다 (`cc678e0`) |
| S-2 | **HTTP 에 Origin·Host 검사가 없다** (R-1 의 나머지 절반) | 🟢 완료 — `app.ts` 첫 훅에서 WS 와 같은 규칙 + `isAllowedHost`. **OBS·프로젝터·강사 모니터·태블릿이 계속 붙는 것을 브라우저로 확인** (`6c16906`) |
| P-1 | **설치판은 태블릿을 못 열고 암호도 못 정한다** | 🟢 완료 — 설정 탭에서 암호를 정하고 열고 닫는다. `resolveHost` 가 환경 변수 우선으로 합친다 (`8bed633`) |

**S-1·P-2 는 고치기 전 코드로 되돌려 새 검사가 실제로 실패하는 것까지 확인했다**
(각각 4개 중 3개 · 6개 중 3개 실패). 통과만 보고 넘기지 않았다.

**P-1 에서 브라우저로 눌러 보고 찾은 것**: `lanOpen` 하나만 주면 켠 직후 화면이
'닫혀 있습니다 · [열기]' 로 남아 **눌렀는데 아무 일도 없는 것처럼** 보이고 되돌릴
방법도 없었다. `lanWanted`(다시 시작하면 열리나)를 따로 주어 고쳤다 — 검사만
돌렸으면 못 찾았을 것이다.

🟢 그 뒤 🟠 셋(S-3 · P-4 · P-3) · 🟡 셋(S-4 · P-5 · P-6) · 나머지(S-5 · S-6 ·
P-7 · D-7 · 감사 L-1 · L-2)까지 **전부 마쳤다** — 아래 표.

**그다음**

**🟠 셋도 완료** (2026-09-07, 사용자 승인 순서대로)

| # | 항목 | 상태 |
|---|---|---|
| S-3 | `style:set` 이 임의 CSS 를 출력 화면에 넘긴다 (감사 L-1) | 🟢 완료 — `isAllowedStyleKey`(`--` + backdrop·anchor)를 **양쪽**에 뒀다. 프리셋 8종의 키 81개를 하나도 막지 않는 것을 실측 (`ac1088d`) |
| P-4 | 설치판에 감시 루프·최상위 오류 처리기가 없다 | 🟢 완료 — `main.cjs` 가 오류를 잡아 `crash.log` 를 남기고 **DB 를 닫고** `app.relaunch()` 한다. 거듭 죽으면 멈추고 알린다 (`6a44e51`) |
| P-3 | 번들에 교독문·곡집·악보 상태가 없는데 문서는 '자료 가져오기로 넣는다' 고 단언한다 | 🟢 완료 — 번들 판 2. **곡집을 곡보다 먼저** 만든다(안 그러면 '기타' 로 떨어진다). 문서를 두 갈래(번들 / 폴더 복사)로 고쳤다 (`079d654`) |

**🟡 셋도 완료** (2026-09-07)

| # | 항목 | 상태 |
|---|---|---|
| S-4 | `npm audit` 2건 (fast-uri 높음 · fastify 중간) | 🟢 완료 — **0건**. `package.json` 은 그대로(`^` 안의 패치). fastify 를 올린 것이라 격리 서버로 기동·라우팅·WS 를 다시 확인했다 (`36fdae2`) |
| P-5 | 설치판에서 `brew` 탐색이 되는지 — **실측이 필요했던 항목** | 🟢 완료 — **정말 문제였다.** 설치 앱의 PATH 가 `/usr/bin:/bin:/usr/sbin:/sbin` 뿐이라 `which brew` 가 실패했다. `pickToolPath` 로 절대 경로 후보를 보고, 설치도 절대 경로로 돌린다 (`787df99`) |
| P-6 | `react`·`react-dom` 이 배포판에 실려 나간다 | 🟢 완료 — 앱 안 node_modules **20MB → 14MB** · DMG **131.7 → 124.4MB**(arm64). 만든 앱을 띄워 확인했다 (`cf6aadd`) |

**나머지도 완료** (2026-09-07)

| # | 항목 | 상태 |
|---|---|---|
| S-5 | 로그인 시도 기록이 주소별로 쌓였다 | 🟢 완료 — 지난 것을 걷고 상한 1,024. **넣은 뒤에 걷는다**(먼저 걷으면 +1 이 된다 · 검사에서 1,025 로 걸렸다) (`1325a1e`) |
| S-6 | `main.cjs` 창 열기 판정이 URL 접두사 비교 | 🟢 완료 — 출처(origin) 비교. `openExternal` 도 http(s) 로 한정. 함께 `activate` 이중 기동과 `void statSync` 정리 (`1325a1e`) |
| P-7 | 받은 사람이 자기 판이 옛것인지 알 수 없다 | 🟢 완료 — `/api/info`·설정 탭에 판 번호 + **만든 시각**. 번호는 빌드마다 안 바뀐다 (`1325a1e`) |
| D-7 | Node 지원 하한이 세 곳에서 달랐다 | 🟢 완료 — **실측으로 정했다**: Electron 44 가 도는 Node = 24.20.0 · CI = 24 → 하한 **24** (`1325a1e`) |
| 감사 L-1 | `deck:load` 페이로드 미검증 | 🟢 완료 — `lib/deck-guard.ts` 가 크기·모양을 본다(5,000장·4MB). 20,000장 묶음이 거부되고 `live_state` 가 301바이트인 것을 실측 (`06a8967`) |
| 감사 L-2 | 요청 수 제한 없음 | 🟢 완료 — 600회/분·주소별. **이 PC 는 세지 않는다.** 훅 순서는 요청 수 제한 → 접속 암호 (`06a8967`) |

**보안 감사(2026-08-16)의 모든 항목이 닫혔다** — H-1~H-3 · M-1~M-3 · L-1 · L-2 ·
권고 4·5. `M-2`(큰 본문 한도)만 '줄었다' 로 남는데, 한도 자체는 실제 번들이 그만큼
크기 때문에 유지하는 것이 맞다 (그 경로에 암호·요청 수 제한·출처 검사가 차례로 붙었다).

**남은 것은 새 항목이 아니라 옛 부채다** — `src/`(React) 테스트 0 · lint 도구 없음 ·
CI 는 태그·수동 실행만 · `PlanPanel.tsx` 2,549줄 (4.3 · 4.5 참고).

**`release/` 의 DMG 는 2026-09-07 빌드다** — 오늘 고친 것이 모두 들어 있고
애드혹 서명 검증을 통과했다. 사용자가 설치한 것(2026-09-05)은 옛 판이다.

**S-3·P-3 은 고치기 전 상태로 되돌려 새 검사가 실제로 잡는 것까지 확인했다**
(서버 필터를 끄고 브라우저로 · 곡집 반입을 곡 뒤로 옮겨서). **P-4 는 Electron 을
띄워 실제로 죽여 봤다** — `crash.log` 두 줄과 `--sermon-restart=2` 가 증거다.
| ~~D-1~D-3~~ | 🟢 완료 — `electron-builder.yml`(서명·오른쪽 클릭) · `config.ts`('인증이 없어') · `origin-check.ts`('REST 는 상대적으로 안전') · USER-GUIDE 태블릿 절. D-4~D-6 수치는 점검 때 바로잡았다 |

---

### 4.1 데이터 — 찬양 자료

| ID | 항목 | 상태 | 담당 | 비고 |
|---|---|---|---|---|
| D-1 | 곡집 구조(다대다) + 통합 검색 | 🟢 완료 | Agent C | 2026-08-13 |
| D-2 | 후렴을 각 절 뒤로 병합 | 🟢 완료 | Agent C | 586곡, 원형 복원이라 무손실 |
| D-3 | 절 간 운율 정렬 + 짝수 행 | 🟢 완료 | Agent C | 측정으로 순위 규칙 결정 |
| D-4 | 전체 곡 초안 + 검토 화면 | 🟢 완료 | Agent C | 구간 10,305개 중 **승인(`manual`) 50 · 원본 줄나눔(`imported`) 2,254 · 자동 8,001** (2026-09-07 실측 — 2026-08-28 `relabel-lines-source` 로 일괄 반입분이 `manual` → `imported` 로 바로잡힌 뒤의 값) |
| D-5 | 찬미예수 2000 가져오기(2,062곡) | 🟢 **완료** | — | **DB 에 2,062곡 들어 있음**(2026-08-26 실측). 보드가 🔴 로 남아 있었다 |
| D-6 | 악보 이미지 연결(번호로 100% 매칭) | 🟢 **완료** · 남은 판단 ⏸ **보류** | Agent C (2026-09-04) | 찬미예수 2000 **2,062곡 ↔ 2,062장 1:1 확인**. ①~⑦ 전부. 남은 판단(155장 훑기)은 **2026-09-09 사용자 보류** — 나중에 다른 방법으로 하겠다고 했다. 지금 상태: `needs_review` 155장 중 판정된 것 **0장**. 화면은 이미 있다(검토 탭 → 악보 단 경계) |
| D-7 | 한/영 병기 — 영어 가사 병합 가져오기 | 🟢 **닫음** (2026-09-09, **사용자 결정**) | Agent J (2026-08-25) · Agent C (점검 08-27) | 영어 **12,525줄 · 637곡** 확보. 줄 대조는 깨져 있다 — 새찬송가 645곡 중 정상 **36곡**(A 8 · B 111 · C 490). **일괄 재분할을 하지 않는다.** 사용자가 **찬양 탭에서 필요한 곡만 그때그때 손으로** 맞춘다. 검토 탭 승인 절차도 만들지 않는다. → 아래 '사용자가 정한 것' · §3.51 |

### 4.2 기능

| 우선 | 항목 | 상태 | 담당 |
|---|---|---|---|
| **최우선** | **'예배 순서' 탭 재설계** — 설계 제안서: [plan-service-tab.md](plan-service-tab.md) | 🟢 완료 | Agent C (2026-08-15) · S-1~S-7 전부 |
| **최우선** | **예배 순서 2차 개선** — [plan-service-tab-2.md](plan-service-tab-2.md) · S-8 광고 Enter · S-9 순서 표시 · S-10 유형 템플릿 | 🟢 완료 | Agent J (2026-08-15) |
| **최우선** | **S-11 배경 그림·반복 동영상** (템플릿별 · 폴더+업로드) | 🟢 완료 | Agent J (2026-08-15) |
| 높음 | **S-12 예배 전 안내** (구분 단위 자동 진행 8초·순환) | 🟢 완료 | Agent J (2026-08-15) |
| 높음 | **S-13 OBS 없이 쓰기** — 전체화면은 이미 가능(README). 전용 앱 창은 Phase 6 과 함께 | ⚪ 보류 | 사용자 결정 유보 (2026-08-15) |
| 높음 | **Phase 6 — Electron 패키징·배포** | 🟢 **완료** | Agent C (2026-09-05) · 맥 .dmg 실측 · 윈도우는 CI · [PACKAGING](PACKAGING.md) |
| 중간 | 언어별 실제 사용 폰트 경고 표시 | 🟢 완료 | Agent J (2026-08-15) · 3.12 |
| 중간 | **열 너비를 마우스로 조절** | 🟢 완료 | Agent C (2026-09-04) · 사용자 요청 |
| 낮음 | 예배 순서 프리셋 저장 | 🔴 미착수 | — |

### 4.3 품질·기술부채

| 항목 | 상태 | 비고 |
|---|---|---|
| 테스트 **1,744개** (103파일) | 🟢 유지 | 단위 + 통합 + UI. `fileParallelism: false`(통합 테스트가 SQLite 파일 공유) |
| **UI 테스트 (`src/`)** | 🟡 **진행중** (2026-09-08) | 0 → **문장 25.7%**. 예배 순서 탭은 훅 90% 대 + 화면 79~100% (`PlanLoadList` 100 · `PlanHead` 92 · `PlanActions` 82 · `PlanCueList` 79). 남은 것은 아직 검사가 없는 화면들 — `PlanAddBar` · `PlanDefaultsCard` · `PlanItemEditor` · `BiblePanel` · `SongPanel` |
| `server/` 커버리지 | 🟢 **완료** (2026-09-09) | 문장 66.6 → **86.9** · 분기 → **80.4** · 줄 → **88.6**. 새로 생긴 일이 아니었다 — 전에는 `lib/`(95.9%)와 한 덩이로 재서 평균으로 통과했고 **낮은 쪽이 가려져 있었다.** 올린 순서: `ws.ts` 57.3 → 86(예배 중 화면이 도는 통로) · `db/songbooks` 36.7 → 96.7 · `routes/songbooks` 36.5 → 97.6(되돌릴 수 없는 사용자 데이터) · `routes/tablet` 36.4 → **100**(태블릿을 여는 유일한 길인데 QR 을 만드는 줄이 한 번도 안 밟혔다) · `db/readings` 76.8 → 97.1(스키마 이전) · `routes/backgrounds` 75.9 → 94.3 · `lan` 65.6 → 93.8. ⚪ 남긴 것: `index.ts`·`electron-entry.ts`(기동 진입점 — 격리 서버로 확인하는 편이 맞다) · `routes/system.ts` 74.3(실제로 Finder 를 띄우거나 `brew install` 을 돌려야 밟힌다. 판단은 `lib/` 에서 검사된다). → [계획서 4번](PLAN-next-2026-09-08.md) |
| lint 도구 | 🔴 없음 | ESLint/Prettier 미도입 — 도입 여부 **사용자 확인 필요**. 근거가 쌓였다: R-4 중에 죽은 import 를 **14 + 9 + 7개** 걷어냈다. `tsconfig` 에 `noUnusedLocals` 가 없어 조용히 쌓인 것이다 |
| CI | 🔴 없음 | GitHub Actions 미설정 |
| `server/db/songs.ts` **1,167줄** | ⚪ 보류 | 분리하면 충돌 위험 커서 기능 안정화 후 판단 |

---

### 4.4 보안 (SECURITY-AUDIT 기준)

감사: [SECURITY-AUDIT.md](SECURITY-AUDIT.md) (2026-08-16, Agent J).
조치 순서는 Agent C 가 검증하며 한 가지를 바꿨다 — **H-2 자동 백업을 H-3 보다 앞으로.**
Origin 검사는 화면이 바뀌는 피해지만, `replace` 는 **되돌릴 수 없는 데이터 손실**이다.

| 순서 | ID | 항목 | 상태 | 담당 |
|---|---|---|---|---|
| 1 | **H-1** | 기본 바인딩을 `127.0.0.1` 로, 태블릿은 환경변수로 열기 | 🟢 완료 | Agent C (2026-08-16) |
| 2 | **H-2** | `replace` 가져오기 직전 서버가 자동 백업 | 🟢 완료 | Agent C (2026-08-16) |
| 3 | **H-3** | WebSocket Origin 검사 (Origin 이 있을 때만) | 🟢 완료 | Agent C (2026-08-16) |
| 4 | **S-1** | 배경 삭제 API + 총량 상한 | ⛔ **기능 자체를 덜어내 소멸** | Agent C (2026-08-18) |
| 5 | 권고 5 | 접속 암호 | ⚪ **보류 (조건부)** | 랜을 열 때 |
| 6 | M-2·L-2 | 요청 수 제한 | ⚪ **보류 (조건부)** | 랜을 열 때 |

**5·6번을 왜 보류했나** (2026-08-16, 사용자 결정)

둘 다 **랜을 열 때만** 의미가 있다. 지금은 `127.0.0.1` 고정(H-1)이라 같은 PC 에서만 닿는다.
"태블릿으로 조작할 계획이 있는가"를 확인했고, **당장은 없다**는 답을 받았다.

태블릿의 이점을 따져 보니 — 설교자가 강대상에서 직접 절을 넘기는 것 하나만 태블릿이어야
가능하고, 나머지(실제 투사면을 보며 조작)는 두 번째 모니터로 대체된다. 반면 비용은
**키보드 단축키 상실**(`PlanPanel`·`ReviewPanel` 의 `↓↑`/`j k`/`Enter`/`Tab` — 예배 중
안 보고 누르던 것을 화면을 봐야 누르게 된다), **예배 중 와이파이 끊김**, 교회 공용망 노출이다.

**되살리는 조건** — 셋 중 하나라도 생기면 5·6번을 먼저 하고 랜을 연다:
1. 설교자가 본인이 넘기고 싶어 하신다
2. 조작자가 두 명 이상이 된다
3. 조작석이 OBS PC 에서 물리적으로 떨어진다

그때 필요한 것은 **접속 암호 + 요청 수 제한 + 터치용 큰 버튼**, 반나절~하루. 미리 해두지
않는다 — 안 쓰는 기능에 붙은 암호는 잊어버리기만 한다. 참고로 컨트롤 패널은 900px 미만에서
2단이 1단으로 접히므로(`styles.css`), 태블릿을 쓴다면 **가로**여야 한다.

**S-1 근거**(감사에 없던 항목): 배경 업로드는 파일당 64MB 상한만 있고 **총량·개수 제한과
삭제 API 가 없다.** 격리 서버에서 무인증 업로드 3건이 쌓이고 `DELETE` 는 404 임을 확인했다.
디스크가 차면 같은 볼륨의 `data/songs.sqlite` 쓰기가 실패해 **가사 손실로 이어질 수 있다.**

---

## 5. 파일별 점유 현황 (충돌 방지)

최근 수정된 파일과 **주의점**입니다. 같은 파일을 만질 때는 6장에 메모를 남기세요.

| 파일 | 줄 수 | 주의 |
|---|---:|---|
| `server/db/songs.ts` | 1,167 | **가장 충돌 위험 높음.** 곡·곡집·검색·사용기록·검토가 모두 지나감. 스키마 변경은 `addUsageColumns` 처럼 **기존 DB 에서도 도는 마이그레이션**으로 |
| `shared/types.ts` | 1,102 | **서버·클라이언트 단일 계약.** 여기를 고치면 양쪽이 함께 바뀜 |
| `src/control/panels/SongPanel.tsx` | 1,022 | 찬양 탭 2단 레이아웃·즐겨찾기·가사 편집 |
| `src/control/panels/ReviewPanel.tsx` | 508 | 줄나눔 검토 화면 |
| `src/control/panels/PlanPanel.tsx` | **2,549** | **예배 순서 — 가장 많이 쓰는 화면.** 선택/송출 분리가 핵심이라 여기를 고칠 때는 "눌러도 안 나가는지" 를 반드시 재확인 |
| `src/control/hooks/useMeasure.ts` | — | 반환 객체를 `useMemo` 로 고정해 뒀다. 풀면 이걸 의존성으로 쓰는 effect 가 무한 재실행된다 |
| `src/control/styles.css` | — | 여러 패널이 공유. `.main:has(...)` 로 탭별 스크롤을 제어하므로 전역 규칙 수정 주의 |
| `lib/meter-align.ts` | — | 순수 로직. 바꾸면 **1,202곡의 줄나눔이 달라짐** — 반드시 `npm run measure:meter` 로 영향 측정 |
| `public/output/output.js` | — | **의존성 0 유지**(React·CDN·외부 폰트 금지). OBS 내장 브라우저 대비 |
| `PLAN.md` / `README.md` | — | 동작을 바꾸면 **문서도 같이** 갱신(사실 불일치 주의) |

---

## 6. 인계 메모 / 다음 Agent 에게

### 바로 이어서 하기 좋은 작업 → **[PLAN-next-2026-09-08.md](PLAN-next-2026-09-08.md)**

우선순위와 착수 방법을 그 문서에 정리했습니다. 요약만 두면:

| | 무엇 | 왜 |
|---|---|---|
| 1 | **`server/ws.ts` 검사** (문장 57.3%, 미커버 64) | **예배 중에 화면이 도는 통로**인데 통합 검사가 하나뿐이다. 이 저장소에서 안전상 가장 값비싼 빈칸 |
| 2 | **곡집 검사** (`db/songbooks.ts` 36.7% · `routes/songbooks.ts` 36.5%) | 되돌릴 수 없는 **사용자 데이터를 쓰는 길**. '곡집을 곡보다 먼저' 함정에 검사가 없다 |
| 3 | **검사 없는 화면 셋** (`PlanItemEditor` 732 · `PlanAddBar` 308 · `PlanDefaultsCard` 264) | 화면 쪽 버그가 실제로 두 번 났다 (§4.6 D · 태블릿 `lanWanted`) |
| 4 | `server/` 커버리지를 문턱 위로 | 1·2 를 하면 상당 부분 따라 온다 |
| 5 | `SongPanel.tsx` 1,022줄 분할 | 800 규칙 위반 + 검사 0%. **R-4 의 방법이 그대로 통한다** |

**사용자 결정이 있어야 움직이는 것** — **지금은 없습니다.**
2026-09-09 에 넷 다 답을 받았습니다: D-7 · lint · CI 는 닫혔고, **D-6 악보 155장
훑기는 사용자가 보류**했습니다(나중에 다른 방법으로).

새로 물을 것이 하나 생겼습니다 — **`npm audit` 3건**(전부 `@vitest/mocker`,
검사 도구라 앱에는 실리지 않습니다. `--omit=dev` 는 0건). 판을 올릴지 여부.
물어야 할 것을 계획서 6장에 적어 두었습니다.

> **lint 는 2026-09-09 에 최소안으로 도입했습니다** — `tsconfig` 두 줄
> (`noUnusedLocals` · `noUnusedParameters`). 새 의존성 0. ESLint·Prettier 는
> 도입하지 않습니다.
>
> **D-7 도 2026-09-09 에 사용자가 정해 닫혔습니다** — 일괄 재분할을 하지 않습니다.
> 찬양 탭에서 필요한 곡만 그때그때 손으로 맞춥니다. **Agent 가 영어 줄을 자동으로
> 다시 나누는 일은 없습니다.** 자세한 것은 아래 '사용자가 정한 것'.

> ⚠️ 2026-09-08 에 이 절을 다시 썼습니다. 그전 목록(예배 순서 재설계 · D-5 ·
> Phase 6)은 **전부 끝난 일**이었는데 권장 순서로 남아 있었습니다 — 다음 Agent 가
> 완료된 작업을 시작할 상태였습니다. 이 절을 고칠 때는 보드(4장)와 대조하세요.

### 작업 시 지켜야 할 이 프로젝트의 관례

- **추측 말고 실측.** 이 프로젝트의 거의 모든 판단은 측정에서 나왔습니다
  (ESV 판본 비교, 운율 순위 규칙, 폰트 대체 비율, 스크롤바 폭).
  숫자 없이 "좋아 보인다"로 정하지 마세요.
- **화면을 스스로 비우지 않습니다.** 서버가 죽어도 출력 페이지는 마지막 내용을 유지합니다.
  예배 중 검은 화면이 최악의 결과입니다.
- **자동 결과는 제안입니다.** 사람이 확인(승인)해야 확정됩니다.
- **본문을 임의로 고치지 않습니다.** 성경은 `text_raw` 에 원문을 남기고, 가사는
  어절 경계에서만 끊어 **글자가 바뀌지 않음**을 테스트로 고정합니다.
- **출력 페이지는 의존성 0.** OBS 내장 브라우저(CEF)에서 초기화가 실패할 지점을 없앱니다.
- **문서 동시 갱신**: 동작을 바꾸면 README·PLAN 의 해당 문구도 고칩니다.
- **검증 3종은 항상 통과**:
  ```bash
  npx tsc --noEmit && npx vitest run && npx vite build
  ```
- **서버 재기동 필수**: TypeScript 를 네이티브로 실행하므로 **코드를 고치면 서버를
  재시작**해야 반영됩니다. 옛 프로세스가 7777 을 잡고 있으면 구버전이 응답해
  "수정이 반영되지 않은 것처럼" 보입니다(실제로 여러 번 겪음).
  ```bash
  lsof -ti:7777 | xargs kill -9
  ```
- **출력 페이지를 고쳤으면 OBS 소스도 새로고침**해야 합니다(2026-08-15 실제로 겪음).
  서버를 재시작해도 OBS 안의 페이지는 **WebSocket 만 다시 붙고 JS 는 옛 판 그대로**입니다.
  게다가 권장 설정상 '장면 활성화 시 새로고침'이 꺼져 있어 저절로 갱신되지 않습니다.
  → 브라우저 소스 더블클릭 → `현재 페이지 새로고침`.
  옛 판이 떠 있으면 **새로 만든 슬라이드 종류가 화면에 나오지 않습니다** — 출력 페이지는
  모르는 내용을 받으면 이전 화면을 유지하므로(의도된 안전 동작) 검은 화면이 아니라
  "안 바뀜"으로 나타나 원인을 찾기 어렵습니다.
  진단: OBS 렌더러 프로세스 시작 시각과 `public/output/output.js` 수정 시각을 비교하면 됩니다.
  ```bash
  for p in $(lsof -ti:7777); do ps -o pid,lstart,comm -p $p | tail -1; done
  ```
- **브라우저 확인은 직접**: 사용자에게 "확인해 보세요"라고 하지 말고 Agent 가 직접
  띄워서 재고 스크린샷으로 보여 줍니다.

### 결정된 사항 (2026-08-14)

| 질문 | 결정 | 반영 위치 |
|---|---|---|
| 데이터 공유 방법 | **같은 PC 에서만 작업** — git·별도 저장소로 공유하지 않음 | 0.3 |
| 브랜치 전략 | **`main` 직통 + 작게 자주 커밋** | 0장 4번 |
| 문서 구성 | **README·PLAN·KNOWN-DATA-ISSUES 3종 유지** | 2장 |
| Agent 명명 | **계정 이니셜** (`Agent C` 등) | 1장 |
| 협업의 취지 | 같은 Claude Code 의 **다른 계정으로 이어달리기** (사용량 한도) | 1장 |

### 아직 열린 질문 (사용자 결정 필요)

> 답이 정해지면 이 목록에서 지우고 해당 장에 반영해 주세요.

- **lint 도구**: ESLint·Prettier 를 도입할지. 지금은 없고 `tsc --noEmit` 만 씀
- **CI**: GitHub Actions 로 푸시마다 테스트를 돌릴지

---

## 7. 변경 이력 (이 문서)

| 날짜 | 갱신자 | 내용 |
|---|---|---|
| 2026-08-27 | Claude (Agent C) | **새찬송가 한/영 대조 점검** (§3.51) — 숫자상 97.8% 였지만 **정상은 36곡뿐**이었다. 영어가 한국어 줄에 맞춰 들어간 게 아니라 **한 덩이 글을 30자쯤에서 기계적으로 자른** 상태다. 둘째 줄 이후 소문자 시작 4,822줄(38.5%) · 600곡(94%), 첫 줄이 소문자인 경우는 **0건** — 순서대로 자른 증거. 기존 `report:missing-en` 은 짝 없는 줄만 세므로 새 305장 같은 4=4 곡이 '정상' 으로 잡혔다. `scripts/report-bilingual-hymns.ts` 를 새로 만들어 A·B·C·D 로 갈라 목록·내용을 냈다(`npm run report:bilingual`). **읽기만 했고 데이터는 바꾸지 않았다** — 재분할은 사용자 결정 뒤에 |
| 2026-08-26 | Claude (Agent C) | **이 문서의 정합성 정리** — 수치가 실제와 어긋나 있었다. 찬양 1,202→**4,396곡** · 템플릿 9→**8종** · 테스트(4.3장) 453→**1,041개** · `songs.ts` 1,067→**1,089줄**. `D-5`(찬미예수 2000)가 🔴 인데 **DB 에 2,062곡 들어 있어** 🟢 로, `D-7`(한/영 병기)은 영어 가사 12,525줄·637곡이 들어와 🟡 로. 8장의 **중복 블록 정리** — '현재 진행 중' 에 같은 줄 3번, '사용자에게 남은 것' 이 두 벌(내가 갱신하며 옛 것을 지우지 않아 겹쳤다)이었다. 로드맵을 실측으로 갱신하고 **UI 테스트 부채를 4.3 보드에 올렸다**(로드맵에만 있어 보드에서 안 보였다). **과거 기록은 손대지 않았다** — `PLAN.md` 의 단계별 테스트 수, `SECURITY-AUDIT` 의 날짜 붙은 위험 서술은 그때의 사실이다 |
| 2026-08-15 | Claude (Agent J) | **순서 표시 글자 리듬 + 명조 + 실제 사용 폰트 표시**(3.12) — 폰트 이름이 안 잡혀 조용히 대체되던 함정을 화면에서 잡아 준다. 518 passed |
| 2026-08-15 | Claude (Agent J) | **'공백' 항목이 무효였던 버그 수정**(3.11) — 공백과 블랙(B)이 같은 장치를 써서 서로 지웠다. 공백은 내용을 실제로 지우고 blanked 는 B 전용으로 분리 |
| 2026-08-15 | Claude (Agent J) | **옛 판 출력 페이지 자동 감지**(3.10) — OBS 안의 페이지가 앱보다 옛 판이면 컨트롤 패널이 '소스를 새로고침하세요' 배너를 띄운다. 새 슬라이드 종류가 조용히 안 나오던 문제의 재발 방지. 498 passed |
| 2026-08-15 | Claude (Agent J) | **순서 표시 배치**(3.9) — 캡처 요청대로 '왼쪽 순서 이름 · 오른쪽 담당자 + 밑줄'. `SlidePayload` 에 `kind: 'order'` 추가(자리가 다른 두 값은 줄 배열로 표현할 수 없다). 493 passed |
| 2026-08-15 | Claude (Agent J) | **S-11 배경 · S-12 예배 전 안내 완료**(3.8). 사용자 결정 ①~⑤ 반영(배경은 템플릿별, 폴더+업로드 둘 다, 백업 제외, 자동 진행 구간, 8초·순환). S-13(OBS 비의존)은 전체화면이 이미 가능해 문서만 남기고 전용 앱 창은 Phase 6 으로 미룸. 487 passed |
| 2026-08-15 | Claude (Agent J) | **예배 순서 2차 개선 S-8~S-10 완료**(3.7) — 광고 Enter, 순서 표시 항목, 예배 유형 템플릿 + 저장/불러오기. 작업 중 기존 버그 1건(추가 바 포커스 유실) 발견·수정. 468 passed. 남은 3건(배경·예배 전 안내·OBS 비의존)은 [plan-service-tab-2.md](plan-service-tab-2.md) 에 갈림길과 권장안을 정리하고 **사용자 결정 대기** |
| 2026-08-15 | Claude (Agent J) | **Agent J 합류 등록**(1장). 인수 시점 상태를 실측으로 확인 — 작업 트리 깨끗(`624ef1b`, 미푸시 없음), `tsc --noEmit` 통과, `vitest run` **462 passed**. 기준 커밋 표기를 `565dff5` → `624ef1b` 로 갱신(실제 HEAD 와 어긋나 있었음) |
| 2026-08-15 | Claude (Agent C) | **예배 순서 탭 재설계 완료**(3.6) — S-1~S-7. 선택/송출 분리, 인라인 추가 바, 직전으로. 작업 중 기존 버그 3건(useMeasure 재생성·←→ 이중처리·실패 시 잔류) 발견·수정. 462 passed |
| 2026-08-14 | Claude (Agent C) | **'예배 순서' 탭 재설계 제안서** 작성([plan-service-tab.md](plan-service-tab.md)) — 보드 최우선 항목으로 등록. 협업 취지(같은 Claude Code 의 다른 계정 이어달리기)와 Agent 명명(계정 이니셜) 명시 |
| 2026-08-14 | Claude (Agent C) | **협업 전제 확정**(0.3 신설) — 같은 PC·같은 data/ 공유, main 직통, 문서 3종. 그로 인한 동시 편집·포트 충돌·미커밋 혼재 대비를 명시 |
| 2026-08-14 | Claude (Agent C) | **다른 프로젝트(translateviewer)의 협업 규칙을 이 프로젝트로 이식.** 0~1장 협업 방식은 유지, 2장 이하는 이 프로젝트 내용으로 재작성. 파이썬/FastAPI 전제의 명령·경로·작업 보드를 Node/TypeScript 로 교체. **0.2(사용자 데이터 취급) 신설** — 실제 사고(3.2)에서 나온 규칙 |

---

## 8. 진행 중 작업 로그 (Live Work Log) ★중단 대비

> **왜 있는가**: Agent 작업은 사용량 소진·세션 만료·크래시로 언제든 끊긴다. "완료 후
> 갱신" 규칙만 있으면 보드에 `🟡 진행중` 만 남아, 다른 Agent 가 **"누가 하는 중이니
> 손대지 말자"** 고 오해해 작업이 정지한다.
>
> 쓰는 규칙은 [CLAUDE.md](../CLAUDE.md) 에 있다 — 착수 즉시 3줄, 단계마다 `다음 단계:` 갱신.
> **이어받을 때의 판정 절차는 아래 8.1** 을 따른다.


> **규칙 A**: 작업을 시작하면 **코드를 만지기 전에** 여기에 3줄(시작·계획·다음 단계)을 씁니다.
> **규칙 B**: 단계가 끝나면 `다음 단계:` 줄을 갱신합니다. 마지막 갱신 지점이 곧 중단 지점입니다.
> 완료되면 이 항목을 지우고 4장 보드를 `🟢 완료` 로 바꾸고 3장에 결과를 요약합니다.

### 현재 진행 중

- [완료 2026-09-10 / Agent C] 맥·윈도우 빌드 + 설치 편의 (사용자 요청)
  요청 셋: ① 윈도우 설치 과정에서 **필요한 다른 프로그램까지** 설치
           ② 맥은 dmg + **첫 실행 때 필요한 터미널 명령을 자동으로**
           ③ 설치 안내 매뉴얼 갱신
  ⚠️ ②는 **말 그대로는 불가능하다.** 격리(quarantine)를 지우는 일은 앱이 뜨기
    **전에** macOS 가 막는 것이라, 앱 자신이 할 수 없다. 할 수 있는 것은
    DMG 안에 **더블클릭 한 번으로 도는 스크립트**를 함께 넣는 것이다.
    → 진짜 되는지 **실측**한다 (스크립트도 격리가 붙는다. 그때 무엇이 뜨는지 본다)
  ⚠️ ①은 **내가 실행해 볼 수 없다** — 윈도우가 없다. NSIS 스크립트는 CI 가
    빌드까지만 해 주고, 실제 설치 동작은 사용자가 눌러 봐야 안다. 그 사실을
    문서와 보고에 분명히 적는다.
  ── 맥 ─────────────────────────────────────────────────────────
  DMG 에 `맥에서 처음 열기.command` + `먼저 읽어보세요.txt` 를 넣었다.
  실측으로 확인한 것:
    · 스크립트 로직은 작동한다 — 격리를 인위로 붙인 앱에서 xattr 0개로 지워졌다
      (`/Applications` 는 사용자 설치본이라 건드리지 않고 `~/Applications` 로 재현)
    · ❗ **그 스크립트도 macOS 가 막는다.** 격리된 `.command` 를 열면
      '…을(를) 열지 않음 / Apple은 … 악성 코드가 없음을 확인할 수 없습니다' 가 뜨고
      버튼은 [휴지통으로 이동] [완료] 뿐이다. 화면을 찍어 확인했다.
      → 그래서 **읽고 붙여넣을 텍스트 파일**을 함께 넣었다 (텍스트는 안 막힌다)
    · 격리 표시는 `cp`·`ditto` 로 **그대로 따라간다** → "USB 로 받으면 괜찮다" 는
      내가 처음 쓴 문장이 부정확했다. **빌드한 맥에서 곧바로 USB 로** 건네줄 때만
      표시가 없다. 문서를 고쳤다
    · 새 빌드는 서명 신원이 제대로 붙는다 (`Identifier=org.jiwumission.…`,
      09-07 판은 `Identifier=Electron` 이었다)
  ⚠️ 시험 방법을 두 번 틀렸다 — 기록해 둔다:
    · `osascript`→Finder 로 여는 방법은 **대조군도 실패**했다(자동화 권한). 무효였다.
      제대로 된 방법은 셸의 `open` — 대조군은 돌고 격리본만 -128 로 막힌다
    · DMG 창의 세로 배치는 `dmg.contents` 의 y 로 바뀌지 않았다(190↔350 뒤집어
      다시 만들어도 그대로). 순서는 **파일 이름**으로 알린다

  ── 윈도우 ─────────────────────────────────────────────────────
  `build-resources/installer.nsh` — 물어보는 화면(OBS 필수·Chrome 선택, 기본 켬) +
  winget 설치. 실패해도 설치는 끝난다. 명령은 `lib/env-check.ts` 와 같은 것.
  ⚠️ **실행해 본 적이 없다** (윈도우가 없다). CI 는 컴파일까지만 확인해 준다.

  ── build.yml 을 처음 돌려 보니 세 가지가 깨져 있었다 ───────────
    1. `GitHub Personal Access Token is not set` — CI 에서 electron-builder 가
       자동으로 Releases 에 올리려 한다. DMG 는 다 만들어진 뒤라 더 헷갈린다.
       → `--publish never` (dist·dist:mac·dist:win)
    2. `macro named "MUI_HEADER_TEXT" not found!` — 이 파일은 MUI2 보다 **먼저**
       끼워진다 → 제목을 라벨로 직접 그린다
    3. `warning 6010/6001 … not referenced` (경고를 오류로 다룬다) — 제거 관리자는
       **따로 컴파일**되는데 `customPageAfterChangeDir` 는 설치 쪽에만 붙는다
       → Var 선언부터 파일 끝까지 `!ifndef BUILD_UNINSTALLER` 로 감쌌다
       (두 번에 걸쳐 겪었다: 함수만 감쌌더니 변수가 걸렸다)
  🎯 곁가지로 **진짜 결함**을 하나 잡았다: `GET /api/system/env` 가 설치 도구를
    `for` 로 하나씩 찾아 **윈도우에서 6초** 걸렸다(없는 도구는 3초 제한까지 간다).
    설정 탭의 '이 PC 준비 상태' 가 그동안 비어 있었다는 뜻이다 → `Promise.all` 로 3초.

  결과 (커밋 8edb02d 에서 CI 가 만든 것, `release/`):
    SermonPresentation-0.1.0-arm64.dmg (124M) · SermonPresentation-0.1.0.dmg (131M) ·
    SermonPresentation Setup 0.1.0.exe (109M)
  검증: CI 두 잡 모두 초록 · **NSIS 경고 0개**(앞서 6010 이 났던 것이 이제 안 난다 =
    페이지가 실제로 컴파일에 들어갔다는 증거) · DMG 를 마운트해 네 항목 확인 ·
    tsc 0 · vitest 2,044 · vite build.
  ❗ **남은 확인은 사용자 몫이다**: 윈도우 설치 관리자를 실제로 눌렀을 때
    OBS·Chrome 이 깔리는지. 나는 윈도우가 없어 확인할 수 없다.

- [완료 2026-09-09 / Agent C] 매 푸시 CI (사용자 결정)
  지금 있는 워크플로는 `build.yml` 하나뿐이고 **설치 파일**을 만드는 것이다
  (`workflow_dispatch` · `v*` 태그). 검사는 Agent 가 손으로 돌린다 —
  검증했습니다 를 믿는 수밖에 없는 구조다.
  더할 것: `tsc` + `vitest` + `vite build` 를 push·PR 마다.
  ⚠️ **먼저 실측할 것**: `data/bible.sqlite` 는 git 에 없다(저작권 자료).
    검사 일부가 그것을 읽으므로 **CI 에서 몇 개가 깨지는지 세어 보고** 설계를 정한다.
    깨지는 수에 따라: (가) 전부 돈다 → 그대로 (나) 몇 개만 깨진다 → 그것만 건너뛴다
    (다) 많이 깨진다 → CI 의 값이 줄어드니 사용자에게 다시 알린다.
  D-6 악보 155장은 **사용자가 보류**했다 — 나중에 다른 방법으로 하겠다고 했다.
  ⚠️ **첫 측정이 무효였다.** 셸에서 `SERMON_BIBLE_DB` 를 없는 경로로 주고 돌리니
    2,044개가 다 통과했는데 — `vitest.config.ts` 의 `test.env` 가 **셸을 이긴다.**
    실제로는 진짜 성경 DB 를 읽고 있었다 (탐침 검사로 확인).
    제대로 재는 법: **`git clone` 으로 사본을 뜬다** — `data/` 가 git 에 없으니
    그것이 곧 CI 의 상태다. (node_modules 는 심볼릭 링크로 빌렸다)
  실측 결과 (clone 에서):
    · `vitest` **2,013 통과 · 31 건너뜀 · 0 실패** — `bible-api.test.ts` 가
      `describe.skipIf(!hasBibleDb)` 로 스스로 물러난다. 누군가 이미 만들어 둔 장치다
    · `tsc` 0 · `vite build` 통과
    · **`--coverage` 는 깨진다** — server 문장 86.9 → 81.2 · 줄 88.6 → 82.6.
      성경 모듈이 통째로 미커버가 되기 때문. 문턱을 그 값까지 내리면 ratchet 이
      뜻을 잃으므로 **CI 에서 커버리지를 돌리지 않는다.** 로컬·수동으로 남긴다
  만든 것: `.github/workflows/check.yml` — push · PR · 수동. ubuntu-latest,
    **Node 24(지원 하한 = Electron 이 싣는 판) · 26(개발용) 둘 다.**
    개발 판에서만 되는 코드가 설치판에서 깨지는 것이 이 프로젝트가 실제로 겪은 종류다.
    `concurrency` 로 같은 가지의 앞선 실행은 취소한다.
    `ELECTRON_SKIP_BINARY_DOWNLOAD=1` — 포장은 `build.yml` 이 하므로 수백 MB 를 받지 않는다.
  단계를 **하나도 안 믿고 다 돌려 봤다**: clone 에 `npm ci` 를 진짜로 해서
    (node_modules 364개 · 245MB · electron/dist 없음) tsc · vitest · vite build 통과.
    YAML 도 파서로 읽어 확인했다.
  📌 곁가지로 찾은 것: **`npm audit` 이 0건이 아니다** — 3건(moderate).
    전부 `@vitest/mocker` 경로 이탈이고 **검사 도구라 앱에 실리지 않는다**
    (`--omit=dev` 는 0건). 계획서 §0 표의 '0건' 을 실측값으로 고쳤다.
    판을 올리면 검사 2,044개가 흔들릴 수 있어 **CI 를 세우는 김에 하지 않았다** —
    할지 말지는 사용자 결정.
  🎯 **첫 실행에서 CI 가 진짜를 찾았다** — Node 26 통과, **Node 24 실패.**
    `plan-api.test.ts` 의 '1MB 를 넘는 번들도 가져온다' 가 5초를 넘겼다.
    파고들어 실측한 것:
      | 어디 | 그 파일 42개 |
      |---|---:|
      | 러너 · Node 26 | **750 ms** |
      | 러너 · Node 24.20.0 | **14,420 ms** |
      | 맥(로컬) · Node 24.16.0 | 604 ms |
      | 맥(로컬) · Node 26.7.0 | 463 ms |
    **리눅스 러너의 Node 24 에서만** 느리다. 같은 러너에서 `song-search`·`plan-storage`
    같은 것은 두 판이 **똑같다**(2.5s ↔ 2.5s) — 느린 것은 DB 쓰기가 많은 검사뿐이다
    (`songbooks-api` · `ws-hub` 도 4배). `node:sqlite` 쓰기 쪽으로 보인다.
    **설치판에는 영향이 없다** — Electron 의 Node 24 는 맥·윈도우에서 돌고 맥 실측이 604ms 다.
    조치: 그 검사 하나에만 30초를 준다(전역 `testTimeout` 을 올리면 진짜로 멈춘 검사까지
    30초씩 기다리게 된다). 잰 값을 그 자리 주석에 표로 남겨 다시 파헤치지 않게 했다.
  ⚠️ 이것이 Node 24 를 행렬에 넣은 값이다 — 개발 판(26)만 돌렸으면 못 봤다.
  ✅ 고친 뒤 **두 판 다 초록** (Node 24 1m18s · Node 26 1m17s).
  ✅ 첫 실행이 띄운 경고도 처리했다 — `actions/*@v4` 가 **폐기된 Node 20** 을 노린다.
     `check.yml`·`build.yml` 둘 다 `@v7`(node24)로 올렸고, 다시 초록·경고 0.
     판 올리기를 여기서 한 이유: **CI 가 스스로 검증한다.** 올리고 초록인지 보면 된다.
  결과: `.github/workflows/check.yml` — push · PR · 수동 · 1분 남짓.
  📌 남긴 것 (다음 Agent 가 헷갈리지 않도록):
     · `31 skipped` 는 정상이다. **`0 skipped` 가 되면** 오히려 봐야 한다
     · CI 에서 `--coverage` 를 켜지 않는다. 켜면 반드시 깨진다(성경 DB 가 없다)
     · `build.yml`(설치 파일, 20분)은 그대로 태그·수동일 때만 돈다

- [완료 2026-09-09 / Agent C] lint 도입 — 최소안 (사용자 결정: 1번)
  물었던 셋 중 **①최소**: 새 도구 없이 tsconfig 두 줄(`noUnusedLocals` ·
  `noUnusedParameters`)만 켠다. ESLint·Prettier 는 도입하지 않는다
  (Prettier 는 사람이 공들여 배치한 주석 표를 흐트러뜨리고 저장소 전체 diff 를 만든다).
  실측(2026-09-09): 죽은 것 **35건** — noUnusedLocals 28 · noUnusedParameters 7.
    src/ 29 · scripts/ 3 · tests/ 1 · server/ 1 · lib/ 1
  커밋 순서: **정리 먼저, 설정 나중.** 설정을 먼저 켜면 그 커밋에서 typecheck 가
    깨진 채로 남는다 — 어느 커밋을 집어도 초록이어야 한다.
  ⚠️ 기계적으로 지우지 않는다. `server/db/bible.ts` 의 안 쓰이는 `limit` 처럼
    **진짜 버그일 수 있는 것**이 섞여 있다 — 하나씩 본다.
  ✅ 커밋 둘로 나눴다 — `a9fc600`(정리 35건) → `tsconfig` 두 줄. 동작 변화 0, 전부 삭제.
     대부분 **R-4 · SongPanel 리팩터링의 잔재**였다: 훅 객체째로 넘기게 바꾼 뒤로
     아무도 안 쓰는 낱개 이름들(PlanPanel 의 storage 낱개 아홉 줄은 통째로) ·
     죽은 프롭 다섯(PlanCueList.template · PlanItemEditor.plan/rows/auto/readingBook —
     선언·전달·검사 세 곳에 있었다) · `SONG_HIT_LIMIT` 은 **설명 주석까지 두 벌**.
  ⚠️ 하나씩 확인한 보람이 있었다:
     · `bible.ts` 의 안 쓰이는 `limit` → 버그가 아니라 중복 (searchLike·searchFts 가 각자 계산)
     · 검사의 `verseSections(source)` → `createSong` 이 `linesSource` 를 따로 넘긴다.
       검사는 멀쩡하고 인자만 군더더기였다
     · PlanAddBar 에서 **줄 번호를 잘못 짚어** `LITURGY_TEXTS` 를 지웠다가 tsc 가 즉시 잡았다.
       TS 가 가리킨 22번 줄은 `lang-select` import 였다
     · 죽은 함수를 걷어내니 **그것만 쓰던 import 둘이 새로 드러났다**(카스케이드).
       한 번 돌리고 끝나지 않는다 — 0 이 될 때까지 돌린다
  설정이 진짜 도는지 확인: 일부러 죽은 변수를 넣으면 TS6133 으로 잡히고,
    `_` 로 시작하는 인자는 면제된다. `npm run build:server`(포장용, 같은 tsconfig 를
    상속한다)도 통과.
  검증: tsc 0 · vitest 2,044 · vite build (번들 415.59 → 415.20 kB).
    브라우저(격리 서버 7863, 사용자 DB 사본): 예배 순서 항목 설정 · 교독문 추가 →
    송출 · 찬양 탭 검색 → 곡 열기 → 두 칸 가사 편집기 전부 정상.
  ⚪ **ESLint · Prettier 는 도입하지 않는다** (사용자 결정). 필요해지면 그때 얹는다 —
    ①과 충돌하지 않는다.

- [완료 2026-09-09 / Agent C] D-7 한/영 병기 — **사용자가 방식을 정했다**
  물었던 세 가지에 대한 답:
    ① 재분할 → **수동.** 곡별로 손으로 맞춘다 (자동 일괄 재분할을 하지 않는다)
    ② 검토 탭 승인 → **필요 없다.** 승인 절차를 만들지 않는다
    ③ C 등급 490곡 → **전부 훑지 않는다.** 찬양 탭에서 **그때그때 필요한 것만**
  그래서 이 항목은 'Agent 가 할 일' 이 아니라 **사용자가 쓰면서 하는 일**이 됐다.
  Agent 가 해야 할 코드 작업은 없다 — 확인해 보니 길이 이미 그렇게 나 있다:
    · 찬양 탭에서 가사를 저장하면 `replaceSections` 가 `lines_source='manual'` 로
      찍는다 (`server/db/songs.ts:730` 기본값). **자동 스크립트가 손대지 않는
      표시**이므로, 손으로 맞춘 곡이 나중에 재정렬에 쓸려 갈 일이 없다.
    · `npm run lyrics:realign` 을 비롯한 자동 작업은 `'auto'` 만 대상으로 한다.
      새찬송가 645곡은 `'imported'`(2026-08-28 relabel) 라 역시 보호된다.
  **하지 않은 것**: 자동 재분할 스크립트 · 검토 탭의 한/영 승인 화면 ·
    찬양 탭의 '대조 깨진 곡 찾기' 목록. 사용자가 "그 때 그 때 필요한 것들을
    해 나갈게" 라고 했으므로 **미리 짓지 않는다.** 필요해지면 그때 만든다.
  `npm run report:bilingual` 은 남겨 둔다 — 읽기만 하는 스크립트라 해가 없고,
    어느 곡이 깨졌는지 알고 싶을 때 쓸 수 있다 (터미널판 한정).

- [완료 2026-09-09 / Agent C] PLAN-next 4번 — server/ 커버리지 마무리
  실측(2026-09-09): server 문장 **85.1** · 분기 78.5 (문턱 84/77 — 이미 위다).
  그러니 목적은 '숫자 넘기기' 가 아니라 **아직 안 밟아 본 위험한 길을 밟아 보는 것**이다.
  계획서 §4 목록 + 계획서에 없던 것 하나를 실측으로 찾았다:
    · `routes/tablet.ts` **36.4%** ← 계획서에 없다. server 에서 가장 낮다
    · `routes/system.ts` 70.3 (**보안 경로**: 암호·랜 열기·데이터 폴더·설치 명령)
    · `routes/backgrounds.ts` 75.9 (**분기 51.1** — 파일을 지우는 길이다)
    · `db/readings.ts` 76.8 · `lan.ts` 65.6
  순서: 위험이 큰 것부터 — system → tablet → backgrounds → readings → lan.
  각 묶음마다 변이 검증(값을 일부러 틀리게 고쳐 검사가 잡는지)을 한다.
  ✅ db/readings.ts — 검사 13개. 옛 스키마 → 새 스키마 **마이그레이션이 검사 0 이었다**
     (사용자 교독문을 통째로 옮기는 길이다). `migrateToBookKey` 가 연결을 받도록
     열어 두고(기본값 그대로) 메모리 DB 로 검사한다 — 실제 app.sqlite 를 담보로 잡지 않는다.
     변이 10개 전부 잡힘. 그중 하나는 **처음에 못 잡았다**: ROLLBACK→COMMIT.
     이름 충돌로 막으면 *첫 문장*에서 죽어 되돌릴 것이 없다 → title NULL 로
     `INSERT … SELECT` 도중에 막는 검사를 더해서 잡았다.
  ✅ routes/tablet.ts + lan.ts — 검사 9개. 있던 검사는 **닫혀 있을 때만** 봤다 —
     QR 을 만드는 줄이 한 번도 안 밟혔다. `node:os` 의 장치 목록만 가짜로 바꿔
     (실제 랜 카드에 기대면 카드 없는 PC 에서 헛돈다) 열린 경우를 밟는다.
     핵심: **주소만으로 만든 QR 과 경로가 글자까지 같은지** 본다 → 담긴 것이 주소뿐이라는 증명
     (= QR 에 암호를 담지 않는다). 덧붙이면 경로가 달라지는 것도 함께 못 박았다.
     변이 7개 전부 잡힘. `internal` 변이는 **처음에 못 잡았다** — 가짜 루프백이
     127.0.0.1 이라 사설 대역 규칙에 이미 걸렸다 → 사설 대역인 내부 주소(192.168.99.1)를
     넣어야 그 줄만 따로 확인된다.
  ✅ routes/backgrounds.ts — 검사 15개. 분기 51.1 → 97.4. 밟히지 않았던 것:
     **경로 이탈 막이**(`../..` 로 데이터 폴더 밖을 보는 것) · 폴더 안의 폴더 ·
     맥이 만드는 `._bg_1.png`(확장자는 그림인 숨김 파일) · 뿌리에 그림이 있을 때.
     ⚠️ **검사가 진짜 어긋남을 찾았다**: 폴더 개수는 `statSync` 를 안 보고 이름만
       훑어서 **이름이 `.jpg` 인 폴더를 한 장으로 셌다.** 화면은 '4장', 실제로
       넘어가는 것은 3장. 세는 규칙과 나열하는 규칙이 갈려 있던 것 — `countImages`
       가 `scanFolder` 를 그대로 쓰게 해서 한 규칙으로 합쳤다.
     변이 11개 전부 잡힘. 셋은 **처음에 못 잡았다** — 셋 다 '자료가 그 줄을 건드리지
       않아서' 였다(숨김인데 그림 · 이름이 그림인 폴더 · 공백 든 파일 이름). 자료를 더했다.
  ✅ routes/system.ts — `openFolder` 의 플랫폼 분기를 `lib/env-check.ts` 로 옮겼다
     (`folderOpenCommand`). 라우트에 있으면 **지금 도는 PC 의 줄만** 밟혀서 윈도우
     줄은 설치판 받은 사람이 눌러 보고서야 드러난다. 세 플랫폼 다 검사한다.
     ⚪ 나머지(74.3%)는 **일부러 남긴다** — 실제로 Finder 창을 띄우거나
       `brew install` 을 돌려야 밟힌다. 검사가 남의 프로그램을 설치하게 둘 수 없다.
       판단(허용된 명령·도구 경로·어느 명령)은 전부 `lib/` 에서 검사된다.
  ✅ 덤: `useFeedback.ts` 가 **0%** 였다 (2026-09-08 이름을 바꾸며 공용화했는데
     검사들이 전부 가짜를 넣고 있었다). 검사 5개 — 특히 **세터가 늘 같은 것**이라는
     약속. 어기면 `usePlanSend` 의 memo 가 통째로 헛도는데 눈에 보이지 않는다.

  결과 (2026-09-09 실측):
    server 문장 85.1 → **86.9** · 분기 78.5 → **80.4** · 줄 87.0 → **88.6**
    routes/tablet 36.4 → **100** · db/readings 76.8 → **97.1** ·
    routes/backgrounds 75.9 → **94.3** · lan 65.6 → **93.8** · routes/system 70.3 → 74.3
    검사 2,000 → **2,044** / 118파일. ratchet 을 84/86/77/84 → **87/88/79/86** 으로 올렸다.
  검증: `tsc` 0 · `vitest` 2,044 · `vite build` · coverage 문턱 통과.
    브라우저(격리 서버 7862): 태블릿 QR 두 장이 실제로 그려진다(en0 · en7, 25모듈) ·
    그림 폴더 드롭다운이 '주일 예배 — **3장**' 으로 나오고 실제 나열도 3장 ·
    `../..` 는 빈 목록.
  다음 단계: 계획서에 남은 것은 **사용자 결정이 필요한 것들뿐**이다 —
    D-7 한/영 병기(세 가지 답이 필요) · lint 도입 · 매 푸시 CI · D-6 남은 판단
```
- [완료 2026-09-08 / Agent C] PLAN-next 5번 — SongPanel.tsx 1,022줄
  왜: 800줄 규칙 위반 · 검사 0%. 남은 가장 큰 화면 빚이다.
  방법은 계획서 §5(= R-4 에서 통한 것) 그대로:
    ① **자르기 전에 결합을 실측한다** — 블록별로 쓰는 이름과 그 출처를 센다.
       R-4 가 전에 멈춘 이유가 '프롭 60개짜리 껍데기' 였고 실측이 그것을 막았다
    ② 산발을 먼저 걷는다 (여러 블록에 1~4개씩 흩어진 상태를 훅으로)
    ③ 낱개 값이 아니라 **훅 객체**를 넘긴다
    ④ 블록당 프롭 10개를 넘으면 자르는 자리가 틀렸다는 신호
    ⑤ 자를 수 있다고 자르지 않는다
    ⑥ 단계마다 tsc + vitest + build, 그리고 **브라우저로 직접** 눌러 본다
  ⚠️ server/db/songs.ts(1,184줄)는 보드가 ⚪ 보류다 — 화면과 서버를 **같이
     건드리지 않는다**.
  0~2단계 완료 (커밋 d69da35 + 이번): **1,022 → 730줄** (800 규칙 안).
        · 0단계 usePlanFeedback → useFeedback (예배 순서 전용이 아니었다)
        · 1단계 useSongSearch (곡집·검색·빠른 칩 줄)
        · 2단계 useSongEditor (연 곡·표시 설정·가사 편집·곡 만들기/지우기)
        · 죽은 상태 셋(linking·linkHits·entryDraft)과 죽은 import 열 개를 걷었다
  **실측을 두 번 했다.** 처음 스크립트가 여러 줄 구조분해를 놓쳐 왼쪽 열이
        24 → 4 로 줄어든 것처럼 보였다. 고쳐 재니 이름 수는 그대로였다 —
        패널이 훅 객체를 다시 낱개로 풀어 쓰기 때문이다. **중요한 것은 이름 수가
        아니라 출처 수**다(컴포넌트로 뺄 때 넘길 프롭이 그것이다):
          곡집 관리 4 · 왼쪽 열 6 · 오른쪽 열 5 — 가드레일(10) 안이다.
  브라우저 실측(격리 서버 7860, 사용자 DB 사본): 검색·곡 열기·가사 편집 미리보기·
        표시 언어 토글·번호 즉시 송출(Enter) 전부 정상. 오류 배너 없음.
  3단계 완료: 훅 검사 37개 (song-editor 24 · song-search 13).
        useSongEditor **95%** · useSongSearch **100%** · src/** 32.8 → **37.9%**.
        문턱을 올렸다 (src: 문장·줄 32→37 · 함수 27→31 · 분기 32→34).
  **변이 검증 아홉 가지 전부 잡힌다**: 빈 가사도 저장 · 지울 때 안 묻기 ·
        곡 바꿔도 악보 보기 유지 · 지운 뒤 목록 안 비움 · 없는 언어로 송출 ·
        디바운스 제거 · 즐겨찾기 비어도 대체 안 함 · onError 를 의존성에 ·
        곡집 범위 무시
  **겪은 것**: 디바운스 변이(200 → 0)가 처음엔 **안 잡혔다.** 진짜 시계로는
        '아직 안 갔다' 를 증명할 수 없다 → 가짜 시계로 DEBOUNCE_MS-1 에서
        안 가고 +1 에서 가는 것을 본다. 상수를 훅에서 내보내 검사와 한 값을 쓴다.
  **자르지 않고 멈췄다** (계획서 §5 ⑤ '자를 수 있다고 자르지 않는다'):
        730줄로 규칙 안이고, 출처 실측이 4~6이라 자를 수는 있다. 그러나 남은
        JSX 는 대부분 표시일 뿐이고 위험한 규칙은 전부 훅으로 내려가 검사가
        붙었다. 더 자르면 파일 수만 늘고 얻는 것이 적다 — 필요해지면 그때
        자른다(안전망이 생겼다).
  다음 단계: PLAN-next 4번(server 커버리지 마무리) 또는 사용자 결정이 필요한
        D-7 한/영 병기·lint 도입
- [완료 2026-09-08 / Agent C] PLAN-next 3번 — 검사 없는 화면 셋
  왜: R-4 에서 화면을 갈랐지만 셋에 검사가 없다. **화면 쪽 버그가 실제로 두 번
      났다** — §4.6 D(조작 화면이 서버 거절을 조용히 버림)와 태블릿 카드
      lanWanted 결함. 후자는 브라우저로 손으로 눌러서야 찾았다.
  대상: PlanItemEditor 732 (항목별 설정 전부 — 가장 크고 가장 많이 만진다) ·
        PlanAddBar 308 (종류 열 개의 입력칸 갈림) · PlanDefaultsCard 264
  틀: tests/unit/plan-cue-list.test.tsx · plan-storage-ui.test.tsx 를 본뜬다.
      거기 적힌 함정 둘 — scrollIntoView 가 jsdom 에 없다(검사에서 채운다) ·
      styleTemplates 를 비우면 <option> 이 없어 select 값이 반영되지 않는다
      (BUILTIN_TEMPLATES 실물을 쓴다)
  순서: 작은 것부터 — DefaultsCard → AddBar → ItemEditor (틀을 먼저 굳힌다)
  PlanItemEditor 는 검사를 붙이면서 **자를지 판단**한다 (계획서 §5 의 방법)
  결과: plan-defaults-card 23 · plan-add-bar 39 · plan-item-editor 56 = **118개**.
        검사 1,845 → **1,963개**. 커버리지:
          PlanAddBar 0 → **92.3** · PlanDefaultsCard 0 → **90.9** ·
          PlanItemEditor 0 → **60.0** · src/** 문장 25.7 → **32.8**
        문턱을 올렸다 (src: 문장·줄 24→32 · 함수 15→27 · 분기 20→32).
  **겪은 것 — 검사 틀에 못을 박았다**: 컴포넌트가 `e.target.value` 를
        `patchDefaults((c) => …)` **안에서** 읽는다. 가짜 훅이 그 함수를 나중에
        부르면 React 가 제어 컴포넌트 DOM 값을 되돌린 뒤라 **바뀌기 전 값**이
        읽힌다. 진짜 훅은 상태 갱신 중 즉시 부르므로 문제가 없다 —
        가짜도 **그 자리에서 적용해 쌓도록** 맞췄다. (userEvent 로 바꿔도 같았다.
        원인은 이벤트 방식이 아니라 '언제 읽느냐' 였다.)
  또 하나: 이 화면들의 `<label>` 은 컨트롤과 **연결돼 있지 않다**(htmlFor 없음).
        getByLabelText 가 듣지 않아, 같은 줄(.row)에서 찾는 헬퍼를 썼다.
        검사 편의로 화면에 id 를 뿌리지 않았다 — 연결해 두면 레이블을 눌러
        컨트롤로 갈 수 있어 실제로도 낫다. **남은 개선거리로 기록한다.**
  **변이 검증 18가지 전부 잡힌다** (카드 6 · 추가 바 6 · 편집 칸 6):
        추가 바 따라가기 없앰 · 대조 상한 없앰 · 제목 띄우기 기본 뒤집기 ·
        '지정 안 함' 이 0 담기 · 한글 조합 중 Enter 처리 · 광고를 한 줄 칸으로 ·
        Shift+Enter 도 추가 · 참조 확인 색 고정 · 폴더 안 골라도 추가 ·
        주 역본이 보조에 남음 · 인용구 미리보기 안 지움 · 송출 안 따라옴 ·
        배치 '좌우' 가 값 남김 · 쌓기에도 담당자 크기 · 송출 중 아닌데 다시 보냄
  **PlanItemEditor 는 자르지 않았다** (계획서 §5 판단): 732줄로 한계(800) 안이고,
        블록이 종류별로 갈려 있어 **한 종류만 보면 되는 구조**다. 자르면 프롭
        스물여덟을 나눠 넘기게 되어 R-4 가 멈췄던 자리로 되돌아간다.
        검사가 붙었으니 다음에 자를 때는 안전망이 있다.
  다음 단계: PLAN-next 4번(server 커버리지 마무리) 또는 5번(SongPanel 1,022줄)
- [완료 2026-09-08 / Agent C] PLAN-next 2번 — 곡집 검사
  왜: **되돌릴 수 없는 사용자 데이터를 쓰는 길**이다. data/songs.sqlite 의 가사는
      git 에 없어 백업뿐이고, 실제로 한 번 지웠다. 그런데 두 파일이 저장소에서
      커버리지가 가장 낮은 축이다 (db 36.7% · routes 36.5%, 미커버 130).
  이미 드러난 함정(2026-09-07 번들 작업 079d654): **곡집을 곡보다 먼저 만들어야
      한다.** 순서가 뒤바뀌면 setEntries 가 수록 정보를 버리고 곡이 전부 '기타' 로
      떨어진다. 지금 이 규칙은 routes/backup.ts 안에서만 지켜지고 **곡집 쪽에는
      그것을 붙잡는 검사가 없다** — 다음에 누가 순서를 바꾸면 조용히 재발한다.
  계획: 계획서 §2 목록 — 만들기·이름 바꾸기·지우기 / 곡이 남아 있는 곡집을 지울 때 /
        setEntries 경계(곡집 없음·번호 겹침·빈 배열) / **위 함정을 검사로 고정** /
        다대다 통합 검색 / songCount 집계
  끝났다고 볼 조건: 두 파일 문장 80% 이상 + 변이 검증
  결과: tests/unit/songbooks-store.test.ts **41개** +
        tests/integration/songbooks-api.test.ts **34개**. 검사 1,770 → **1,845개**.
        db/songbooks **36.7 → 96.7** · routes/songbooks **36.5 → 97.6** ·
        server/** 문장 79.5 → **85.1**.
        문턱을 올렸다 (server: 문장 78→84 · 줄 79→84 · 함수 83→86 · 분기 71→77).
  **함정을 검사로 못 박았다**: 곡집 없이 setEntries 를 부르면 수록 정보가
        통째로 '기타' 로 떨어지고 **번호가 사라진다**(그래서 '새 305' 가 안 된다).
        정상 순서와 나란히 두어, 다음 사람이 규칙을 읽지 않아도 검사가 막는다.
  덮은 것: 내장 곡집 셋·중복 심기 / songCount(한 곡이 여러 곡집) / 만들기(빈 이름·
        id 충돌·한글 이름 순번·대표 글자 두 자) / 바로가기 슬롯(뺏기·범위 밖·비우기) /
        고치기(빈 값 무시·내장 번호 체계 보호) / 지우기(기타로 대피·다른 곡집에도
        있으면 그대로·내장 보호·이미 기타에 있어도 안 깨짐) / findGaps(번호 없는
        수록곡 제외) / markImported / **옛 스키마 이주 8개**(양방향 대응곡·인덱스
        먼저 삭제·두 번 불러도 안전) / REST 전부 / **일괄 반입**(text·songs·replace·
        건너뜀 알림·빠진 번호·번호 없는 곡집) / 미리 보기가 저장하지 않는 것
  **변이 검증 아홉 가지 전부 잡힌다**: 기타 대피 제거 · 지울 때 곡 안 옮김 ·
        내장 곡집 삭제 허용 · 내장 번호 체계 변경 허용 · 슬롯 안 뺏음 ·
        replace 인데 안 지움 · 대응곡 한 방향만 · 번호 없는 곡집에 번호 저장 ·
        미리 보기가 저장까지 함
  다음 단계: PLAN-next 3번 — 검사 없는 화면 셋
        (PlanItemEditor 732 · PlanAddBar 308 · PlanDefaultsCard 264)
- [완료 2026-09-08 / Agent C] PLAN-next 1번 — server/ws.ts 검사
  (인계 확인: 미커밋 변경 0 · 원격과 동기화됨 · tsc 0 · 검사 1,744/103파일.
   커버리지도 직접 재서 계획서 숫자와 같은 것을 확인했다 — ws.ts 문장 57.33%.)
  왜 여기부터: 예배 중에 **화면이 도는 통로**다. 깨지면 예배가 멈춘다.
  그런데 통합 검사가 ws-style 하나뿐이다.
  계획: 계획서 §1 의 목록을 그대로 따른다 —
        역할별 접속·접속 수 집계 / 붙는 즉시 스냅샷 / deck:load 가 deck-guard 를
        지나는가 + **거부 로그** / style:set 관문 / goto·next·prev 경계 /
        모르는 종류·깨진 JSON 에 죽지 않는가 / 끊긴 소켓에 쏘지 않는가 /
        live_state 저장·복구
  끝났다고 볼 조건: ws.ts 문장 85% 이상 + **변이 검증**(검사가 헛돌지 않는지)
  결과: tests/integration/ws-hub.test.ts **26개**. 검사 1,744 → **1,770개**.
        ws.ts 문장 **57.3 → 86.0** (줄 85.3) · server/** 문장 77.5 → **79.5**.
        문턱을 올렸다 (server: 문장 76→78 · 줄 78→79 · 함수 82→83 · 분기 70→71).
  덮은 것: 붙는 즉시 스냅샷(상태·템플릿·덱) / 덱은 컨트롤·강사 모니터에만 /
        프로젝터는 다른 템플릿 / 역할·레이어별 접속 수(preview 는 안 셈) /
        끊길 때 컨트롤에 통보 / deck:load 문지기·거부 로그·거부 시 상태 불변 /
        goto·next·prev 경계 / 블랙·복구·비우기 / 깨진 JSON·t 없음·모르는 종류·
        처리 중 예외에 죽지 않음 / template:set(없는 id 포함) / client:error 전달 /
        measure:report / pushTemplate
  **변이 검증 일곱 가지 전부 잡힌다** (거부 로그 삭제 · deck-guard 우회 ·
        close 때 집계 미갱신 · preview 를 OBS 로 셈 · 덱을 출력 페이지에도 보냄 ·
        프로젝터도 활성 템플릿 · 모르는 종류를 조용히 무시). 문턱을 99 로 올려
        ratchet 이 실제로 거는 것도 확인했다.
  겪은 것 셋 (전부 검사가 헛돌 뻔한 것):
    · **`resetStateForTest()` 는 `listeners.clear()` 까지 한다** — 허브의 구독이
      끊겨 첫 검사 뒤로 상태 변화가 아무 화면에도 안 갔다. `state.clear()` 로 바꿨다.
    · 서버는 붙는 즉시(hello **전에**) `state` 를 한 통 보낸다. 그것을 기다리면
      hello 가 처리되기 전에 돌아와 접속 수가 `output: 3` 으로 어긋났다 →
      hello 응답으로만 오는 `template` 을 기다린다.
    · 접속 수 검사가 **집계 함수만** 봐서, 끊길 때 컨트롤에 알리는 것을 빼도
      통과했다(변이 ③). 컨트롤이 받는 통보까지 보도록 고쳤다.
  함께 고친 것: `WsHub.counts()` 의 타입이 실물과 달랐다 (`{control, output}` 만
        적혀 있었으나 실제로는 stage·projector 도 준다). 검사를 쓰다 드러났다.
  다음 단계: PLAN-next 2번 — 곡집 검사 (db/songbooks 36.7% · routes 36.5%)
- [완료 2026-09-07 / Agent C] src/(React) 테스트 — 가장 큰 빚 (사용자 요청)
  결과: 검사 1,440 → **1,500개**(96파일). src/ 줄 커버리지 **0 → 7.0%**.
        보드 4.3 의 '🔴 UI 테스트 없음' 을 🟡 시작함으로 바꿨다.
  틀(harness):
    · `.test.tsx` 가 `include` 에 없어 **"No test files found" 만 나왔다** → 넓혔다
    · DOM 은 **파일별** `// @vitest-environment jsdom` 으로 준다 — 전역을 바꾸면
      통합 검사 1,440개가 함께 영향받는다
    · `afterEach(cleanup)` 을 파일마다 명시한다 (globals:false 라 자동으로 안 붙고,
      없으면 다음 검사에서 같은 요소가 두 개 잡힌다 — 실제로 겪었다)
    · 새 devDependency 셋: jsdom · @testing-library/react · user-event.
      **프로덕션 의존성은 그대로 5개다.** jest-dom 은 안 들였다 (필요한 것이
      `disabled` 값 하나여서 직접 봤다)
  덮은 것 (값이 큰 순서):
    · useLiveState 19개(줄 93%) — 컨트롤 화면의 모든 표시가 지나는 곳
    · TabletSetup 16개(줄 83%) — 암호 없이 랜을 열지 못하게 하는 화면 쪽 관문
    · api.ts 의 request 14개 — 서버의 거절(403·409·429)을 사람에게 전하는 통로
    · useMeasure 11개 — **이미 사고가 난 자리**(useMemo 를 풀면 미리보기가 빈다)
  **넷 다 고치기 전 코드로 되돌려 검사가 실패하는 것을 확인했다.**
  드러난 것: **server/ 커버리지가 79.5% 로 80% 아래다.** 새로 생긴 일이 아니라,
        전에는 lib/(97.9%)와 한 덩이로 재서 평균 86% 로 통과하며 가려져 있었다.
        묶음별 문턱(ratchet)으로 바꾸니 보였다 — 문서에 사실로 적었다.
  검증: tsc 0 · vitest 1,500개/96파일 · vite build · npm audit 0건 ·
        `vitest run --coverage` 통과
  다음 단계: PlanPanel.tsx(2,549줄)이 남은 가장 큰 빚이다. **R-4 훅 분리 뒤에**
        붙이는 편이 낫다 — 예배를 진행하는 화면이라 기계적으로 밀어붙이지 않는다.
- [완료 2026-09-08 / Agent C] 우선순위 계획서 작성 (사용자 요청 — Agent 교대 예정)
  왜: 4.5·4.6·4.7 과 R-4 가 닫히면서 보드에서 🔴·🟡 이 거의 사라졌다.
      "다음에 무엇을" 이 흐려진 상태였다.
  **드러난 것 하나**: §6 '바로 이어서 하기 좋은 작업' 의 권장 0~2번(예배 순서
      재설계 · D-5 · Phase 6)이 **전부 이미 끝난 일**이었다. 다음 Agent 가 그것을
      읽고 완료된 작업을 시작할 상태였다 — 그 절을 다시 썼다.
  결과: **docs/PLAN-next-2026-09-08.md** (계획서). §6 이 그것을 가리킨다.
  정한 우선순위와 근거 (비용이 가장 큰 실패 = '예배 중에 화면이 멈추는 것' 기준):
    1) server/ws.ts 검사 — 문장 57.3%, 미커버 64. **화면이 도는 통로**인데
       통합 검사가 하나뿐이다 (ws-style). 안전상 가장 값비싼 빈칸
    2) 곡집 검사 — db/songbooks.ts 36.7% · routes/songbooks.ts 36.5%.
       되돌릴 수 없는 사용자 데이터 + '곡집을 곡보다 먼저' 함정에 검사가 없다
    3) 검사 없는 화면 셋 — PlanItemEditor 732 · PlanAddBar 308 · PlanDefaultsCard 264
    4) server/ 커버리지를 문턱 위로 (1·2 를 하면 상당 부분 따라 온다)
    5) SongPanel.tsx 1,022줄 분할 — R-4 방법 그대로
  **D-7(한/영 병기)을 1번으로 두지 않은 이유**: 값은 가장 크지만 재분할 방식이
      사용자 결정 대기다(§3.51). lint·CI·D-6 도 같다 — 물어야 할 것을 계획서 6장에 적었다.
  실측한 것: 커버리지 파일별(coverage-final.json 집계) · 800줄 넘는 파일 다섯 ·
      보드 4.1~4.7 전수 대조. tsc 0 · vitest 1,744개 · vite build 통과 상태에서 시작.
  다음 단계: 계획서 1번(server/ws.ts 검사)부터. 격리 서버 7840 을 쓰고
      **7777 은 건드리지 않는다** — 지금 사용자 서버가 떠 있다.
- [완료 2026-09-08 / Agent C] PlanPanel JSX 분할 → 화면 검사 (사용자 요청, 순서 지정)
  결과: 88a35a9(0단계 산발) · aeaf483(1단계 저장 계열 넷) · f501873(2단계 add-bar) ·
        1516cb9(3단계 cue-list) · 547ce76(4단계 액션·기본설정) · fb2e7af(검사 59개).
        PlanPanel.tsx **1,378 → 444줄** (R-4 통산 3,147 → 444).
        검사 1,685 → **1,744개**. **R-4 를 마쳤다.**
  결정적 판단: **자르기 전에 블록별 이름의 출처를 셌다.** add-bar 는 이름
        스물여덟 중 스물다섯이 usePlanAdd 하나에서 온다 → 낱개로 넘기면 프롭 28,
        객체째로 넘기면 4. **구조 원칙은 이것 하나였다: 훅 객체를 넘긴다.**
        훅 분리가 프롭 그룹을 이미 정해 놓았으므로 새로 설계할 것이 없었다.
  가드레일이 실제로 일했다: 블록당 프롭 10개를 기준으로 두니 cue-list 가 11 로
        걸렸고, 원인이 '지금 무엇이 나가는지' 판정 셋이 화면에 있는 것이었다 →
        usePlanSend 로 보내 9 가 됐다. usePlanFeedback·usePlanBackgrounds 도
        같은 판정에서 나왔다 (산발을 안 걷고 자르면 컴포넌트마다 프롭이 1~4개 늘었다).
  **자를 수 있다고 자르지 않았다**: PlanDefaultsCard(264줄)는 계획대로 안을
        다시 봤고, 6행이 한 폼이라 나누면 select 한 줄짜리 껍데기 여섯이 된다.
  겪은 것:
    · 측정이 양방향으로 틀린다. 따옴표 안을 지우니 planNoun·loadOpen 을 놓쳤고
      (하한), 쌍따옴표를 안 지워 rows·error 를 과대 계상했다 (상한). tsc 가 잡아 준다.
    · **브라우저 창이 숨어 있으면 키 이벤트가 페이지에 아예 도달하지 않는다**
      (window.innerWidth 가 0). 처음에 그것을 회귀로 오해했다.
    · scrollIntoView 는 jsdom 에 없다. 컴포넌트에 검사용 방어 코드를 넣지 않고
      검사에서 채웠다.
    · 변이 검증이 **내 검사의 빈틈**을 또 하나 찾았다 — '이름 바꿀 때 겹치면
      확정 버튼이 막힌다' (안내문만 보고 있었다).
  함께 고친 것: "같은 이름의 유형**가**" → "유형**이**" (원래 있던 조사 오류).
  검증: tsc 0 · vitest 1,744개/103파일 · vite build · --coverage 통과 (src 20 → 24)
        변이 검증 24건 전부 잡힘.
        격리 서버 7832 에서 단계마다 브라우저로 (사용자의 7777 이 떠 있어 건드리지 않았다).
        특히 3단계에서 선택·펼치기·송출·자동 진행 순환·항목 이동·키보드(↓ Enter
        Shift+Tab)를 전부 눌러 봤다. 콘솔 오류 0.
  다음 단계: PlanAddBar · PlanDefaultsCard · PlanItemEditor 에 아직 검사가 없다.
        이 방식(결합 실측 → 훅 객체로 넘기기 → 프롭 10 가드레일)은 다른 탭에도
        그대로 통한다 — SongPanel(1,150줄) · TemplatePanel(700줄)이 다음 후보다.
- [완료 2026-09-07 / Agent C] R-4 이어서 — 척추 · 저장 · 추가 (사용자 요청)
  결과: 56fbbaa(usePlanDraft + usePlanStorage) · c0e2989(usePlanAdd) ·
        12057cd(검사 90개). PlanPanel.tsx **2,032 → 1,378줄**,
        검사 1,595 → **1,685개**. **R-4 훅 분리를 마쳤다.**
  판단: 읽기·저장과 추가가 **둘 다** 같은 것(plan·items·cursor·expandedId·
        dirty·patchItems)을 붙잡고 있었다. 그것을 두고 두 훅을 만들면 프롭 열
        개씩 받는 껍데기가 된다 — R-4 가 전에 멈춘 이유가 그것이다.
        그래서 **척추(usePlanDraft, 95줄)를 먼저** 뗐다. 그 뒤 입력이
        저장 셋 · 추가 다섯으로 줄었다.
  결합을 줄인 방법 둘:
    · openPlan 이 추가 바의 역본을 되돌리는 것 → **onOpened 콜백**으로 뺐다.
      순서표 열기와 추가 바 사이에 실제로 있는 유일한 결합이다.
    · 이름 칸 ref 는 저장 훅이 갖는다 — '이름 바꾸기를 열면 전체 선택' 규칙이
      nameBar 와 한 몸이다 (브라우저에서 0–7 전체 선택 확인).
  겪은 것:
    · usePlanAdd 호출을 배경 파일 목록 effect **앞**에 둬야 했다 (그 effect 가
      addKind 를 본다). tsc 가 '선언 전 사용' 으로 잡아 줬다.
    · 맨몸 `localStorage` 는 Node 자체의 실험적 전역이 jsdom 것을 가려서
      `--localstorage-file` 없이는 **undefined** 다. `window.sessionStorage` 로 부른다.
    · 변이 검증이 **내 검사의 빈틈**을 하나 찾았다 — insertItems 쪽 입력 비우기를
      지웠는데 아무 검사도 실패하지 않았다. 검사를 더했다.
    · **픽스처가 현실과 달라 검사가 틀린 것**도 하나. deletePlan 목이 실제로
      지우지 않아, 지운 뒤 reload 가 그 유형을 돌려주고 '첫 유형을 연다' 가 다시
      돌았다. 목을 실제로 지우게 고쳤다 — 유형이 남아 있으면 다른 것이 열리는 것이
      **맞는 동작**이라 그 경우도 검사로 나눴다.
  검증: tsc 0 · vitest 1,685개/101파일 · vite build · --coverage 통과 (src 8 → 20)
        변이 검증 25건 전부 잡힘.
        격리 서버 7831 에서 브라우저로 저장·추가 경로를 하나씩 —
        openPlan · patchItems(저장 안 됨) · saveCurrent(**API 로 DB 확인**) ·
        duplicateCurrent · 이름 바꾸기(전체 선택) · removeTemplate(초안까지 버림) ·
        성경/교독문/전례문 추가 · 종류 바꿀 때 포커스 · 송출까지. 콘솔 오류 0.
  다음 단계: **남은 1,378줄은 거의 전부 JSX 다.** 목록 블록(plan-single)이 스코프
        식별자 81개를 공유하는 문제는 그대로다 — 줄 수를 더 줄이는 일은 이제
        화면 쪼개기 문제이고, 프롭 껍데기가 되지 않게 **구조를 먼저 정해야** 한다.
- [완료 2026-09-07 / Agent C] R-4 PlanPanel 훅 분리 → 검사 (사용자 요청, 순서 지정)
  결과: 9f14df0(usePlanPreview) · 83f2eaf(버그 + lib 순수 함수) ·
        afa5079(usePlanSend + 죽은 import) · 23b92f7(검사 78개).
        PlanPanel.tsx **2,549 → 2,036줄**, 검사 1,500 → **1,595개**.
  왜 분리를 먼저 했나: 지금 검사를 씌우면 분리할 때 전부 다시 써야 한다.
  어떻게 자리를 골랐나: **결합을 먼저 실측했다** (섹션별 공유 식별자 수).
        전에 멈춘 이유가 "프롭 60개짜리 껍데기" 였으므로 가장 얇은 자리부터 갔다 —
        미리보기 입력 셋 · 송출 여덟.
  **분리 중에 실제 버그가 나왔다**: 기본 템플릿을 바꾼 직후 ▶ 를 누르면 이전 판이
        나갔다. sendItem 의 의존성에 plan 이 없는데 그 안에서 plan.defaults 를 읽었다.
        격리 서버 7830 에서 가린 실험으로 확정했다 — 항목을 옮겨 sendItem 을 다시
        만들면 맞게 나갔다(같은 버튼·같은 설정). sendTitle 에는 그 의존성이 이미
        들어 있었다(한쪽만 고쳐진 흔적). 값을 인자로 받게 바꿔 그럴 자리를 없앴다.
  헛돈 검사 하나를 잡았다: 템플릿 목록 catch 검사가 초기값이 이미 [] 라서
        실패 경로를 관측하지 못했다 (변이 검증에서 드러났다). 바꿨다.
  함께: 죽은 import 14개 제거. tsconfig 에 noUnusedLocals 가 없어 쌓인 것이다.
  검증: tsc 0 · vitest 1,595개/99파일 · vite build · --coverage 통과 (src 6 → 8) ·
        브라우저로 미리보기·송출·전체 올리기·자동 넘김 순환까지 직접 확인
  다음 단계: **아직 800줄이 아니다.** 남은 2,036줄 중 약 1,000줄이 JSX 다.
        남은 로직은 usePlanAdd(항목 추가) · usePlanStorage(읽기·저장)이고,
        목록 블록(plan-single)이 식별자 81개를 공유하는 문제는 그대로다 —
        그쪽은 프롭 껍데기가 되지 않게 다시 봐야 한다.
- [완료 2026-09-07 / Agent C] 점검 나머지 + 문서 전면 갱신 (사용자 요청)
  결과: 1325a1e(S-5·S-6·P-7·D-7) · 06a8967(감사 L-1·L-2) · 문서 커밋.
        **점검 14건과 보안 감사의 미결 항목이 모두 닫혔다.**
  한 것:
    · S-5 시도 기록에 상한(1,024) + 지난 것 걷기. **넣은 뒤에 걷는다** —
      먼저 걷으면 상한까지 줄인 다음 하나를 더해 +1 이 된다(검사에서 1,025)
    · S-6 창 열기를 출처 비교로. openExternal 도 http(s) 로 한정.
      함께 activate 이중 기동 · after-pack 의 void statSync 정리
    · P-7 server/version.ts — 판 번호 + **만든 시각**(빌드마다 바뀌는 값).
      따로 빌드 단계를 두지 않고 지금 돌고 있는 파일의 수정 시각을 쓴다
    · D-7 **실측으로 정했다** — Electron 44 가 도는 Node 24.20.0 · CI 24 → 하한 24
    · L-1 lib/deck-guard.ts — 크기·모양만. 내용은 안 본다(종류가 계속 늘고,
      출력 페이지가 모르는 종류를 안전하게 넘긴다). 개수만으로는 부족해 직렬화
      크기도 잰다 — 한 장에 큰 글자를 담으면 개수 검사를 지나간다
    · L-2 lib/rate-limit.ts 600회/분·주소별. **이 PC 는 세지 않는다.**
      훅 순서는 요청 수 제한 → 접속 암호 (인증 전에 폭주를 멈춘다)
  문서 (사용자 요청):
    · README — 설치판 절에 **태블릿 열기**와 **새 판을 받았을 때**(판 확인 방법),
      보안 절에 **출처 검사**(403 이 나는 주소와 SERMON_ALLOWED_ORIGINS),
      **포장 스크립트 표**(dist:mac 등 5개가 빠져 있었다 — §4.6 E 의 규칙 위반)
    · USER-GUIDE — 6장을 카드별 표로 다시 쓰고 6.1 데이터 이전(두 갈래) ·
      6.2 내 앱이 어느 판인가 신설. 7장에 되살리기 설명
    · TROUBLESHOOTING — 2.4.18(403) · 2.4.19(429) 신설. **0장 맨 위에** 세 줄
      (403 · 갑자기 닫힘 · 오늘 고친 것이 반영되지 않을 때)
    · PACKAGING — 번들에 담기는 것을 표로, **새 판을 나눠 줄 때**(갱신·판 확인) ·
      **앱이 죽으면 어떻게 되는가**(crash.log) 신설
    · CHANGELOG — 2026-09-07 항목(14건) · SECURITY-AUDIT '남은 것 → 없습니다'
    · CLAUDE.md — 양쪽을 함께 고칠 짝(isAllowedStyleKey) · 바깥 값은 형태부터 ·
      **설치판에서만 나타나는 함정** 세 줄을 코드 규칙에 더했다
  겪은 것:
    · CHANGELOG 에 1,441 이라 적었는데 실제는 1,440 이었다 — 커밋 전에 다시 재서
      바로잡았다. 이 저장소가 수치 어긋남으로 여러 번 헤맨 곳이다
    · 옛 기록(리뷰 문서의 '점검 시점 1,316개', 지난 CHANGELOG 항목)은 **그대로
      둔다** — 그때의 사실이다. 상태 문서(§2·§4.3)만 지금 값으로 맞춘다
  검증: tsc 0 · vitest 1,440개(92파일) · vite build · npm audit 0건 ·
        격리 서버로 deck 검사·요청 수 제한 실측 · **README ↔ package.json
        스크립트 대조 0개 남음** · docs 상대 링크 검사(깨진 것은 스킬이 만든
        diagram-design-profile.md 뿐 — 이 프로젝트 문서가 아니다)
  다음 단계: 커밋 · 푸시
- [완료 2026-09-07 / Agent C] 점검 남은 셋 — S-4 → P-5 → P-6 (사용자 승인)
  결과: 36fdae2(S-4) · 787df99(P-5) · cf6aadd(P-6). 보드 4.7 · 리뷰 · 감사 9장 갱신.
  한 것:
    · S-4 npm audit fix → **0건**. package.json 은 그대로(^ 안의 패치).
      fastify 5.11.3 → 5.12.3 이라 격리 서버(7816)로 기동·라우팅·WS·S-2 의 403 을
      다시 확인했다 — 프레임워크를 올린 것은 테스트만으로 넘기지 않는다
    · P-5 **정말 문제였다.** 사용자가 띄워 둔 설치 앱의 프로세스 환경을 읽으니
      PATH=/usr/bin:/bin:/usr/sbin:/sbin 뿐이었다. brew 는 /opt/homebrew/bin 에
      있는데 그 목록에 없어 which 가 실패한다 → lib/env-check 에
      toolCandidates·pickToolPath 를 두고, 설치도 절대 경로로 돌린다
    · P-6 react·react-dom 을 devDependencies 로. 앱 안 node_modules 20MB → 14MB ·
      DMG 131.7 → 124.4MB(arm64) · 138.5 → 130.8MB(x64)
  겪은 것:
    · P-5 를 /api/system/env 로 확인하려 했는데 **OBS·Chrome 이 둘 다 설치돼 있어
      command 칸이 아예 안 나온다**(checkEnv 는 없을 때만 채운다). 그래서 판정을
      lib 으로 떼어(pickToolPath) 좁힌 PATH 에서 직접 재고, 그 경로가 실제로
      실행되는 것까지 확인했다
  검증: tsc 0 · vitest 1,400개(점검 시점 1,316 → 신규 84) · vite build ·
        npm audit 0건 · **앱을 다시 만들어 띄웠다**(7818/7819 · 별도
        user-data-dir): react 빠진 것 확인 · 오늘 고친 것이 앱 안에 들어간 것 확인 ·
        좁은 PATH 로도 /api/system/env 200 · 애드혹 서명 검증 통과
  ⚠️ **release/ 의 DMG 는 이제 2026-09-07 빌드다.** 사용자가 설치한 것(09-05)은
     옛 판이므로 다시 설치해야 오늘 고친 것이 반영된다.
  건드리지 않은 것: 사용자의 설치 앱(7777) · data/
  다음 단계: 남은 것 S-5 · S-6 · P-7 · D-7 · 감사 L-1 잔여 · L-2 — 어느 것도
        나눠 주기를 막지 않는다
- [완료 2026-09-07 / Agent C] 점검 남은 셋 — S-3 → P-4 → P-3 (사용자 승인)
  결과: ac1088d(S-3) · 6a44e51(P-4) · 079d654(P-3). 보드 4.7 · 리뷰 · 감사 9장 갱신.
  한 것:
    · S-3 lib/template-css 의 isAllowedStyleKey 를 ws.ts 와 output.js **양쪽**에.
      먼저 정당한 키를 실측했다 — 프리셋 8종의 81개 중 `--` 아닌 것은 backdrop 뿐
    · P-4 main.cjs 가 최상위 오류를 잡아 crash.log 를 남기고 **DB 를 닫고**
      app.relaunch 한다. 판정은 electron/restart-policy.cjs (start.sh 와 같은 규칙).
      횟수는 --sermon-restart=N 으로 다음 실행에 넘긴다
    · P-3 번들 판 2 — readings(book·source 까지) · songbooks(사용자 것만) ·
      sheets(상태만, 그림은 폴더 복사). **곡집을 곡보다 먼저** 만든다
  겪은 것 셋:
    · P-4 의 restart-policy 를 .ts 로 두려다 말았다 — **크래시 경로에서 부르는
      코드는 가장 튼튼해야 한다.** 거기서 동적 import 가 실패하면 되살리기 자체가
      안 된다. .cjs 로 두고 .d.cts 로 타입만 붙였다
    · P-3 에서 곡집을 곡보다 나중에 만들면 수록이 조용히 'misc' 가 된다.
      순서를 뒤집어 보고 검사가 잡는 것을 확인했다
    · replace 여도 교독문을 지우면 안 된다 — 옛 판(v1) 번들에는 그 칸이 없어서
      그 번들을 받는 순간 213편이 사라진다. 자연 열쇠가 있어 덮어쓰기로 충분하다
  검증: tsc 0 · vitest 1,391개(점검 시점 1,316 → 신규 75) · vite build ·
        **서버 필터를 끄고 브라우저로 출력 페이지 방어막을 따로 확인**(S-3) ·
        **Electron 을 띄워 실제로 죽여 crash.log 두 줄과 --sermon-restart=2,
        wal 0바이트를 확인**(P-4) · **격리 서버 두 대로 실제 이전**(P-3)
  건드리지 않은 것: 사용자의 설치 앱(7777) · data/ · 시험용 임시 코드는 지웠다
  다음 단계: 남은 것은 🟡 뿐 — S-4(npm audit fix) → P-5(앱을 띄워 brew 탐색 확인) →
        P-6(react 를 devDependencies 로) 순으로 권한다
- [완료 2026-09-07 / Agent C] 점검 4건 조치 — S-1 → P-2 → S-2 → P-1 (사용자 승인)
  결과: 커밋 셋 — cc678e0(S-1·P-2) · 6c16906(S-2) · 8bed633(P-1).
        보드 4.7 · REVIEW-release-2026-09-07 · SECURITY-AUDIT 9장에 반영.
  한 것:
    · S-1 auth.ts 가 SECRET_SETTING_KEYS 를 내보내고 backup 이 제외한다.
      lan_open 도 함께 뺐다 (그 PC 의 보안 결정이라 남에게 옮기면 안 된다)
    · P-2 writeSections 가 **구간별** linesSource 를 받는다 (없으면 곡 단위 기본값).
      applyBundle 이 그것과 isFavorite·links 를 되살린다. 대응곡은 songIdMap 이
      다 채워진 뒤에 걸어야 한다
    · S-2 app.ts 첫 onRequest 훅 + lib/origin-check 의 isAllowedHost.
      루프백을 면제하지 않는다 — 공격이 오퍼레이터 PC 의 브라우저에서 온다
    · P-1 POST/DELETE /api/system/password · PUT /api/system/lan (루프백 전용) ·
      resolveHost(환경 변수 우선) · 설정 탭의 TabletSetup
  겪은 것 (둘 다 실제로 눌러 보고 알았다):
    · **lanOpen 하나로는 부족하다.** 켠 직후 화면이 '닫혀 있습니다 · [열기]' 로
      남아 눌렀는데 아무 일도 없는 것처럼 보이고 되돌릴 방법도 없었다 →
      lanWanted(다시 시작하면 열리나)를 따로 준다. 검사만 돌렸으면 못 찾았다
    · resolveHost 가 부를 때마다 process.env 를 읽으면 host-binding 테스트의
      helper(env 를 복원한 뒤 반환)와 어긋난다 → ENV_HOST 를 모듈 읽을 때 한 번
      잡아 HOST 와 같은 값을 보게 했다
  검증: tsc 0 · vitest 1,361개(점검 시점 1,316 → 신규 45) · vite build ·
        **고치기 전 코드로 되돌려 S-1·P-2 검사가 실패하는 것을 확인** ·
        격리 서버 7811 로 공격 5건 403 · 정당한 경로 10건 200 · WS 4경우 ·
        브라우저로 컨트롤 패널·출력 페이지·설정 탭 실측 · **LAN 을 켜고 다시 띄워
        실제로 태블릿 주소·QR 2장이 나오는 것까지 확인** (뒤에 격리 서버는 닫았다)
  건드리지 않은 것: 사용자의 7777 · data/ (읽기 전용으로만 열었다)
  다음 단계: 남은 것은 S-3(세 줄) → P-4 → P-3 → S-4 순으로 권한다 (보드 4.7)
- [완료 2026-09-07 / Agent C] 배포 직전 전면 점검 (사용자 요청)
  요청: "보안 문제, 배포를 통해 다른 사람들이 사용할 수 있는 stand alone 빌드에
  앞서 면밀하게 프로젝트와 각 파일들을 점검해줘."
  결과: [REVIEW-release-2026-09-07.md](REVIEW-release-2026-09-07.md) · 보드 4.7
        🔴 4건 · 🟠 3건 · 🟡 6건 · 문서 불일치 7건.
  **코드와 사용자 데이터는 바꾸지 않았다.** 문서 수치만 바로잡았다(§2·§4.1·§4.3·§5).
  실측으로 확인한 것 (격리 서버 7810 · 별도 데이터 폴더):
    · 번들에 lan_password·lan_session_secret 이 실리고, 그 열쇠로 만든 쿠키가 통했다
    · Origin: evil.example + text/plain POST → /api/songs/1/confirm 200,
      lines_source auto → manual (CLAUDE.md 가 지키라는 값이다)
    · Host: evil.example 로 GET /api/backup/export 200 (WS 는 같은 Host 로 401) ·
      같은 방법으로 mode:replace 가져오기가 성공해 곡 1 → 0
    · style:set 의 {opacity:0, display:none} 이 걸러지지 않고 출력 페이지로 갔다
    · 번들에 confirmed·isFavorite·linesSource 가 실려 있는데 가져오면 전부 auto,
      즐겨찾기 0곡
  검증: tsc 0 · vitest 1,316/81 · vite build · npm audit 2건.
  건드리지 않은 것: 사용자의 7777 서버(PID 20573) · data/ 는 readOnly 로만 열었다 ·
        POST /api/system/open-data-dir 는 사용자 화면에 창이 떠서 일부러 실행 안 함.
  다음 단계: 사용자가 고칠 순서를 정하면 4.7 의 S-1 → P-2 → S-2 → P-1 순으로.
- [완료 2026-09-07 / Agent C] 설치 안내가 받는 사람 눈에 안 띈다 (사용자 지적)
  '손상되었기 때문에 열 수 없습니다' 를 푸는 xattr 한 줄이 **PACKAGING.md 에만**
  있다. 그 문서는 머리말이 '배포판을 만드는 사람을 위한 것' 이라고 못 박고 있다.
  정작 이 오류를 만나는 사람은 **DMG 를 받은 봉사자**이고, 그 사람이 볼 곳은
  README(설치)와 TROUBLESHOOTING(무언가 안 될 때)이다 — 둘 다 비어 있다.
  CLAUDE.md 도 '무언가 안 될 때 → 먼저 TROUBLESHOOTING 을 본다' 고 적어 두었다.
  계획: ① TROUBLESHOOTING 에 증상 → 원인 → 조치 형식으로 넣는다 (그 문서의 형식)
        ② README 에 '설치판으로 쓰기' 절을 만든다 — 지금 README 는 개발자용
           설치(npm install · bible:build)만 있어서, 앱을 받은 사람이 볼 것이 없다
        ③ PACKAGING 에서는 그 두 곳을 가리키게 한다 (같은 말을 세 곳에 적지 않는다)
  한 것: ① TROUBLESHOOTING 2.4.16 신설 + **0장 맨 위에 한 줄** 걸었다
           (그 문서는 위에서부터 읽으니, 앱이 아예 안 열리는 사람은 0장에서 걸려야 한다)
        ② README 에 '설치판으로 쓰기' 절을 맨 앞에 만들었다 — 설치 → 격리 표시
           지우기 → 자료 넣기 → 번들 가져오기 → OBS 확인, 여섯 단계.
           기존 '설치와 실행' 에는 '여기부터는 소스에서 돌릴 때' 라고 못 박았다.
        ③ PACKAGING 에는 **'받는 사람에게 이 문서를 주지 마세요'** 와 두 링크만
           남겼다 — 같은 말을 세 곳에 적으면 한 곳만 고쳐 어긋난다.
        ④ CLAUDE.md 문서 표에 '설치 파일을 받은 사람이 물을 때' 줄을 더했다.
  남은 생각: 공증(Apple 개발자 계정)을 하면 이 안내 자체가 필요 없어진다.
- [완료 2026-09-05 / Agent C] 다른 맥에서 '손상되었기 때문에 열 수 없습니다' (사용자 보고)
  증상: 만든 맥에서는 잘 되는데, DMG 를 다른 맥에 옮기면 실행이 막힌다.
  실측 진단 (추측 아님):
    · codesign -dvv → `Identifier=Electron` · `adhoc, linker-signed`
      = **번들이 제대로 서명되지 않았다.** 링커가 실행 파일에 붙인 것뿐이다.
    · spctl -a -t exec → `code has no resources but signature indicates they
      must be present` = 서명이 깨졌다고 판정한다.
    · stapler → 공증 티켓 없음. 이 맥에 서명 인증서 0개.
    · 격리 표시(com.apple.quarantine)를 붙여 재현 → 같은 판정.
  왜 만든 맥에서는 되나: 내려받은 파일이 아니라 **격리 표시가 없다.**
  옮기면(에어드롭·클라우드·USB) 그 표시가 붙고 Gatekeeper 가 검사한다.
  서명이 깨져 있으면 macOS 는 '손상됐다' 고 말한다 — 실제로 깨진 것은 아니다.
  ⚠️ README 에 적어 둔 '오른쪽 클릭 → 열기' 는 **이 경우에 듣지 않는다.**
     그 방법은 '확인되지 않은 개발자' 일 때 쓰는 것이다. 내 문서가 틀렸다.
  계획: ① after-pack 에서 **번들을 제대로 애드혹 서명**한다 (안쪽부터 바깥으로).
           그러면 '손상' 이 아니라 '확인할 수 없음' 이 되어 설정에서 열 수 있다
        ② 서명이 유효한지 빌드 중에 검사하고, 아니면 빌드를 실패시킨다
        ③ 받는 사람 안내를 실제로 듣는 방법으로 고친다 (xattr 로 격리 표시 제거)
        ④ 진짜 해법(공증)은 Apple 개발자 계정이 필요하다는 것을 적는다
  조치: after-pack 이 **깊은 곳부터 바깥으로** 애드혹 서명한다.
        `--deep` 은 쓰지 않는다(Electron 에서 깨진다). 순서를 지키지 않으면
        `In subcomponent: .../chrome_crashpad_handler` 로 실패한다 —
        실제로 x64 빌드가 그렇게 막혔고, 그래서 라이브러리 → 헬퍼 실행 파일 →
        안쪽 번들(깊은 것부터) → 앱 순으로 고쳤다.
        서명 뒤 `codesign --verify --strict --deep` 으로 검사하고 실패하면
        **빌드를 실패시킨다** — 만든 사람은 격리 표시가 없어 끝까지 모른다.
  검증(실측):
    · 전: Identifier=Electron · linker-signed · spctl '…no resources…'(= 손상)
    · 후: Identifier=org.jiwumission.sermon-presentation · adhoc(hashes 3+3)
          spctl 은 여전히 rejected(공증 안 했으니 당연) — 하지만 이유가 다르다.
          '손상됨' 이 아니라 '확인할 수 없음' 이라 설정에서 열 수 있다.
    · 격리 표시를 붙였다 지우는 것까지 재현했고, 서명한 앱이 잘 뜨는 것도 확인
      (사용자 앱이 7777 을 쓰고 있어 --user-data-dir 을 따로 줘서 시험했다 —
       처음에는 단일 인스턴스 잠금 때문에 조용히 종료돼 '서명이 깨졌다' 고
       오해할 뻔했다).
  문서: docs/PACKAGING.md 의 '오른쪽 클릭 → 열기' 를 지웠다. **그 방법은 이
        경우에 듣지 않는다.** 확실한 방법(`xattr -dr com.apple.quarantine`)과
        근본 해법(공증)을 적었다.
- [완료 2026-09-05 / Agent C] 포장한 앱이 성경 DB 를 엉뚱한 곳에서 찾는다 (사용자 보고)
  증상: 사용자가 data/ 를 앱 데이터 폴더에 그대로 복사했는데도
        '성경 DB 가 없습니다. 터미널에서 npm run bible:build ...' 가 뜬다.
  실측: 앱 데이터 폴더에 bible.sqlite 107MB 가 **분명히 있다.**
  원인: 내가 electron/main.cjs 에 넣은 한 줄.
        SERMON_BIBLE_DB = APP_ROOT/data/bible.sqlite → **앱 번들 안**을 가리킨다.
        거기엔 data/ 가 없다(저작권 자료라 일부러 안 넣는다).
        paths.ts 의 기본값(DATA_DIR/bible.sqlite)이 이미 맞았는데 덮어썼다.
  함께 고칠 것: 안내 문구가 'npm run bible:build' 를 시킨다 — 포장한 앱을 쓰는
        사람에게는 터미널도 저장소도 없다. 무엇을 어디에 넣으라고 말해야 한다.
  계획: ① main.cjs 의 덮어쓰기를 지운다 (기본값이 맞다)
        ② 포장 여부를 서버에 알려(SERMON_PACKAGED) 안내 문구를 갈라 쓴다
        ③ 격리 서버로 두 경우를 다 확인하고, 앱을 다시 빌드해 실제로 성경이 뜨는지 본다
  조치: ① main.cjs 의 SERMON_BIBLE_DB 덮어쓰기를 지웠다 (paths.ts 기본값이 맞다)
        ② main.cjs 가 SERMON_PACKAGED=1 을 남기고, lib/bible-missing.ts 가
           그것을 보고 안내를 갈라 쓴다. 문구가 서버 로그·조작 화면·설정 탭
           **세 군데**에 흩어져 있어 한 곳으로 모았다 — 한 곳만 고치면 나머지가
           옛말을 한다. /api/info 에 bibleDb·packaged 를 실어 화면도 쓸 수 있게 했다.
        ③ 찾은 자리를 함께 보여 준다 — 어디를 봤는지 모르면 엉뚱한 곳에 넣고 헤맨다.
  검증: tsc · vitest 1,316개(신규 7) · vite build.
        **앱을 다시 빌드해 사용자 데이터 폴더 그대로 띄웠다** —
        성경 준비 True · 자리 …/Application Support/sermon-presentation/data/bible.sqlite ·
        역본 12개 · 요 3:16 조회됨 · 곡 4,531 · 순서표 7.
        아이콘도 이 빌드에 들어 있다(해시 일치) — 사용자가 설치한 것은 아이콘 넣기
        전 빌드라 기본 아이콘이 보였을 것이다.
- [완료 2026-09-05 / Agent C] 앱 아이콘 (사용자 요청)
  지금은 Electron 기본 아이콘이라 독·작업표시줄에서 다른 Electron 앱과 구별되지 않는다.
  근거로 삼을 것: 프로젝터 PWA 아이콘(public/projector/icon-512.png)이 이미 있다 —
  **어두운 바탕에 가운데 정렬된 흰 줄 세 개**. 화면에 나가는 가사 모양 그대로다.
  그 정체성을 이어받아 앱 아이콘을 만든다 (둘이 따로 놀면 같은 앱으로 안 보인다).
  계획: ① 1024px 원본을 스크립트로 그린다 (손으로 그린 PNG 를 두면 고칠 수가 없다)
        ② 맥 규격에 맞춘다 — 아이콘은 스스로 둥근 사각형을 그려야 한다(맥이 깎아
           주지 않는다). 여백 약 10%, 모서리 반경은 Apple 규격 비율
        ③ build/icon.icns(맥) · build/icon.ico(윈도우) 로 뽑는다.
           electron-builder 의 buildResources 가 build/ 라 이름만 맞으면 집어 간다
        ④ 작은 크기(16·32px)에서 읽히는지 실제로 축소해 눈으로 본다
  한 것: build-resources/icon.svg(원본) · icon-small.svg(작은 크기용) ·
        scripts/build-icons.ts(`npm run icons`) → icon.icns · icon.ico ·
        프로젝터 PWA 아이콘 두 개까지 한 번에.
  겪은 것 둘 (둘 다 축소해 눈으로 보고 알았다):
    · **16px 에서 줄 셋이 한 덩어리로 뭉쳤다.** 1픽셀 = 64단위인데 틈이 40단위라
      0.6px 이었다. 16px 은 맥의 목록·메뉴와 윈도우 작업표시줄이 쓰는, 사람이
      가장 자주 보는 크기다 → 그 크기만 **줄 두 개짜리 변형**을 쓴다.
      큰 그림도 틈을 40 → 58 로 넓혔다(32px 에서 갈라져 보이게).
    · `.gitignore` 가 `build/` 를 산출물로 보고 무시한다. 아이콘은 **저장소에
      있어야** CI(윈도우 빌드)가 쓴다 → 폴더를 `build-resources/` 로 나눴다.
  함께 정리: scripts/make-projector-icons.ts 를 지웠다. 같은 두 파일
        (public/projector/icon-{192,512}.png)을 새 스크립트도 만들게 되어
        **원본이 둘**이 됐다 — 옛것을 돌리면 프로젝터 아이콘만 옛 모양으로
        되돌아가 앱 아이콘과 어긋난다.
  검증: tsc · vitest 1,309개 · vite build · **맥 앱을 다시 빌드해 확인** —
        'default Electron icon is used' 경고가 사라졌고, 앱 안 icon.icns 의
        해시가 원본과 같다. 밝은 바탕·어두운 바탕 양쪽에서 눈으로 봤다.
- [완료 2026-09-05 / Agent C] Phase 6 — 맥·윈도우 배포 (사용자 요청)
  사용자 결정 셋: ① 윈도우는 GitHub Actions 로 빌드 (이 맥에 Wine 이 없다)
                  ② 사전 설치는 '점검 + 명령 한 줄' (자동 설치 아님, 확인받고 실행)
                  ③ '데이터 폴더 바로가기' 는 설정 탭
  먼저 푼 것: **Electron 이 .ts 를 읽을 수 있는가** — 기댈 필요가 없다.
        tsc 의 `rewriteRelativeImportExtensions` 로 `./app.ts` → `./app.js` 까지
        바꿔 그냥 JS 로 뽑힌다(실측 확인). 로더 마법 없이 어느 Node 에서나 돈다.
        개발은 지금처럼 .ts 직접 실행 그대로 두고, **포장할 때만** 빌드한다.
  순서(각 단계마다 커밋):
        ① 설정 탭 '데이터 폴더 열기' — 라우트 + 단추. Electron 이 없어도 동작해야 한다
        ② 환경 점검 카드 — OBS·폰트 확인, 빠진 것은 명령 한 줄 (실행 전 확인)
        ③ tsconfig.build.json + electron/main + electron-builder + npm 스크립트
        ④ .github/workflows — 맥·윈도우 동시 빌드
  ①② 완료 (커밋함): server/routes/system.ts · lib/env-check.ts ·
        EnvSetupCard.tsx. 설치 명령은 **흰 목록과 글자까지 같을 때만** 돈다 —
        이 앱은 LAN 에도 열리므로 화면이 보낸 문자열을 그대로 실행하면 구멍이 된다.
        막는 자리를 둘로 뒀다(LAN 암호 미들웨어 + 라우트 자신의 루프백 판정).
  ③④ 완료: tsconfig.build.json · electron/main.cjs · electron/after-pack.cjs ·
        electron-builder.yml · .github/workflows/build.yml · docs/PACKAGING.md
  **맥 .dmg 두 개를 실제로 만들고 실행해 확인했다** (arm64 126MB · x64 130MB).
        포장한 앱이 7778 에 서버를 띄우고 조작·출력·프로젝터·강사 화면이 모두 200.
        데이터는 ~/Library/Application Support/sermon-presentation/data.
  겪은 함정 셋 (전부 docs/PACKAGING.md 에 적었다):
    · **한글 앱 이름** — productName 이 Helper 앱 이름이 되는데, Electron 이
      한글 이름의 Helper 를 못 찾아 'Unable to find helper app' 으로 죽는다.
      겉으로는 창도 로그도 없이 종료 코드 133(SIGTRAP) 만 남아 원인을 찾기
      어려웠다. 최소 코드로도 죽는 것을 보고 번들 문제로 좁혔다.
      → productName 은 영문, 보이는 이름은 CFBundleDisplayName.
      **CFBundleName 은 덮어쓰면 안 된다** (그것도 Helper 를 찾는 데 쓰인다).
    · asar:false 인데 Info.plist 에 없는 asar 의 무결성 항목이 남아 죽는다
      → after-pack 에서 지운다. 안 지워지면 빌드를 실패시킨다.
    · 포장하면 public/ 이 dist-server/ 옆에 없어 조작 화면이 404
      → SERMON_APP_ROOT 로 알려 준다.
  윈도우: GitHub Actions 로 만든다 (이 맥에 Wine 이 없다). Actions 탭에서
        '설치 파일 빌드' 를 손으로 돌리거나 v* 태그를 밀면 맥·윈도우가 함께 나온다.
  남은 것: 아이콘이 없다(Electron 기본). 맥 서명·공증도 안 했다(개발자 계정 필요) —
        받는 사람은 격리 표시를 지워야 한다 (2026-09-05 에 애드혹 서명을 더했다).
- [완료 2026-09-05 / Agent C] 데이터 이전이 예배 순서의 곡 연결을 끊는다 (실측 재현)
  '집에서 준비하고 교회에서 실행하려면' 질문을 확인하다 나왔다.
  재현: 집 서버에 곡 3개 → 2번 삭제(id 에 구멍) → '셋째곡'(id 3) 을 담은 순서 작성
        → 내보내기 → 교회 서버에 replace 로 가져오기
        → 곡이 id 1,2 로 **새로 매겨지는데** 순서 항목은 여전히 songId 3
        → 그 id 는 없다. 예배 중에 '곡을 찾을 수 없습니다' 를 만난다.
  merge 도 못 쓴다: createSong 을 무조건 불러서 같은 번들을 두 번 넣으면
        곡이 통째로 복제된다 (중복 검사가 없다).
  계획: ① 가져오기가 '번들의 옛 id → 새 id' 표를 들고 순서 항목의 songId 를 바꾼다
           (양쪽이 같은 번들 안에 있으므로 추측할 것이 없다)
        ② merge 는 이미 있는 곡을 건너뛰고 **그 곡의 id 로 이어 준다**
           짝 기준: (곡집, 번호) + 제목. 번호가 없으면 제목만.
           같은 번호를 다른 곡이 쓰는 경우가 실제로 35건 있어 번호만으로는 안 된다
        ③ 못 이은 항목은 조용히 넘기지 말고 결과에 적는다
  조치: 계획 그대로. lib/song-identity.ts 신설(순수 함수라 검사할 수 있다).
  같은 시험을 다시: 교회 PC 에서 곡이 id 1,2 로 매겨지고 **순서 항목도 songId 1**
        → 열어 보면 '셋째곡'. 합치기를 두 번 더 해도 곡 수는 2 그대로.
  검증: tsc · vitest 1,290개(신규 19) · vite build.
- [완료 2026-09-04 / Agent C] 종료할 때 songs.sqlite 를 닫지 않는다 (실측으로 발견)
  '데이터 폴더를 드랍박스에 두면 되지 않느냐' 는 질문을 확인하다 나왔다.
  실측: 격리 서버에 곡 하나 만들고 SIGINT → app.sqlite 는 32KB 로 정리되는데
        songs.sqlite 는 **4KB + WAL 234KB** 가 남는다. 그 파일만 복사해 열면
        `no such table: songs` — **빈 DB** 다.
  원인: server/index.ts 의 종료 처리가 closeBibleDb·closeAppDb 만 부른다.
        closeSongsDb 는 만들어 두고 아무도 안 부른다.
  왜 중요한가: 평소에는 탈이 없다(다음에 열 때 SQLite 가 WAL 을 읽는다). 하지만
        · 파일을 복사·동기화하면 빈 DB 가 따라간다 (CLAUDE.md 가 경고하는 그 함정)
        · 클라우드 동기화는 세 파일을 따로 올려 더 위험하다
  조치: server/index.ts 종료 처리에 closeSongsDb() 를 넣었다.
  고친 뒤 같은 시험: songs.sqlite **98KB · -wal/-shm 없음** · 한 파일만 복사해
        열어도 곡 1개가 보인다.
  검증: tsc · vitest 1,271개 · vite build.

- [완료 2026-09-04 / Agent C] 악보를 조작 화면에서 켜고 끈다 (사용자 요청)
  사용자 보고 셋:
    ① 악보가 아직 불완전하다 → **기본을 가사로** 바꿔야 한다 (지금은 악보가 기본)
    ② 프로젝터에서 S 를 누르는 것이 번거롭다 → **찬양 항목 설정에 토글**로
    ③ **S 로 가사로 바꾼 뒤 악보로 돌아가는 길을 못 찾았다**
  ③ 이 내 잘못이다. S 는 키보드에만 있고 막대에도 안내문에도 없다 —
  보이지 않는 스위치를 만들어 놓고 문서에만 적었다. 게다가 localStorage 에
  남아서 **다음 주에도 꺼진 채**다.
  구조상 알아 둘 것: 프로젝터는 `deck` 을 받지 않는다. `state.slide` 한 장만
  본다 — 그래서 '악보로 낼지' 는 **슬라이드에 실려야** 한다. 서버가 붙일지
  말지로 정하면 된다.
  계획: ① 덱 라우트가 `?sheet=1` 일 때만 악보를 붙인다 (기본 = 안 붙임 = 가사)
        ② PlanItem(song) 에 `sheet?: boolean` · normalizeItems 흰 목록에 추가
        ③ 예배순서 항목 설정과 찬양 탭 곡 카드에 '가사 | 악보' 토글
           (악보가 없는 곡이면 그렇다고 알려 준다)
        ④ 프로젝터: localStorage 저장을 **없앤다**(조작 화면이 주인이다) ·
           막대에 보이는 단추를 넣고 안내문에 S 를 적는다
  한 것: 계획 그대로. 더한 것 하나 — 프로젝터 막대 단추의 **글자는 '지금 나가는
        것'** 이다. 악보가 안 실려 온 화면에서 '악보' 라고 적혀 있으면 악보가
        나가는 줄 알고 벽을 다시 쳐다보게 된다.
        토글은 ProjectorSheetToggle.tsx 한 곳에 두고 두 화면이 같이 쓴다.
        곡을 새로 열면 찬양 탭의 선택이 꺼진다 — 앞 곡에서 켠 것이 남으면 검토가
        안 된 다음 곡의 악보가 예고 없이 벽에 걸린다.
  검증: tsc · vitest 1,271개(신규 2) · vite build. 격리 서버 7816(사용자 DB 사본 ·
        악보 폴더는 심볼릭 링크로 읽기만) 에서 프로젝터 창을 함께 띄우고 실측:
        · 기본 송출 → 프로젝터에 **가사** · 단추 흐림 + '조작 화면에서 켭니다'
        · 찬양 탭에서 '악보' → 송출 → 0007.webp · 단추 '악보'(파랑)
        · 단추를 눌러 가사 → 다시 눌러 악보 (**되돌아온다**)
        · 새로고침 → 조작 화면 설정으로 복귀 · localStorage 값 없음
        · 예배순서 찬양 항목의 '가사 | 악보' 로도 양방향 확인
  남은 것: 예배 순서 '기본 설정' 에는 넣지 않았다. 항목마다 켜는 것이 지금 요구다 —
        전부 켜고 싶어지면 그때 기본값을 둔다.

- [완료 2026-09-04 / Agent C] 열 너비를 마우스로 조절 (사용자 요청)
  세 곳: .body(본문↔오른쪽 열, **모든 탭이 함께 쓴다**) · .song-split(찬양) ·
  .review-split(검토). 예배순서는 한 열이라 나눌 것이 없다.
  한 것: 잡이를 격자의 **가운데 칸**으로 넣었다(겹쳐 띄우면 밑의 단추를 가린다).
        보이는 폭 6px + ::before 로 잡는 폭만 ±5px 넓힘. gap 16 → 5+6+5 로 간격 유지.
        lib/column-split.ts 에 셈만 떼어 검사한다(src/ 는 테스트가 없다).
        localStorage(useTheme 와 같은 이유 — 이 PC 의 취향이다).
        되돌리기 두 길: 더블클릭 · Home. 키보드 ←→ 16px. 태블릿용 touch-action:none.
  겪은 것 셋 (셋 다 화면에서 실제로 눌러 보고 알았다):
    · CSS 변수가 자식에게 물려 내려간다 — 이름이 --split 하나면 껍데기가 정한
      오른쪽 열 너비가 탭 안쪽 격자까지 흘러가 아무도 안 끈 열이 넓어졌다.
      → --split-side / --split-song / --split-review 로 나눴다.
    · `.body` 는 줄 안쪽 칸에도 쓰이는 이름이다(.cue-row .body 등). 지금은
      display:flex 가 덮어 무사하지만 변수가 흘러가므로 `.app > .body` 로 못 박았다.
    · 화살표를 연달아 누르면 **한 칸만 움직였다.** 처리기가 useState 값(다음 그리기)과
      화면 폭(아직 옛 값)을 읽어서다 → ref 를 진짜 값으로 두고 state 는 그리기 전용.
  검증: tsc · vitest 1,250개(신규 9) · vite build. 격리 서버 7811 에서 실측 —
  끌기·저장·새로고침 유지·더블클릭 되돌리기·실제 키 입력 확인.
  경계 실측(검토 탭, 창 1440): 왼쪽 끝 240px(=min) · 오른쪽 끝 544px(이웃 320 남김) ·
  Home 누르면 CSS 기본값(385/462)으로 복귀. 900px 아래에서는 한 열 + 잡이 숨김.
  다크 모드 확인(잡이가 파랗게 밝아진다).

- [완료 2026-09-04 / Agent C] D-6 ⑥ 악보 검토 화면
  오선이 5줄로 잡히지 않은 **155장**(2,061장 중 7.5%)을 사람이 볼 자리가 없다.
  실측 표본: 5번·15번은 마지막 단이 4줄, 21번은 6줄 — 대개 마지막 단이다.
  중요한 것: `needs_review` 는 '틀렸다' 가 아니라 **'봐야 한다'** 는 신호다.
  대부분은 멀쩡할 것이므로, 화면이 할 일은 고치기보다 **빠르게 넘기는 것**이다.
  계획: ① song_sheets.review_state (NULL 안 봄 | 'ok' | 'bad').
           ⑦ 과 같은 이유로 putSheet 이 건드리지 않는다 — 다시 검출해도 사람의
           판정이 날아가면 안 된다
        ② GET /api/sheets/review (목록) · PUT /api/sheets/:곡집/:번호/review
        ③ 검토 탭에 '가사 줄나눔 | 악보' 전환. ReviewPanel 이 460줄이라
           SheetReview.tsx 로 따로 만든다 (한 파일 800줄 넘기지 않는다)
        ④ **악보 그림 위에 잡힌 단 경계를 겹쳐 그린다** — 이게 핵심이다.
           눈으로 보지 않으면 맞는지 알 수 없다. 이상한 단은 다르게 칠한다
        ⑤ 손버릇을 가사 검토와 맞춘다: Enter 괜찮다+다음 · ↑↓ 이동
  한 것: 계획 그대로. 검토 탭 위에 '가사 줄나눔 | 악보 단 경계' 전환을 두고
        SheetReview.tsx(새 파일)로 뺐다. SheetSystem·SheetReviewState 는 화면까지
        건너가므로 server/db 가 아니라 shared/types.ts 로 옮겼다 (src → server 를
        직접 import 하는 곳이 이 프로젝트에 하나도 없었다 — 그 경계를 지켰다).
        titlesByEntry() 신설 — findByEntry 를 155번 부르면 곡마다 가사·수록까지
        통째로 읽는데, 목록에 필요한 건 제목 한 줄뿐이다.
  실측으로 확인한 것: **표시된 장이 대개 멀쩡하다는 짐작이 맞았다.**
        5번(6단 중 6번째가 4줄)을 화면에서 보니 빨간 점선 칸이 마지막 오선과
        가사 두 줄을 정확히 감싸고 있다 — 사람이 볼 것은 '괜찮음' 한 번이다.
  겪은 것: 판정을 눌러도 **진행 막대가 0 에 머물렀다.** 목록 줄만 고치고 숫자를
        그대로 뒀기 때문이다. 155장을 훑는 내내 일이 쌓이는 것이 안 보인다 →
        counts.reviewed 를 함께 올리고 내린다(취소하면 줄어든다).
  검증: tsc · vitest 1,269개(신규 19: 저장소 8 · 라우트 11) · vite build.
        격리 서버 7815 — **사용자 DB 를 VACUUM INTO 로 사본 뜨고 악보 폴더는
        심볼릭 링크로 읽기만** 했다. 실제 155장 목록·그림 겹치기·Enter·↑↓·
        다시 봐야 함·판정 취소·진행 막대 전부 확인.
        사용자 DB 는 그대로다 (review_state 칸이 아직 없다 — 다음 서버 시작 때 붙는다).
  남은 것: 경계를 **끌어서 고치는** 길은 없다. 지금은 '다시 봐야 함' 표시까지다.
        먼저 155장을 훑어 **정말 틀린 것이 몇 장인지** 세어 보고 정하는 것이 맞다.

- [완료 2026-09-04 / Agent C] D-6 ⑦ 악보 모양을 사람이 고치는 길
  짐작이 틀린 곡(흔들리는 536곡)을 고칠 방법이 없었다 — shared/sequential 을
  attachSheets 가 인자로 받기만 하고 저장할 자리가 없었다.
  한 것: ① song_sheets.layout (NULL = 자동 짐작) + addLayoutColumn 이주.
           **putSheet 가 layout 을 건드리지 않는다** — 검출을 다시 돌려도 사람이
           고른 것이 조용히 날아가면 안 된다 (`Omit<SheetRow,'layout'>` 로 막았다)
         ② PUT /api/sheets/:곡집/:번호/layout — 'shared'·'sequential'·null(자동)
         ③ 요약·덱 **양쪽이** `sheet.layout ?? guessLayout(...)` 을 쓴다.
           한쪽만 고치면 화면은 '겹쳐' 인데 나가는 것은 '이어' 가 된다
         ④ 곡 카드 악보 줄에 '자동 | 겹쳐 | 이어' + 짐작일 때 '(짐작)' 표시
  **송출을 다시 하지 않는다** — 지금 뜬 덱을 다시 보내면 첫 슬라이드로 튄다.
  3절 부르다 1절로 돌아가는 것보다 '다음 송출부터 반영됩니다' 가 안전하다.
  검증: tsc·vitest(1,241 / 신규 17)·vite build. 격리 서버 7810 에서 눌러 확인 —
  '자동'(절이 이어 적힘(짐작)) → '겹쳐' 누르니 활성이 옮겨 가고 안내 문구가 뜬다.
  덱 실측(lines=1): 이어 = ♪1 2 3 4 5 6 · 겹쳐 = ♪1 3 5 1 3 5 로 **실제로 달라진다.**
  알게 된 것: 한 슬라이드에 여러 줄이 들어가면(2줄씩·섹션 전체) 그 섹션의 **첫 단만**
  나간다. 여러 단에 걸친 슬라이드는 그 범위를 통째로 잘라야 맞다 — 아래 남은 일에 적었다.

- [완료 2026-09-04 / Agent C] D-6 ⑤ 조작 화면에 악보 상태 표시
  SheetRef 에 system·systemCount · isUncertain 을 lib 로 빼서 **붙일 때와 알릴 때가
  같은 것을 쓴다** · GET /api/songs/:id 가 악보 요약을 함께 준다 (곡을 여는 순간
  알아야 한다 — 덱을 기다리면 송출하고 나서야 안다) · 찬양 탭 곡 카드에 '악보 …단'
  줄과 경고 두 종류(배분·검출은 다른 문제라 따로 적는다) · 슬라이드 목록에 ♪n
  (흔들리면 ♪n?) — **배분이 맞는지 눈으로 확인하는 유일한 자리다.**
  실측 확인(7821): 찬7 맞는 곡(♪1 ♪1 ♪3) · 찬5 흔들리는 곡(경고 둘 + ♪1? ♪4?) ·
  새305 악보 없음. 다크 모드 확인.
  남은 것: ⑥ 검토 155장을 볼 화면 · ⑦ shared/sequential 을 사람이 고치는 길

- [완료 2026-09-04 / Agent C] D-6 악보 — 1단계: 이미지 변환
  실측으로 확인한 것 (원본은 읽기만 했다):
    · `~/Desktop/Data/Praise/찬미예수 2000 악보` = BMP **2,062장** + Thumbs.db
      이름은 `0001.bmp`~`1888.bmp` 와 `(1889).bmp`~`(2062).bmp` 두 형식인데
      **번호가 이어지는 한 벌**이다 (겹침 0 · 빠짐 0).
    · 찬미예수 2000 수록 2,062곡 ↔ 악보 2,062장 **1:1 완전 매칭** (양쪽 다 0개 남음).
      ⚠️ 다른 곡집도 '100%' 로 보이는 것은 **착시**다 — BMP 번호가 1~2062 라
      그 범위에 드는 곡집은 전부 겹친다. 이 악보는 찬미예수 2000 것뿐이다.
    · 8비트 회색조 · 폭 ~1000px · 높이 949~2245px · 합계 340MB.
    · 한 장에 단(staff system) 4~10개. **한 단에 1·2절 가사가 겹쳐 적힌 것도 있다**
      (1000번 실측) — 슬라이드↔단 매칭에서 이걸 다뤄야 한다.
  계획: ① BMP → WebP 변환 (data/sheets/, 원본은 건드리지 않는다)
        ② 단 경계 검출 → DB  ③ 프로젝터 레이어로 송출 (강사 모니터는 가사 유지)
  ① 완료 (커밋함) — npm run sheets:convert. 2,061장 · 340MB → 52MB · 픽셀 동일.
     ImageMagick 무손실. 손실(q90)은 오히려 6배 컸다 — 선화라 그렇다.
     원본 1255.bmp 가 잘려 있었다(128KB 에서 끊김) → KNOWN-DATA-ISSUES 4.7.
  ② 완료 (커밋함) — lib/staff-detect.ts. 전수 2,061장 중 **1,906장(92.5%) 완벽**,
     단을 하나도 못 찾은 장 0. 나머지 155장(7.5%)은 어느 단의 줄 수가 5가 아니라
     사람이 봐야 한다.
     막힌 곳 셋을 실측으로 뚫었다:
       · '검은 픽셀이 많은 행' → 실패(스캔 기울기). **행별 최장 연속 구간**으로 바꿈.
       · 그래도 많이 기운 장은 실패(0301: 7단인데 2줄) → **변환 때 -deskew 로 펴서 저장.**
         검출할 때만 펴면 좌표가 화면 그림과 어긋난다. 편 뒤 -threshold 로 흑백 복귀
         (안 하면 무손실 용량 7배).
       · 자를 범위를 비율로 잡으니 **가사 두 줄짜리가 잘렸다**. 간격도 안 됨
         (오선↔가사 8px > 가사↔다음코드 7px). **띠 높이**로 코드/가사를 가른다.
       · 볼타 괄호(1.·2. 반복표시) 가로선이 오선으로 잡혀 '1줄짜리 단' 이 생겼다
         → 3줄 미만 무리는 버린다.
  ③ 완료 (커밋함) — server/db/sheets.ts + npm run sheets:detect.
     song_sheets 표에 2,061장 전부. (곡집, 번호)로 묶는다 — 곡 id 로 묶으면 곡을
     지웠다 다시 넣을 때 그림은 그대로인데 연결만 끊긴다.
     단 목록은 JSON 한 칸 (늘 통째로 읽는다. service_plans.items 와 같은 이유).
     깨진 JSON 은 그 장을 통째로 버린다 — 반쪽 목록으로 자르면 엉뚱한 자리가 나간다.
     사람이 봐야 하는 155장은 needs_review 로 표시해 뒀다.
  ④ 진행 중 — 슬라이드↔단 매칭. 사용자 결정: **A(절 안에서 비례 배분)**.
     넘어야 할 산: 한 단에 1·2절 가사가 겹쳐 적힌 악보가 흔한데 DB 는 절을 따로
     갖고 있다 → 1절 슬라이드와 2절 슬라이드가 같은 단을 가리켜야 한다.
     ④-1 완료 (커밋함) — lib/sheet-match.ts + 테스트 19개.
       모양을 짐작(shared/sequential) → 섹션마다 단 범위 → 섹션 안에서 비례 배분.
       **실측한 자료 구조** (찬미예수 2000 2,061곡):
         · 섹션 1개 957곡(46%) · 여러 섹션 1,104곡
         · 여러 섹션 중 겹쳐 적힘 595(53.9%) · 이어 적힘 239(21.6%) ·
           **어느 쪽도 아님 270(24.5%)**
       **믿을 수 있는 정도: 1,525곡(74.0%)** — 줄 수와 단 수가 맞는 곡.
       나머지 536곡(26.0%)은 배분이 흔들릴 수 있다 → 화면이 알려야 한다.
       눈으로 확인: 7번(겹침)·11번(이어짐) 정확. 1000번은 어긋나는데 **DB 쪽 문제** —
       1·2절이 한 섹션 16줄로 합쳐져 있어 절 경계를 알 수 없다.
     ④-2 진행 중 — 서버 라우트 + 프로젝터 송출.
       부품 확인: image 슬라이드의 crop(0~100%)을 output.js 가 **이미 그릴 줄 안다**
       (2026-08-30 에 만들어 둔 것). 새로 그릴 것은 없고 이어 붙이기만 하면 된다.
       완료 (커밋함). 만든 것: /sheets/ 정적 라우트 · 곡 덱에 sheet 붙이기
       (lib/sheet-attach.ts) · output.js 의 SermonSlideFilter 문 · 프로젝터가 S 로 전환.
       **서버가 아니라 보는 쪽이 고른다** — 서버가 화면마다 다른 슬라이드를 보내면
       덱이 갈라져 진행 위치가 어긋난다. 한 슬라이드가 가사와 악보를 함께 들고 다닌다.
       ⚠️ 잡은 것 — **crop 렌더링이 한 번도 동작한 적이 없었다.** §4.6 에서 'output.js 가
       자를 줄 안다' 고 적었는데 그건 코드를 읽은 것이지 그려 본 것이 아니었다. 실제로는
       ① 상자 높이가 0 이라 overflow:hidden 이 다 가렸고(새까만 화면)
       ② 창의 가로세로 비를 안 맞춰 5:1 짜리 단이 세로로 늘어났고
       ③ .image-slide img.fit-contain 의 max-height 가 우선순위에서 이겨 눌렀다.
       셋 다 고쳤다. 교훈: 코드를 읽고 '된다' 고 판정하지 말 것.

직전 작업 7: §4.6 C·E 부채 정리 (2026-09-04 완료, Agent C).
  지운 것: t:'show' + state.show() · t:'state:patch' + stage.js 처리기 ·
           buildSongSlides · 어긋난 주석 하나.
  옮긴 것: 블랙 유지 규칙(show 주석 → state.ts '조작' 절 머리말).
  고친 것: 테스트 11곳이 state.show() 로 만들던 '묶음 없는 한 장' 을
           비우기→복구(실제 도달 경로)로 바꿨다.
  README 에 빠져 있던 스크립트 11개를 넣었다.
  검증: tsc · vitest 1,149 · vite build · 격리 서버(7818)에서 강사 모니터·출력 페이지가
        그대로 도는 것 확인(state:patch 처리기를 뗀 자리다). 자세히는 CHANGELOG.

직전 작업 6: §4.6 U-4 곡 수록 정보 편집 (2026-09-03 완료, Agent C).
  찬양 탭 곡 카드 '수록' 줄 + '고치기'. 통합 테스트 7개.
  덤: SongPanel 이 1,120줄이 되어(상한 800) 수록·대응곡 줄을
      src/control/components/SongMetaRows.tsx 로 떼어 냈다 → 859 + 346.
      중복된 entryLabel 을 lib/plan-item-view.ts 의 shortEntryLabel 로 합쳤다.
  검증: tsc · vitest 1,150 · vite build · 격리 서버(7817) 브라우저. 자세히는 CHANGELOG.

직전 작업 5: §4.6 B 글자 값 5개 (2026-09-03 완료, Agent C).
  템플릿 탭에서 자간·절대 행간·글자 투명도·글자 뒤 상자·대문자화를 바꿀 수 있다.
  lineGapPx 를 number|null 로 바꿨다 — undefined 로는 끌 수 없었다(JSON 이 키를 지운다).
  검증: tsc · vitest 1,144 · vite build · 격리 서버(7816)에서 출력 페이지의
        computed style 을 직접 읽어 5개가 다 먹는 것 확인. 자세히는 CHANGELOG.

직전 작업 4: §4.6 이어서 (2026-09-03, Agent C).
  U-2 최근 부른 곡 🟢 · U-3 대응곡 연결·해제 🟢(통합 테스트 5개) ·
  U-7 성경 DB 빌드 정보 🟢 · D 서버 오류 배너 🟢
  U-5 는 ⚪ 보류로 판정 — 이미 더 나은 길(lib/lyrics-parser + draftDeck)이 있다.
  남은 것: U-4 수록 정보 편집 · B 글자 값 5개 · C 자리만 잡은 것.

직전 작업 3: U-1 성경 낱말 찾기 (2026-09-03 완료, Agent C).
  성경 탭 참조 칸 아래 '🔍 낱말로 찾기'. src/control/components/BibleSearch.tsx ·
  lib/search-highlight.ts(테스트 17) · api.searchBible.
  누르면 참조 칸에 넣기만 한다 — 예배 중에 잘못 눌러도 화면이 안 바뀐다.
  검증: tsc · vitest 1,133 · vite build · 격리 서버(7812) 브라우저. 자세히는 CHANGELOG.

직전 작업 2: 만들어져 있는데 화면에 길이 없는 기능 전수 조사 (2026-09-03, Agent C).
  방법: 서버 라우트 67개 · api.* 45개 · WS 메시지 · TextStyle 14필드 · CueItem 8종을
        화면 사용처와 대조하고, 후보는 격리 서버(7811)에서 실제로 호출해 확인.
  결과: §4.6 — A 부를 곳 없는 기능 9개(성경 전문 검색·최근 부른 곡·대응곡 연결 …) ·
        B 화면에서 못 바꾸는 글자 값 5개 · C 자리만 잡은 것 5개 · D 버려지는 서버 오류 ·
        E 문서 없는 스크립트 1개. **고치지는 않았다 — 무엇부터 할지 사용자 결정 대기.**

직전 작업: 예배 순서 탭 네 가지 개선 (2026-09-03 완료, Agent C).
  ① 탭 이동에 편집 초안이 사라졌다 → src/control/panels/plan-draft.ts (sessionStorage)
  ② 찬양 검색이 8건만 나와 「은혜」가 안 보였다 → 서버 일치 등급 정렬 + limit 30 + total 실측
  ③ 불러온 순서에 '저장하기' 가 없었다 → 맨 위 칸에 유형+순서를 함께, 버튼이 kind 를 따라감
  ④ '기본 설정' 저장 버튼을 못 찾았다 → 카드 맨 아래에 같은 버튼 + 상태 문구
     (덤: 이름을 바꾸면 저장 안 한 defaults 가 서버 값으로 덮이던 버그도 고침)
  검증: tsc · vitest 1,116 · vite build · 격리 서버(7810) 브라우저 확인. 자세히는 CHANGELOG.

직전 작업: 새찬송가 한/영 대조 문제 목록 (2026-08-27 완료, §3.51).
  결과: A 영어 없음 8 · B 줄 부족 111 · C 줄 수는 맞지만 끊김 490 · D 정상 36
        → 손볼 곡 609/645. 리포트: npm run report:bilingual
  ⚠️ **재분할은 아직 하지 않았다** — 사용자 결정이 필요하다 (아래 '남은 것' ⑧).
```

### 사용자에게 남은 것 (읽고 지우세요)
```
① 새찬송가 **639 「주 함께 하소서」** 만 영어 원제가 비어 있습니다 (1,127곡 채워짐).
   출처에 그 곡이 아예 없어서 짐작해 채우지 않았습니다. 원제를 아시면 알려 주세요.
② `title_alt`(원제)는 **저장·API 로만 나가고 화면·검색에는 안 보입니다.** 찬양 목록·
   검색에 보이게 할지 **결정이 필요합니다** — 원제를 넣은 목적(영어 가사 찾기)이
   그것 없이는 닿지 않습니다. (§4.5 · REVIEW-agent-c §5 도 같은 항목을 대기 중)
③ 프로젝터가 **항목별 템플릿을 무시합니다.** 항목에 '교독문'·'순서 표시' 템플릿을
   지정해도 늘 '전체' 로 그립니다. 지금은 요청에 맞지만(늘 크고 두껍게), 항목이
   고른 것을 따르게 바꿀 수 있습니다 (plan-projector.md §9).
④ 새찬송가 교독문 **5번 '시편 8편'이 원본에서 내용을 반복합니다**
   (KNOWN-DATA-ISSUES §4.6). 고치지 않고 넣었습니다 — 실물을 보고 판단하세요.
⑤ 템플릿 **id 3 사본**을 지우려면:
   node scripts/migrate-template-ids.ts --delete-copies --ids 3 --apply
⑥ **옛 순서표의 찬양 항목에는 곡집 표기가 없습니다** — 제목 슬라이드가 곡 제목만
   뜹니다(허용하신 형태). 새로 넣는 항목부터 '새찬송가 1장' 이 붙습니다.
⑦ **lint(ESLint·Prettier) · CI 도입 여부** — §6 '아직 열린 질문' 에도 있습니다.
⑧ ★ **새찬송가 한/영 재분할 — 609곡.** 영어가 한국어 줄에 맞지 않고 30자쯤에서
   기계적으로 잘려 있습니다. 화면에 `Amazing grace! how sweet the / sound! That
   saved a wretch like` 처럼 나갑니다. 목록·내용은 `npm run report:bilingual`.
   **재분할 방식을 정해 주세요** — 자동 재분할 후 사람 승인 / 송출 기록 25곡만 먼저 /
   손으로 하나씩.
```

> 이 블록은 **하나만** 둡니다. 2026-08-26 에 같은 제목의 블록이 두 개(낡은 것·새 것)로
> 겹쳐 있던 것을 합쳤습니다 — 겹치면 어느 쪽이 최신인지 알 수 없습니다.


### 남은 로드맵 (2026-08-26 실측으로 갱신)
```
- UI 테스트 (src/ 커버리지 0) — 검수 리포트가 '가장 큰 빚' 으로 지목.
  1,041개가 전부 lib/·server/ 대상이고, src/control 을 겨냥한 파일은 1개뿐이다.
- R-4 PlanPanel 훅 분리 — 🟡 부분 조치로 멈춤 (3,147 → 2,244줄).
  목록 블록이 스코프 식별자 81개를 공유해 '껍데기로 옮기기' 는 오히려 나쁘다.
  길은 usePlanStorage·usePlanSend·usePlanAdd 로 상태를 빼는 것 (REVIEW-agent-c §5).
- 영어 원제를 화면에 보이기 — 데이터는 1,127곡 있는데 그리는 코드가 없다 (위 ②).
- D-6 여러 단에 걸친 슬라이드 — 한 슬라이드에 여러 줄이 들어가면(2줄씩·섹션 전체)
  그 섹션의 **첫 단만** 나간다. 슬라이드가 걸친 단 범위를 통째로 잘라야 맞다.
  1줄씩 송출할 때는 정확하므로 급하지 않다 (2026-09-04 격리 서버 7810 실측).
- D-C 검토 대기열 재설계
- Electron 패키징 (Phase 6)
- 퍼블릭 도메인 성경 DB (배포용) — 근거: REVIEW-external-data-2026-08-21.md
```

### 작성 예시 (복사해서 쓰세요)
```
- [시작 2026-08-15 14:30 / Agent B] D-5 찬미예수 2000 가져오기
  계획: scripts/import-songbook-folder.ts 신규. 폴더=곡집, 「번호 제목.txt」=곡.
        원본 줄바꿈을 보존하므로 linesSource='imported'
  다음 단계: praise:check 로 자료 점검 → 파서 단위테스트 → 미리보기 → 사용자 확인 → --apply
```

### 중단·인계 기록 (Interrupted / Handed over)
> 중단이 확인되면 여기에 남깁니다(규칙 C·E). 이어받은 Agent 도 여기에 이어서 적습니다.

```
(없음)
```

---

## 8.1 중단된 작업을 이어받는 절차

### 판정 — 추측하지 말고 실제 상태를 본다
`🟡 진행중` 인데 담당자가 응답 없거나 오래됐다고 판단되면 **아래 근거로 직접 확인**하세요.
추측하지 말고 실제 상태를 보고 판단합니다.

```bash
# 1) 미커밋 변경 = 진행 중이던 흔적 (무엇을 만지고 있었는지)
git status --short
git diff                     # 어디까지 고쳤는지

# 2) 파일 수정 시각 = 마지막 활동 시점 (중단 시각 추정)
ls -lt lib/*.ts server/**/*.ts src/control/**/*.tsx scripts/*.ts docs/*.md | head

# 3) 남은 잔여물 확인
git stash list               # 중단 전 stash 했을 수 있음
git branch --no-merged main  # 미병합 작업 브랜치

# 4) 코드가 실제로 동작하는 상태인지 (반쯤 고친 상태일 수 있음)
npx tsc --noEmit
npx vitest run
```

**판정 기준 (제안)**
| 상황 | 판정 | 조치 |
|---|---|---|
| 미커밋 변경 있음 + 파일 수정이 **수 시간 이상 전** | 중단됨 | 8장 로그를 읽고 이어받기. 상태를 `🟡 진행중(인계: 본인)` 으로 갱신 |
| 미커밋 변경 없음 + 보드만 `🟡` | **착수 직후 중단** | 그 항목은 사실상 미착수 → `🔴` 로 되돌리고 새로 시작 |
| 테스트/타입검사 실패 | **반쯤 고친 상태** | 먼저 그 부분을 완성하거나 되돌린 뒤 진행 (사용자에게 보고) |
| 판단이 애매함 | — | **사용자에게 확인** (임의로 되돌리지 말 것) |

### 사용자가 할 수 있는 최소 조치
Agent 가 갑자기 멈췄을 때 사용자는 다음 한 줄만 남겨도 충분합니다.
```
- [중단 2026-08-15 15:10] Agent B 사용량 만료로 중단. 위 항목 이어받을 Agent 필요.
```

---

- [시작 2026-08-28 / Agent C] 하이픈 정리 중 발견한 `lyrics-parser` 언어표 오인 버그
  계획: `SECONDARY_LINE` 정규식이 `| en-vy` 의 `en` 을 언어 코드로 먹는다.
        태그 뒤에 공백/줄끝을 요구하도록 고치고 테스트로 못 박는다. DB 피해 범위도 잰다.
  결과: 고쳤다. `SECONDARY_LINE` 이 `| en-vy` 의 `en` 을 언어 표로 먹었다.
        표로 인정하는 조건에 '뒤가 라틴 낱말이 아닐 때' 를 넣었다 (`|zh中文` 은 그대로 산다).
        `tests/unit/lyrics-multilang.test.ts` 에 7가지 함정을 못 박았다.
        검증: `tsc --noEmit` · `vitest run` 1050개 통과 · `vite build`.
  피해: DB 의 새 104·215·316·402·520·568장 영어 가사에서 `en` 이 사라져 있다 (26줄).
        폴더 자료 반입 때 함께 되돌아온다 — 별도 수리는 필요 없다.
  다음 단계: 폴더 자료 반입 스크립트.

- [시작 2026-08-28 / Agent C] 하이픈 정리 규칙 확정 (`lib/lyrics-hyphen.ts`)
  계획: 반입 때 쓸 ① 음절 하이픈 붙이기 ② 크롤링 잔재 떼기 를 함수로 만들고 실측으로 못 박는다.
  결과: 41곳(줄 넘어 쪼개진 낱말)은 사용자가 전부 고쳤다 — **확인했다**.
        붙이기 규칙을 세 번 고쳤다. 전부 실측으로 잡았다.
        ① 하이픈 양쪽 공백 (`glo - ry`) ② 잇단 하이픈의 겹치는 자리 (`cre-a - tion`
        → 한 번 치환하면 `crea - tion`) ③ **뒤가 소문자일 때만** 붙인다 — '양쪽 소문자'
        로 좁히면 `A-men` `O-ver-come` 이 안 붙는다.
        사전(`/usr/share/dict/words`)으로 자료 전체를 재서 **하이픈을 남겨야 하는
        합성어 21가지**를 찾았다 (`nail-scarred` 20번, `thorn-crowned`, `blood-bought`,
        `far-off`, `well-nigh` …). 이것 없이 붙이면 뜻이 상한다.
  남은 것: 두 낱말 사이 하이픈 **4곳** — 붙이지 말고 공백으로 바꿔야 한다.
        새 580장 25·51·77줄 `garden - land` · 새 94장 23줄 `His - dear`.
        `npm run report:hyphen` 두 번째 절에 후보 8곳으로 나온다 (나머지 4곳은 정상).
  다음 단계: 없음 — 아래 항목으로 이어짐.

- [완료 2026-08-28 / Agent C] 새찬송가 한/영 자료 재반입 641곡
  경위: 반입 스크립트는 **이미 있었다** — `scripts/import-hymn-bilingual.ts`
        (`npm run hymn:bilingual`, Agent J, 8/23). 새로 만들지 않고 그것을 썼다.
        내가 만든 `lib/lyrics-hyphen.ts` 는 기존 `lib/hymn-hyphen.ts` 와 겹치고
        더 나빴다(사전 굴절 처리·고유명사 예외·접두사 규칙이 없었다) — **지웠다.**
  고친 것: `lib/hymn-hyphen.ts` 에 규칙 하나를 더했다 — **하이픈 뒤가 대문자면 남긴다.**
        DB 에 `LifeLine` 이 들어가 있었다 (새 500장 21번, 원본은 `Life-Line`).
        사전에 `lifeline` 이 있어 규칙이 붙여 버렸다. 자료 전체를 재서 이 조건에
        걸리는 것은 `Life-Line` `Life-Boat` `plea-Christ` 셋뿐이고 전부 남기는 것이 맞았다.
  반입 전 확인: 새찬송가 643곡의 `updated_at` 이 8/25 13:25 에 밀리초 단위로 뭉쳐 있다
        — 일괄 스크립트(`restore-song-lyrics`) 흔적이고 그 뒤 사람이 UI 로 고친 곡은 없다.
        덮어써도 잃을 것이 없음을 확인한 뒤 넣었다.
  스냅샷: `data/backups/songs-before-hymn-bilingual-2026-08-28T20-40-54.sqlite`
  결과: 641곡 · 영어 12,450줄. 가사 글자가 달라 **넣지 않은 곡 4곡** (588·624·638·645)
        — 손으로 봐야 한다.
  검증(반입 뒤 DB 조회):
        · 파서가 먹었던 `en` 6곡 전부 돌아왔다 (envy·enchained·enfold·encircling·enter·endure)
        · 하이픈으로 시작하는 영어 줄 26줄 → **0줄**
        · 크롤링 잔재 → **0줄**
        · 줄 넘어 쪼개졌던 41곳 14곡 전부 낱말로 들어갔다
        · `Life-Line` 유지 확인
  한/영 대조 (`npm run report:bilingual`): A 8→**3** · D 36→**99** · B 85 · C 458.
        **실제로 부른 곡 중 문제 있는 것은 4곡.**
  남은 일: 아래 항목으로 이어짐.

- [완료 2026-08-28 / Agent C] 새찬송가 645곡 전수 반입 — 사람이 집어 허용하는 옵션 둘
  사용자가 고친 것: 41곳(줄 넘어 쪼개진 낱말) · 4곳(두 낱말 사이 하이픈) ·
        가사가 달라 막혔던 4곡(588·624·638·645) · 새 384장. **45곳 전부 DB 에서 확인했다.**
  옵션 추가 (`scripts/import-hymn-bilingual.ts`):
        `--allow-text-change 624,638` — 가사 글자가 달라도 넣는다
        `--keep-amen 624` — 줄 끝 아멘을 표시로 빼내지 않는다
        **둘 다 번호를 적어야 듣는다.** 전체를 끄는 스위치는 두지 않았다 — 그러면
        아무도 보지 않은 채 645곡의 가사가 바뀔 수 있다. 어느 곡을 왜 허용했는지가
        명령줄에 남아야 한다.
  왜 624장에 `--keep-amen` 인가: 그 곡은 `우리 모두 찬양해 아멘` 이 네 번 나온다 —
        아멘이 **가사**다. 표시 하나로 바꾸면 화면에 아멘이 한 번만 나온다.
  찾아 고친 결함 (`lib/hymn-amen.ts` 로 분리 + 테스트 13개):
        아멘을 **한국어만** 떼고 있었다. 그래서 227곡에서 한국어는 아멘 없이,
        영어는 `… Amen.` 으로 나가고 그 뒤에 아멘 슬라이드가 또 붙었다.
        ① 짝 한국어에서 아멘을 뗀 줄이면 영어 `Amen` 도 뗀다
        ② 곡의 **맨 끝** 영어 `Amen` 도 표시로 옮긴다 (한국어에 아멘이 없던 148곡)
        결과: 어긋난 아멘 **243줄/227곡 → 6줄/3곡.** 남은 셋은 절마다 반복되거나
        (새 181장 4절) 어구의 일부(새 613장 `hallelu, amen`)라 건드리지 않는 것이 맞다.
  검증(반입 뒤 DB 조회): 645곡 · 영어 12,498줄 · 영어 없는 곡 **0** ·
        하이픈으로 시작하는 줄 0 · 크롤링 잔재 0 · `LifeLine` 0 / `Life-Line` 21 유지 ·
        손으로 고친 45곳 15곡 전부 살아 있다.
        `tsc --noEmit` · `vitest run` 1066개 · `vite build`.
  한/영 대조: **A 0곡** (8→3→0) · **D 103곡** (36→99→103) · B 83 · C 459.
        **실제로 부른 곡 중 문제 있는 것은 3곡.**
  남은 일: **C 459곡** — 영어가 악절이 아니라 고정 폭으로 끊겨 한국어와 짝이 안 맞는다.
        새 384장이 전형이었는데 사용자가 손으로 고쳐 D 가 됐다(위 표본 참고).
        원본 자료 자체의 한계이므로 영어를 악절 단위로 다시 끊어야 한다.
        한국어 줄 수에 맞춰 영어를 재배분하는 자동 제안 + 사람 승인이 다음 후보다.

- [시작 2026-08-28 / Agent C] C 459곡 — 영어를 한국어 줄에 맞춰 다시 끊기
  계획: 영어가 고정 폭으로 끊겨 한국어와 짝이 안 맞는다. 한 절의 영어를 이어 붙인 뒤
        한국어 줄 수만큼 다시 나눈다. 끊는 자리는 문장 부호와 낱말 경계, 그리고
        한국어 줄 길이 비율로 정한다(DP). **낱말이 하나도 사라지지 않는 것**을 불변식으로 검사한다.
  결과: `lib/lyrics-rebreak.ts` (동적 계획법) + `npm run hymn:realign` 을 만들었다.
        **아직 쓰지 않았다 — 미리보기와 리포트까지다. 사람이 승인해야 확정된다.**
  정확도: 이미 사람이 맞춰 둔 섹션 663개로 재서 **85.1% 를 그대로 재현**한다
        (`npm run hymn:realign -- --measure`). 낱말 손실 0. 나머지는 대개 한 낱말 차이다.
  신호를 셋 찾았다 (전부 실측):
        ① **대문자** — 찬송가는 줄마다 대문자로 시작한다. 65.6% → 79.0%
        ② **벌점을 더한다** — 대문자면 0 으로 자르고 부호로 깎던 판은 부호 신호를
           통째로 버려 어떤 값을 넣어도 79.0% 에서 꿈쩍하지 않았다. 79.0% → 87.7%
        ③ **끊김 판정을 조인다** — '소문자로 시작' 만으로는 새 4·7장처럼 쉼표 뒤라
           정상인 것을 잡아 **더 나쁘게** 만들었다(미리보기에서 걸렸다).
           앞 줄이 부호 없이 끝날 때만 끊긴 것으로 본다.
  적용하면: **C 458 → 64곡 · D 104 → 498곡** (509곡 1,470섹션 6,651줄)
  적용: 사용자 승인 후 `--apply`. **508곡 · 1,468섹션**을 썼다.
        스냅샷 `data/backups/songs-before-hymn-realign-2026-08-28T23-14-09.sqlite`
  검증: 스냅샷과 현재 DB 의 낱말 열을 645곡 한국어·영어 모두 대조 — **1290/1290 일치.**
        줄 수도 25,213 로 같다. 끊는 자리만 달라졌다. 다시 돌리면 0곡(수렴).
  결과: **C 458 → 64곡 · D 104 → 498곡** (예측과 정확히 일치). 부른 곡 중 문제 3곡.
  남은 C 64곡의 성격(실측): 줄 수가 다르다 118섹션 · 부호 뒤 소문자라 정상일 수 있다
        74섹션 · 아직 끊겨 있다 67섹션. 마지막 것은 **한국어가 영어 어절보다 잘게
        나뉜 곡**이 많아(새 83장 `자나 깨나` `깨끗하게`) 영어만 다시 끊어서는 풀리지
        않는다. 한국어 줄까지 합쳐야 하는데 그것은 가사를 바꾸는 일이라 사람이 정해야 한다.
  다음 단계: 없음. 새찬송가 한/영 작업은 여기서 마무리.

- [시작 2026-08-28 / Agent C] 예배 중 위험 셋 (사용자 요청: 순서대로)
  ① 서버가 죽으면 되살릴 장치가 없다 — 감시 루프 + 전역 예외 처리
  ② `lines_source='manual'` 이 새찬송가 645곡 전부라 사람 작업을 가려내지 못한다
  ③ 백업이 DB 와 같은 디스크에만 있다
  ①  완료. `start.sh` 감시 루프 + `server/index.ts` 전역 핸들러(**파일 맨 위**).
     핸들러를 종료 처리 옆에 두었더니 최상위 `await` 보다 뒤라 걸리지 않았다 —
     `PORT=1` 로 시험해 잡았다. 검증: kill -9 → 코드 137 → 1초 뒤 새 PID 로 기동,
     브라우저가 새 프로세스에 다시 붙었다(`output: 1`). 20초 안 5회면 포기.
     문서: TROUBLESHOOTING 2.6c
  ②  완료. 반입을 `'imported'` 로 바꾸고 기존 645곡 2,262섹션을 옮겼다
     (`scripts/relabel-lines-source.ts`, 스냅샷 2026-08-29T03-36-28).
     `'imported'` 는 자동 작업에서 `manual` 과 똑같이 보호되고(둘 다 `!== 'auto'`)
     검토 대기열에도 안 올라온다 — 확인: 645/645 confirmed 유지, 대기열 그대로.
     이제 `manual` 은 통일찬송가 4곡뿐 = **사람이 앱에서 승인한 것만**이다.
     도구는 `--to manual` 을 거부한다 — 승인은 사람이 하는 것이다.
     CLAUDE.md 의 보호 규칙에 `manual` / `imported` 구분을 적었다.
  ③  완료. `SERMON_BACKUP_DIR` 로 다른 디스크를 가리킬 수 있게 하고(`~` 도 풀린다),
     같은 디스크면 **서버가 뜰 때** 알린다 — 스크립트를 돌릴 때만 말하면 정작 봐야 할
     사람이 못 본다. `onSameDevice()` 는 알 수 없으면 false 로 본다(헛경고 금지).
     검증: 다른 폴더로 스냅샷이 실제로 떠지는 것 · 기동 경고가 나오는 것을 확인.
     문서: TROUBLESHOOTING 3.1b
  **백업 위치를 실제로 옮기는 것은 하지 않았다** — 교회 가사가 클라우드로 올라가는
  일이라 사용자가 정할 문제다. 방법만 문서에 남겼다.
  남은 것: 백업 정리 장치가 없다(43개 135MB, 쌓이기만 한다) · 조작 화면 둘일 때 잠금 없음
     · 폰트 미설치 시 조용히 대체 · B 83곡 / C 64곡

- [시작 2026-08-29 / Agent C] 한영악보 PDF 128개 → 찬양 자료
  계획: 먼저 글자 추출 가능 여부를 확인하고(스캔이면 방법이 달라진다), 표본으로
        형식을 맞춘 뒤 전체를 돌린다. 새찬송가와 같은 문단 구분 + 맞춤법 정리.
  다음 단계: PDF 구조 확인 → 표본 3곡 추출 → 사용자와 형식 합의

- [시작 2026-08-29 / Agent C] 한영악보 PDF 128곡 → 찬양 자료
  조사 결과: 전부 글자가 든 PDF(스캔 아님). 가사는 폰트·크기로 코드/음표와 깨끗이 갈린다.
    **영어는 완전히 되살릴 수 있다** — 낱말이 각각 놓이고 음절 사이에만 `-` 조각이 낀다.
    **한국어 띄어쓰기는 PDF 에 없다** — 음절이 음표 밑에 하나씩 놓여 있다.
    다만 128곡 중 102곡이 이미 DB 에 있고(제목 대조) 한국어는 제대로 띄어쓰여 있다.
    영어가 이미 있는 곡은 **0곡** — 128곡 전부 새 내용이다.
  계획: ① 검토용 추출 리포트(쓰기 없음) → ② 제목이 하나로 딱 떨어지는 31곡 → ③ 나머지
  추출기: 스크래치에 pdfjs-dist 로 만들었다(프로젝트 의존성은 건드리지 않았다).
    128/128 성공 · 영어 중앙값 64낱말 · 후렴이 있을 수 있는 곡 32개.
    리포트: `data/reports/hanyoung-pdf-2026-08-29.md` (쓰기 없음, 검토용)
  남은 흠 두 가지 (고칠 수 있다):
    · 늘임표 `-` 를 음절 하이픈으로 오인 (`de-sire-and` → `desireand`).
      틈 분포가 매끈해 좌표로는 못 가른다 → 사전으로 왼쪽부터 하나씩 붙이는 방식이 맞다.
    · 줄을 넘는 낱말 (`sat-is-` / `-fy`) — 새찬송가에서 푼 것과 같은 문제다.
  띄어쓰기·문장 단락 적용 (2차):
    · **영어는 해결됐다.** 음절 하이픈은 붙이고(`sat-is-fy`→`satisfy`), 늘임표는 띄우고
      (`de-sire-and`→`desire and`), 줄 넘는 낱말을 잇고, 부호 뒤 늘임표도 처리
      (`man,-You`→`man, You`). 판정은 **전체를 먼저 붙여 보고 안 되면 앞에서부터 가장 긴
      낱말을 떼는** 방식 — 왼쪽부터 하나씩만 보면 `sat`+`is` 가 낱말이 아니라 부서진다.
      `lib/hymn-hyphen.ts` 에서 `isWord` 를 내보냈다(그 파일의 '셋 이상은 음절' 규칙은
      늘임표가 낀 악보 자료에 맞지 않아 쓸 수 없다).
    · **한국어는 DB 를 쓴다.** PDF 에 띄어쓰기가 없고, 악보 줄바꿈은 낱말을 자른다
      (`헤매이듯`/`이 내 영혼`). 그래서 DB 의 가사·줄나눔을 그대로 쓰고 영어만
      `rebreakEnglish` 로 맞춘다.
    · 결과: 102곡에 영어가 붙었다. **92곡은 괜찮고 10곡은 줄이 잘게 부서졌다** —
      DB 판의 절 구조가 곡의 실제 구조와 달라서다(`psalm_praise` 「지금은 엘리야때처럼」은
      16줄을 전부 1절에 넣어 두었다). 6개 단락은 짝지을 영어가 없었다.
    · 리포트: `data/reports/hanyoung-정리-2026-08-29.md`
  **사용자 결정 대기**:
    ① 71곡이 같은 제목으로 여러 곡집에 있다 — 어디에 넣을지
    ② 절 짝짓기를 위치로 정할 수 없다 (「목마른 사슴」 한국어 2절 = 영어 3절)
    ③ 26곡은 DB 에 없다 — 한국어 띄어쓰기를 새로 만들어야 한다

- [진행 2026-08-29 / Agent C] 한영악보 → 기타 곡집 (사용자 결정 반영)
  결정: 곡집은 **기타(misc)**, **후렴은 각 절에 포함**.
  후렴을 절에 넣으니 어긋남이 사라졌다 — 악보의 절 흐름이 원래 절+후렴이기 때문이다.
  영어 851줄 중 3낱말 미만이 4줄(0%). 전에는 DB 절 구조에 맞추다 10곡이 부서졌었다.
  한국어: PDF 에 띄어쓰기가 없어 **DB 4,396곡 전체를 말뭉치로** 되살렸다.
    왼쪽부터 가장 긴 조각을 잡되 **조각 끝이 DB 에서도 낱말 경계**인 것을 먼저 고른다
    (`창조하셨`+`네` → `창조하셨 네` 를 막는다). DB 줄 끝은 낱말을 자를 수 있어
    띄어쓰기로 단정하지 않는다.
  결과: 940줄 중 627줄 복원. **86곡 완성 · 42곡은 ⚠ 줄이 남았다.**
  결과물: `data/reports/기타-한영/` (반입 형식 txt 128개) · `기타-한영-2026-08-29.md`
  다음 단계: ⚠ 42곡의 한국어를 사람이 띄어쓰기 → 반입

- [완료 2026-08-29 / Agent C] 기타 곡집에 한영악보 86곡 반입
  `npm run misc:import -- --dir <폴더> --only ok --apply` (새 스크립트)
  86곡 · 108절 · 979줄 (한국어 490 · 영어 489). `lines_source = imported`.
  영어 원제를 `title_alt` 에 넣었다 (86곡 전부).
  **한 번 잘못 넣었다가 되돌렸다.** 한국어 없이 `|` 줄이 잇달으면 파서가 같은 줄
  번호로 보고 앞엣것을 버려 **5곡에서 영어 14줄이 조용히 사라졌다.**
  주석(`#`)으로 남기려 했더니 파서가 그 줄을 가사로 읽었다. 결국 한국어 없는 절은
  `_english-only.tsv` 로 옮겼다(9절). 그리고 **반입 스크립트에 줄 수 검사를 넣었다** —
  파일의 `|` 줄 수와 읽힌 줄 수가 다르면 그 곡을 넣지 않는다.
  되돌리기는 `deleteBySource('hanyoung-score')` 로 내가 넣은 것만 지웠다.
  검증: 86곡을 파일과 한 줄씩 대조 — 한 줄도 빠지지 않았다.
  이어서: ⚠ 42곡을 손으로 띄어썼다 (60개 절 · 4,109자, `_hand-spaced.tsv`).
    **음절이 한 자도 바뀌지 않았는지 기계로 검사한다** — 실제로 한 곳을 잘못 적었다
    (`누가 주의 같으리` 를 `주와` 로). 검사가 잡아 원본대로 되돌렸다.
    추출기의 `ㅡ`(U+3161) 처리도 함께 고쳤다 — 늘임표를 모음 글자로 찍은 자료가 있어
    가사에 섞여 들어갔다. 그 덕에 11개 절은 자동으로도 풀렸다.
  결과: 한국어 940줄 **전부(100%)** 띄어쓰기 복원. **기타 곡집 128곡 · 168절 · 1,558줄.**
    128곡을 파일과 한 줄씩 대조 — 한 줄도 빠지지 않았다.
  남은 것: 영어만 있는 9절의 한국어 번역 (`_english-only.tsv`)

- [시작 2026-08-29 / Agent C] 가사 편집 UI 를 두 칸 하나로 (사용자 요청)
  요청: ① 편집 수단이 넷(격자·전체 지우기·원문·타언어)이라 복잡하다 →
        '한국어 | English' 두 칸을 **양쪽 다 편집**되게 하고 나머지를 없앤다
        ② 편집 중에도 아래 '슬라이드' 가 이전 곡이다 → 편집 중인 곡으로 보이게
  조사: 실제 쓰이는 언어는 ko·en 둘뿐 (zh/ja/grc/heb 0줄).
        `buildSongDeck` 이 lib 의 순수 함수라 **저장 전에도** 서버와 같은 코드로
        미리보기를 만들 수 있다 (라이브 출력은 건드리지 않는다).
  완료. `lib/lyrics-compose.ts`(테스트 12개) + `LyricsTwoPane.tsx`.
    지운 것: `LyricsGrid.tsx` · `TranslationPane.tsx` · '원문으로 편집' · 'X 가사 전체 지우기'
    한국어가 절 나눔과 줄 수를 정한다 — 두 칸이 어긋날 때 무엇을 믿을지 정해 두지 않으면
    저장할 때마다 결과가 달라진다. 남는 영어 줄은 버리지 않고 알린다.
    손대지 않은 언어(zh 등)는 원문에서 뽑아 두었다가 줄 번호 그대로 되살린다.
    두 칸의 입력값은 컴포넌트가 들고 있는다 — 합친 결과에서 되읽으면 한글 조합 중
    커서가 튄다.
    **빈 가사 저장을 막았다** — 한국어 칸을 비우고 저장하면 곡이 통째로 사라졌다.
  미리보기: 편집 중에는 아래 슬라이드가 **고치는 곡**을 보인다. `buildSongDeck` 은
    서버가 쓰는 것과 같은 lib 함수라 실제 송출과 같고, **라이브 출력은 건드리지 않는다.**
    누를 수 없게 했다 — 송출 중인 곡이 아니라 누르면 라이브가 엉뚱한 자리로 튄다.
    원문에 있는 언어를 모두 보인다(표시 언어 설정을 따르면 방금 적은 번역이 안 보인다).
  검증(격리 서버 7813, 실제 자료 복사본): 두 칸 양쪽 편집 → 저장 → DB 에서 확인 ·
    줄 수 표시 · 미리보기 실시간 갱신 · 빈 저장 거부 후 가사 12줄 그대로.

- [완료 2026-08-29 / Agent C] 기타 곡집의 '승인' 표시 제거 (사용자 요청)
  `node scripts/relabel-lines-source.ts --songbook misc --from imported --to auto --apply`
  126곡 · 166섹션. 스냅샷 `songs-before-relabel-lines-source-2026-08-29T17-16-46.sqlite`
  **사용자가 앱에서 직접 고쳐 저장한 2곡은 그대로 두었다** (「모든 능력과 모든 권세」·
  「감사해요 주님의 사랑」, 16:14·16:49 저장 → `manual`). 사용자가 고른 선택지가
  그 둘을 포함하지 않았다.
  딸려 오는 것(사용자에게 알림): 126곡이 **검토 대기열에 올라오고**(전체 128 · 대기 126)
  자동 작업(`chorus:merge` 등)의 대상이 된다. `lyrics:realign` 은 hymn_new 만 본다.
  다른 곡집은 그대로다 — hymn_new 645곡은 여전히 `imported`(승인 표시 유지).
  덧붙임: `relabel-lines-source` 의 표본 출력이 번호 없는 곡집에서 `null장` 으로 나오던 것을 고쳤다.

- [완료 2026-08-29 / Agent C] 기타 곡 제목을 '한국어 - 영문' 으로 (사용자 요청)
  `npm run song:retitle -- --songbook misc --apply` (새 스크립트, `--undo` 로 되돌린다)
  128곡. 스냅샷 `songs-before-retitle-2026-08-29T21-08-43.sqlite`
  **왜 필요했나**: `title_alt`(영어 원제)가 **화면 어디에도 나오지 않는다** — 목록·검색
  모두 한국어 제목만 보여 준다. 그래서 영어로 곡을 찾을 수 없었다.
  `title_alt` 는 그대로 두었으므로 꼬리를 떼면 원래 제목이 나온다(`--undo`).
  한국어 제목에 이미 ` - ` 가 있으면 나중에 어디서 끊을지 알 수 없어 건드리지 않는다
  (기타에는 그런 곡이 없었다).
  검증: 검색이 한국어·영어 양쪽으로 된다 (`As the deer` → 1곡, `Give Thanks` → 2곡).
  다른 곡집 제목은 그대로.
  → 근본 해법으로 대체했다 (아래).

- [완료 2026-08-29 / Agent C] 목록·검색에 영어 원제를 그린다
  제목을 고치는 대신 **화면에 그렸다** — `title_alt` 는 이미 검색 결과에 실려 오고 있었고
  UI 가 안 그렸을 뿐이다. 서버·DB 변화 없음, `styles.css` + `SongPanel.tsx` 만.
  **1,255곡이 한 번에 살아났다** — hymn_new 644 · hymn_old 483 · misc 128.
  제목이 이미 원제를 품고 있으면 두 번 적지 않는다.
  기타 제목은 `--undo` 로 되돌렸다 — 이제 필요 없다.
  잡은 것: 곡 머리에서 원제가 **대문자로 변형**됐다 (`.card h2` 의 `text-transform`).
  원제는 고유명사라 `text-transform: none` 으로 되돌렸다.
  검증(격리 서버 7814): `Amazing Grace` → 새305 · `By Your side` → 주곁에 ·
  `As the deer` → 목마른 사슴. 곡 머리 대소문자 보존 확인.

- [시작 2026-08-30 / Agent C] 그림 슬라이드 + 슬라이드쇼 항목 (예배 전 순환)
  계획: `image` 슬라이드 종류 + `slideshow` 순서 항목. 폴더를 가리키면 그 안의 그림을
        이름순 슬라이드로. 1장이면 썸네일, 여러 장이면 기존 자동 넘김(`auto`)이 순환한다.
        악보 출력도 이 위에 올라간다.
  완료. `image` 슬라이드 종류 + `slideshow` 순서 항목.
    **폴더만 담고 그림 이름은 담지 않는다** — 담아 두면 폴더에 파일을 더 넣을 때마다
    순서표를 고쳐야 한다. 폴더에 넣기만 하면 되는 것이 이 기능의 요점이다.
    순환은 **기존 자동 넘김을 그대로 쓴다** (구분 행의 `auto`) — 새로 만들지 않았다.
  잡은 것 셋 (전부 브라우저에서 실측):
    ① 자동 축소(`--fit-scale` 0.87)가 그림까지 줄여 가장자리에 흰 띠가 남았다 →
       그림은 축소에서 뺀다. 창 크기 변경·스타일 변경 경로에서도 종류를 넘기게 고쳤다.
    ② 템플릿의 글자 여백(80/50px)에 갇혀 화면을 못 채웠다 → `full-bleed` 로 여백을 없앤다.
    ③ 그림 뒤 '비우기' 를 하면 여백이 안 돌아왔다 — `clear` 는 `render` 를 거치지 않고
       `clearSlide()` 를 직접 부른다. 그 함수에서도 끄게 했다.
  보안: 폴더 이름은 `safeFolderName` 으로 거른다(`..`·경로 구분자·숨김 금지).
    저장 경로와 목록 라우트 **두 곳에서** 막는다 — 한쪽만 막으면 뒤처진다.
  검증: 저장 왕복 테스트 8개(인용구가 사라졌던 화이트리스트 함정 대비) ·
    격리 서버 7815 에서 폴더 3장/1장 · 송출 · 화면 꽉 참 · 비운 뒤 여백 복귀 확인.
  다음 단계: 악보 — PDF → PNG 변환 + 단 경계를 DB 에

- [완료 2026-09-01 / Agent C] 예배 전 안내 — 있는 줄도 몰랐던 문제
  신고: '▶ 를 눌러도 자동 전환이 안 된다'. **버그가 아니었다** — 자동 넘김을 켠
  구분에만 ▶ 가 나오고, 켜기 전에는 구분 행에 아무 표시도 없었다.
  게다가 `.cue-divider .actions { opacity: 0 }` 이라 **마우스를 올려야만** 보였다.
  고친 것:
    · 구분 행에 **늘 보이는 흐린 ⏱**(0.45). 누르면 켜지고 그 자리가 ▶ 로 바뀐다 —
      켜는 곳과 시작하는 곳이 같아야 헤매지 않는다.
    · 감싼 칸을 투명하게 두고 자식만 올리면 소용없다(**투명도는 곱해진다**).
      칸을 켜 두고 숨길 버튼(↑↓✕)만 하나씩 껐다.
    · 편집기 체크박스 옆에 켠 뒤 무엇이 생기는지 적었다.
  검증(격리 서버 7816 + WebSocket 관찰): ⏱ 0.45 · 화살표 0 · ⏱ 클릭 → ▶ 로 바뀜 ·
    ▶ 누르니 `예배 부름 ↔ 대표기도` 가 2초마다 순환(끝에서 처음으로). 자동 진행 자체는
    처음부터 정상이었다.

- [시작 2026-09-01 / Agent C] 찬양 탭에서 새 곡 만들기 · 곡 삭제
  서버는 이미 있다 (`POST /api/songs` · `DELETE /api/songs/:id`) — 부르는 UI 만 없었다.
  계획: api.ts 에 두 함수 → 찬양 탭에 '＋ 새 곡' → 만든 뒤 두 칸 편집기로 이어짐 → 삭제 버튼
  완료. `api.createSong` · `api.deleteSong` + 찬양 탭의 '＋ 새 곡' · '곡 삭제'.
  고친 것: 서버의 `POST /api/songs` 가 `lines_source` 를 기본값 `'auto'` 로 넣고 있었다.
    사람이 앱에서 직접 쓴 곡이 검토 대기열에 올라오고 자동 작업 대상이 된다 —
    검토할 '자동 결과' 가 없는데도. `'manual'` 로 바꿨다.
  만든 뒤 **곧바로 가사 편집을 연다** — 제목만 있는 곡을 목록에 남기면 '가사 없는 곡'
    이 쌓이고 무엇을 하려던 것인지 나중에 알 수 없다.
  삭제는 제목을 보여 주고 한 번 묻는다. `data/songs.sqlite` 는 git 에 없다.
  검증(격리 서버 7817, 실제 자료 복사본): 만들기 → 두 칸 편집 → 저장 → DB 에
    `source=manual · lines_source=manual` 로 한/영 4줄 확인. 128 → 129곡.
    삭제 → 128곡으로 복귀, **고아 섹션·고아 줄 0** (CASCADE 확인).

- [시작 2026-09-01 / Agent C] 사람이 친 줄은 묶지 않는다 (사용자 결정 B)
  증상: 편집기에 12줄을 쳤는데 화면에 4줄이 두 줄로 묶여 나갔다.
  원인: `fitLinesToWidth` 가 이웃 두 줄을 묶는다(찬송가 운율 행을 위한 장치).
        폭 판정이 **공백을 빼고** 세어 34자짜리를 24자로 본다.
  결정: 공백을 세게 고치면 모든 곡집 22~34% 가 달라진다(실측) → 하지 않는다.
        대신 `lines_source='manual'`(사람이 앱에서 친 줄)만 묶지 않는다.
  완료. `SongSection.linesSource` 를 `getSong` 이 싣고, `buildSectionSlides` 가
    `manual` 이면 `fitLinesToWidth` 를 건너뛴다. 편집 중 미리보기도 `manual` 로 그린다
    (안 그러면 미리보기와 저장 후가 어긋난다).
    `server/db/songs.ts` 의 `LinesSource` 는 shared 것을 그대로 쓰게 했다(두 곳에 적으면 갈린다).
  **곡 2 의 '첫 장 3줄' 은 이것과 다른 장치다** — 줄이 13개(홀수)라 2줄씩이면 마지막에
    한 줄만 남는다. `chunkWithoutOrphans` 가 그것을 막으려고 한 장을 3줄로 만든다.
    manual 이어도 그대로 일어난다. 고칠 것이 아니라 **사용자가 알아야 하는 것**이다.
  검증: 곡1 3장(4줄 묶임) → **6장(친 대로 2줄씩)**. 찬송가는 그대로 묶인다(테스트로 못 박음).
  발견: 사용자가 편집 중이라 `주님 말씀하시면` 이 22:44 에 저장되며 13줄 → 12줄이 됐다
    (마지막 두 가사가 한 줄에 들어갔다). 사용자에게 알린다.
