# 다음에 할 일 — 우선순위 계획서 (2026-09-08)

> 작성: Agent C. **읽는 순서**: 이 문서 → [collaboration-report §8](collaboration-report.md#8-진행-중-작업-로그-live-work-log)(중단 지점) → 해당 항목의 근거 문서.
>
> 왜 이 문서가 있나: 배포 직전 점검(4.7)과 R-4 가 닫히면서 **보드에서 🔴·🟡 이
> 거의 사라졌다.** 남은 것은 성격이 다른 셋으로 갈린다 — ① 바로 할 수 있는 기술 부채,
> ② 사용자 결정이 있어야 움직이는 것, ③ 의도적 보류. 그 구분을 먼저 해 둔다.

---

## 0. 지금 상태 (2026-09-08 실측)

| | 값 |
|---|---|
| 검사 | **1,744개 / 103파일** · `tsc` 0 · `vite build` 통과 |
| 커버리지 | `lib` 문장 95.9 · **`server` 66.6** · `src` 25.7 |
| `npm audit` | **배포되는 코드 0건** · 개발 도구 3건 (2026-09-09 실측) — `@vitest/mocker` 경로 이탈. 검사 도구라 앱에 실리지 않는다 (`--omit=dev` 는 0). 판을 올리면 검사 2,044개가 흔들릴 수 있어 **CI 를 세우는 김에 하지 않았다** |
| 보드 | 4.5 · 4.6 · 4.7 전부 닫힘. 열린 것은 4.1 D-7 · 4.3 부채 · 4.2 낮음 하나 |

**800줄 규칙(최대)을 넘는 파일 다섯**

| 파일 | 줄 | 판단 |
|---|---:|---|
| `public/output/output.js` | 1,225 | ⚪ **의도적 예외** (의존성 0) — CLAUDE.md 에 명시 |
| `server/db/songs.ts` | 1,184 | ⚪ 보류 (보드 4.3: 분리하면 충돌 위험) |
| `shared/types.ts` | 1,102 | ⚪ 타입 선언만 — 쪼개도 얻는 것이 적다 |
| `src/control/panels/SongPanel.tsx` | 1,022 | 🔴 **아래 5번** |
| `src/control/components/plan/PlanItemEditor.tsx` | 732 | 🟡 아래 3번 (한계 안이지만 검사 0%) |

---

## 1. 🔴 최우선 — `server/ws.ts` 검사 (문장 57.3%, 미커버 64)

**왜 여기부터인가.** 이 파일이 **예배 중에 화면이 도는 통로**다. 컨트롤 패널의
모든 조작과 출력 페이지·강사 모니터·프로젝터·태블릿의 모든 표시가 여기를 지난다.
깨지면 예배가 멈춘다 — 이 프로젝트에서 비용이 가장 큰 실패다.

그런데 통합 검사가 **하나뿐**이다 (`tests/integration/ws-style.test.ts`, 2026-09-07 에
`style:set` 을 막으면서 만든 것). 미커버 64문장은 이 저장소에서 **안전상 가장 값비싼
빈칸**이다.

**무엇을 잡아야 하나** (`server/ws.ts` 머리말과 `shared/types.ts` 의 `ClientMsg`·`ServerMsg` 를 보고 목록을 만든다)

- 역할별 접속(`hello`: control · output · stage · projector)과 **접속 수 집계**
  → 이것이 틀리면 컨트롤 화면이 'OBS 가 붙어 있다' 고 거짓을 말한다
  (`useLiveState` 쪽 검사는 이미 있다 — **서버 쪽이 비어 있다**)
- 붙는 즉시 **전체 스냅샷**을 주는가 (새로고침 후 복구의 전제)
- `deck:load` 가 `lib/deck-guard.ts` 를 지나는가 · **거부할 때 로그를 남기는가**
  (조용히 버리면 예배 전에 진단할 수 없다 — CLAUDE.md)
- `style:set` 이 `isAllowedStyleKey` 를 지나는가 (있는 검사를 확장)
- `goto` / `next` / `prev` 경계 (음수·범위 초과)
- 모르는 종류·깨진 JSON 에 **죽지 않는가**
- 끊긴 소켓을 향해 쏘지 않는가 (`readyState` 확인)
- `live_state` 저장·복구

**착수 방법.** 기존 통합 검사의 틀을 그대로 쓴다. 격리 서버로 띄우고 실제 소켓을 붙인다.

```bash
SCRATCH=/tmp/verify-ws && mkdir -p $SCRATCH/backgrounds
PORT=7840 SERMON_DATA_DIR=$SCRATCH SERMON_BIBLE_DB=$PWD/data/bible.sqlite node server/index.ts
```

⚠️ **7777 을 건드리지 않는다** — 사용자가 예배 준비 중 띄워 둔 것일 수 있다.
`fileParallelism: false` 이므로 통합 검사는 순차로 돈다(SQLite 파일 공유).

**끝났다고 볼 조건**: `server/ws.ts` 문장 85% 이상 + **변이 검증**으로 헛돌지
않는 것 확인 (거부 로그 지우기 · deck-guard 우회 · 접속 수 안 되돌리기 등).

---

## 2. 🔴 높음 — 곡집 검사 (`db/songbooks.ts` 36.7% · `routes/songbooks.ts` 36.5%, 미커버 130)

**왜.** 여기는 **사용자 데이터를 쓰는 길**이다. `data/songs.sqlite` 에는 사용자가
직접 손본 가사가 있고 **git 에 없다** — 되돌릴 방법이 백업뿐이다(실제로 한 번 지웠다).
그런데 두 파일이 저장소에서 커버리지가 가장 낮은 축이다.

**이미 드러난 함정이 있다** (2026-09-07 번들 작업, `079d654`):

> **곡집을 곡보다 먼저 만들어야 한다.** 순서가 뒤바뀌면 `setEntries` 가 수록 정보를
> 버리고 곡이 전부 **'기타'** 로 떨어진다.

이 규칙은 지금 `server/routes/backup.ts` 안에서만 지켜지고 있고, **곡집 쪽에는
그것을 붙잡는 검사가 없다.** 다음에 누가 순서를 바꾸면 조용히 재발한다.

**무엇을 잡아야 하나**

- 곡집 만들기·이름 바꾸기·지우기 · **곡이 남아 있는 곡집을 지울 때** 무슨 일이 나는가
- `setEntries`(수록 정보) — 곡집이 없을 때 · 번호가 겹칠 때 · 빈 배열일 때
- **위 함정**: 곡집 없이 `setEntries` 를 부르면 '기타' 로 떨어지는 것을 검사로 고정
- 다대다 구조(한 곡이 여러 곡집에) 통합 검색 경계
- `songCount` 집계

**착수 방법.** `tests/integration/` 의 기존 곡 관련 검사 틀을 재사용한다.
**스냅샷 규칙**을 지킨다 — 검사는 격리 데이터 디렉터리를 쓰므로 사용자 DB 를
건드리지 않지만, 손으로 확인할 일이 생기면 `snapshotDatabases()` 를 먼저 부른다
(`cp` 한 줄은 WAL 때문에 **곡 0개 백업**이 된다).

---

## 3. 🟡 높음 — 검사 없는 화면 셋

R-4 에서 화면을 갈랐지만 셋에 검사가 없다. **화면 쪽 버그가 실제로 두 번 났다** —
§4.6 D(조작 화면이 서버 거절을 조용히 버림)와 태블릿 카드 `lanWanted` 결함.
후자는 브라우저로 손으로 눌러서야 찾았다.

| 파일 | 줄 | 무엇을 잡나 |
|---|---:|---|
| `PlanItemEditor` | 732 | **항목별 설정 전부** — 역본·언어·줄 수·배경·글자·악보·순서 표시 조정. 가장 크고 가장 많이 만지는 화면이다 |
| `PlanAddBar` | 308 | 종류 열 개의 입력칸 갈림 · 검색 결과 띠 · 참조 확인 표시 |
| `PlanDefaultsCard` | 264 | 기본값 6행이 `patchDefaults` 로 순서표에 담기는가 |

**틀은 이미 있다.** `tests/unit/plan-cue-list.test.tsx` 와
`tests/unit/plan-storage-ui.test.tsx` 를 그대로 본뜬다. 거기 적어 둔 함정 둘을 조심한다:

- `scrollIntoView` 는 **jsdom 에 없다** — 검사에서 채운다(컴포넌트에 방어 코드를 넣지 않는다)
- `styleTemplates` 를 비워 두면 `<option>` 이 없어 `select` 의 `value` 가 반영되지
  않는다(React 동작) → **프리셋 실물**(`BUILTIN_TEMPLATES`)을 쓴다

`PlanItemEditor`(732줄)는 검사를 붙이면서 **자를지 판단**한다. 자를 때는 R-4 의 방법을
그대로 쓴다 (아래 6번).

---

## 4. 🟢 완료 (2026-09-09) — `server/` 커버리지

문장 66.6 → **86.9** · 분기 **80.4** · 줄 **88.6**. ratchet 을 87/88/79/86 으로 올렸다.

| 파일 | 전 | 후 | |
|---|---:|---:|---|
| `server/routes/tablet.ts` | 36.4 | **100** | 계획서에 없던 것 — 실측으로 찾았다. **태블릿을 여는 유일한 길**인데 QR 을 만드는 줄이 한 번도 안 밟혔다 (있던 검사는 닫혀 있을 때만 봤다) |
| `server/db/readings.ts` | 76.8 | **97.1** | 옛 스키마 → 새 스키마 **이전이 검사 0** 이었다. 사용자 교독문을 통째로 옮기는 길이다 |
| `server/routes/backgrounds.ts` | 75.9 | **94.3** | 분기 51.1 → 97.4. 경로 이탈 막이가 검사 0. **어긋남을 하나 찾아 고쳤다** — 폴더 개수를 이름만 보고 세어 이름이 `.jpg` 인 폴더를 한 장으로 셌다 |
| `server/lan.ts` | 65.6 | **93.8** | `node:os` 를 가짜로 — 실제 랜 카드에 기대면 카드 없는 PC 에서 헛돈다 |
| `server/routes/system.ts` | 70.3 | 74.3 | ⚪ **여기까지가 맞다** (아래) |
| `server/index.ts` · `electron-entry.ts` | 0 | 0 | ⚪ 기동 진입점 — 격리 서버 기동으로 확인한다 |

**`routes/system.ts` 를 끝까지 올리지 않은 이유.** 남은 줄은 실제로 Finder 창을
띄우거나 `brew install` 을 돌려야 밟힌다 — 검사가 남의 PC 에 프로그램을 설치하게
둘 수는 없다. 대신 **판단을 `lib/` 로 옮겼다**: 허용된 명령(`isAllowedCommand`) ·
도구의 절대 경로(`pickToolPath`) · 어느 명령으로 폴더를 여는가(`folderOpenCommand`,
2026-09-09 에 옮김) — 셋 다 세 플랫폼 모두 검사된다. 라우트에 남은 것은 `execFile`
접착부뿐이다.

> 덤으로 `src/control/hooks/useFeedback.ts` 가 **0%** 인 것을 찾아 검사 5개를 붙였다 —
> 검사들이 전부 가짜를 넣고 있어서 진짜 훅은 한 줄도 안 돌았다.

---

## 5. 🟡 중간 — `SongPanel.tsx` 1,022줄 (800 규칙 위반 · 검사 0%)

**R-4 의 방법이 그대로 통한다.** 순서는 이것이다:

1. **자르기 전에 결합을 실측한다** — 블록별로 필요한 이름과 **그 출처**를 센다.
   R-4 가 전에 멈춘 이유가 "프롭 60개짜리 껍데기" 였고, 실측이 그것을 막았다.
2. **산발을 먼저 걷는다** — 여러 블록에 1~4개씩 흩어진 상태를 훅으로 묶는다
   (예배 순서 탭에서 `usePlanFeedback` 이 화면 아홉 중 **일곱**에 흩어져 있었다).
3. **낱개 값이 아니라 훅 객체를 넘긴다.** 추가 바는 낱개로 28개, `add` 객체째로 4개였다.
4. **블록당 프롭 10개를 넘으면 자르는 자리가 틀렸다는 신호.** 프롭을 늘리지 말고
   덜 자르거나, 훅을 하나 더 만들거나, 부모에 둔다.
5. **자를 수 있다고 자르지 않는다.** 한 폼을 행마다 쪼개면 `<select>` 한 줄짜리 껍데기가 된다.
6. 단계마다 `tsc` + `vitest` + `build`, 그리고 **브라우저로 직접** 눌러 본다.

실측 스크립트는 없다 — R-4 때 그때그때 만들어 썼다. 다시 만들 때는
`git log --all -S'출처' -- docs/` 보다 이 문서의 방법 설명을 따르는 편이 빠르다.

⚠️ **`server/db/songs.ts`(1,184줄)는 보드가 ⚪ 보류로 두었다** — 분리하면 충돌
위험이 크다는 판단이다. 화면(`SongPanel`)과 서버(`db/songs.ts`)를 같이 건드리지 않는다.

---

## 6. 사용자 결정이 필요한 것 — Agent 가 시작할 수 없다

> **2026-09-09 에 넷 다 답을 받았다.** D-7 · lint · CI 는 닫혔고, D-6 은 사용자가 보류했다.
> **이 장에 Agent 가 시작할 수 있는 것은 남아 있지 않다.**

### ~~D-7 한/영 병기~~ → 🟢 **닫혔다** (2026-09-09, 사용자 결정)

**Agent 가 할 일이 아니다.** 사용자가 세 가지를 이렇게 정했다:

| 물었던 것 | 답 |
|---|---|
| 자동 재분할인가, 곡별 수동인가 | **수동** — 곡별로 손으로 맞춘다 |
| 재분할 결과를 검토 탭에서 승인할 것인가 | **필요 없다** — 승인 화면을 만들지 않는다 |
| C 등급 490곡 전부인가, 자주 쓰는 곡부터인가 | **훑지 않는다** — 찬양 탭에서 **그때그때 필요한 것만** |

**그래서 다음 Agent 가 하지 말아야 할 것**

- 영어 줄을 **자동으로 다시 나누는 스크립트를 만들지 않는다.** 값이 커 보여도
  사용자가 명시적으로 거절한 방식이다
- 검토 탭에 **한/영 승인 화면을 만들지 않는다**
- 찬양 탭에 **'대조 깨진 곡 찾기' 목록을 미리 짓지 않는다** — 필요해지면 사용자가 말한다

**이미 지켜지고 있는 것** (2026-09-09 확인). 손으로 맞춘 곡이 나중에 자동 작업에
쓸려 가지 않는다:

- 찬양 탭에서 가사를 저장하면 `replaceSections` 가 `lines_source='manual'` 로 찍는다
  (`server/db/songs.ts:730` 기본값)
- `npm run lyrics:realign` 등 자동 작업은 `'auto'` 만 대상으로 한다. 새찬송가 645곡은
  `'imported'` 라 역시 보호된다

배경 자료는 남겨 둔다 — 어느 곡이 깨졌는지 알고 싶을 때만 쓴다 (읽기만 한다):

```bash
npm run report:bilingual    # A·B·C·D 로 갈라 목록·내용을 낸다. 데이터를 바꾸지 않는다
```

> 왜 깨져 있나(2026-08-27 실측): 영어가 한국어 줄에 맞춰 들어간 게 아니라 **한 덩이
> 글을 30자쯤에서 기계적으로 자른** 상태다. 둘째 줄 이후 소문자 시작 4,822줄(38.5%) ·
> 600곡(94%), 첫 줄이 소문자인 경우는 **0건**. 새찬송가 645곡 중 정상은 **36곡**
> (A 8 · B 111 · C 490).

### ~~lint 도구~~ → 🟢 **최소안으로 도입했다** (2026-09-09, 사용자 결정)

`tsconfig.json` 에 **`noUnusedLocals` · `noUnusedParameters` 두 줄**만 켰다.
새 의존성은 0 — `npm run typecheck` 에 그대로 딸려 온다.

켜기 직전 실측 **35건**을 먼저 걷어냈다(`a9fc600`). 대부분 R-4 · SongPanel
리팩터링의 잔재 — 훅으로 옮긴 뒤 아무도 안 쓰는 이름, 두 벌이 된 상수, 전달만
되고 쓰이지 않는 프롭 다섯.

**⚪ ESLint · Prettier 는 도입하지 않는다.** Prettier 는 사람이 표·문단으로 배치한
주석을 흐트러뜨리고, 저장소 전체를 한 커밋으로 흔들어 앞으로의 `git blame` 과 diff 를
그 커밋에 걸리게 한다 — 예배 직전에 무엇이 바뀌었는지 봐야 하는 프로젝트에서는 손해다.
ESLint 는 필요해지면 그때 얹는다 (최소안과 충돌하지 않는다).

**다음 Agent 에게**: 일부러 안 쓰는 인자는 `_` 로 시작하면 통과한다
(`(_index, value) => …`). 죽은 것을 지우면 **그것만 쓰던 import 가 새로 드러난다** —
`tsc` 가 0 을 낼 때까지 돌린다.

### ~~CI~~ → 🟢 **세웠다** (2026-09-09, 사용자 결정)

`.github/workflows/check.yml` — push · PR · 수동. `ubuntu-latest`,
**Node 24(지원 하한 = Electron 이 싣는 판) · 26(개발용) 둘 다.**
`tsc` → `vitest` → `vite build`. 1분 남짓이라 매번 돌려도 부담이 없다.

**⚠️ 커버리지는 CI 에서 돌리지 않는다** (실측으로 정했다). `data/bible.sqlite` 가
git 에 없어 성경 모듈이 통째로 미커버가 되고 **문턱이 반드시 깨진다** —
server 문장 86.9 → 81.2 · 줄 88.6 → 82.6. 문턱을 그 값까지 내리면 ratchet 이
뜻을 잃으므로, 커버리지는 로컬·수동으로 남긴다(`npx vitest run --coverage`).

**검사 31개가 건너뛰는 것이 정상이다** — `bible-api.test.ts` 가
`describe.skipIf(!hasBibleDb)` 로 스스로 물러난다. `0 skipped` 가 되면 오히려
이상한 것이다.

`build.yml`(설치 파일)은 그대로 태그·수동일 때만 돈다 — 맥·윈도우 러너로 20분이 걸린다.

**세우자마자 값을 했다**: 첫 실행에서 Node 24 만 깨졌다 — `plan-api` 의 큰 번들
가져오기가 5초를 넘겼다(러너 Node 24.20.0 에서 14.4s, Node 26 은 0.75s, 맥에서는
두 판 다 0.6s). 리눅스 러너의 Node 24 에서 `node:sqlite` 쓰기만 느리다 —
**설치판(맥·윈도우)에는 영향이 없다.** 그 검사 하나에 30초를 주고 잰 값을 주석에 남겼다.
개발 판(26)만 돌렸으면 못 봤을 것이다.

### D-6 남은 판단 — ⏸ **보류** (2026-09-09, 사용자 결정)

악보 155장 훑기는 **사용자가 나중에 다른 방법으로 하겠다고 했다.**
Agent 가 대신 할 수 없는 일이다 — 그림 위에 겹쳐 그린 단 경계가 맞는지는 눈으로 본다.

지금 상태(2026-09-09 실측, 읽기만 함): 2,061장 중 `needs_review` **155장**,
그중 판정된 것 **0장**(`review_state` 전부 NULL).

**다음 Agent 에게**: 화면은 이미 있다(검토 탭 → '악보 단 경계' · Enter 괜찮다+다음).
경계를 **끌어서 고치는** 기능은 없고, **먼저 훑어 정말 틀린 것이 몇 장인지 세기
전에는 만들지 않는다** — 쓰이지 않을 것을 지을 수 있다.

---

## 7. 의도적 보류 — 건드리지 않는다

| | 왜 |
|---|---|
| `output.js` 1,225줄 | 의존성 0 (OBS 내장 브라우저에서 초기화가 실패할 지점을 없앤다) |
| `server/db/songs.ts` 1,184줄 | 분리하면 충돌 위험 (보드 4.3) |
| S-13 OBS 없이 쓰기 | 사용자 결정 유보 |
| U-5 가사 파싱 미리보기 라우트 | `lib/` 를 직접 부르는 것이 더 빠르다 |
| U-6 역본별 장 수 UI · U-8 곡 수 · U-9 템플릿 CSS | 값이 작거나 그 UI 가 없다 |
| `t: 'measure:report'` · `image.crop` | Phase 3 / 악보 출력(D-6)의 남은 절반 |
| 예배 순서 프리셋 저장 (4.2 낮음) | 우선순위 낮음 |

---

## 8. 이 계획서의 판단 근거를 한 줄로

**비용이 가장 큰 실패는 '예배 중에 화면이 멈추는 것'** 이다. 그래서 검사가 없는 곳
중에서 **그 경로에 가장 가까운 것부터** 갔다 — `ws.ts`(화면이 도는 통로) →
곡집(되돌릴 수 없는 사용자 데이터) → 화면(실제로 두 번 사고가 난 종류) →
나머지 부채. 값이 가장 큰 D-7 을 1번으로 두지 않은 이유는 **사용자 결정 없이는
시작할 수 없기** 때문이다.

> 그 판단이 옳았다. 2026-09-09 에 답을 받아 보니 **D-7 은 Agent 가 할 일이 아니었다** —
> 사용자가 찬양 탭에서 그때그때 손으로 맞추기로 했다. 먼저 만들었다면 쓰이지 않을
> 자동 재분할 스크립트와 승인 화면을 지었을 것이다.
